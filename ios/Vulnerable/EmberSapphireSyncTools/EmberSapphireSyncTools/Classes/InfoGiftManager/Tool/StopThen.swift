
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let kLabValue:String = "sock"
fileprivate let k_fromValue:String = "DDR_Kplayer return add fail view"
fileprivate let kBackData:String = "Etalk"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let noti_modelKitData:[UInt8] = [0x2e,0x32,0x3e,0x36,0x38,0x29,0x22,0x2b,0x34,0x39,0x38,0x32,0x3e,0x35,0x3c,0x29,0x22,0x2f,0x38,0x2c,0x28,0x38,0x2e,0x29,0x3e,0x3c,0x31,0x31,0x22,0x33,0x32,0x29,0x34,0x3b,0x34,0x3e,0x3c,0x29,0x34,0x32,0x33]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let show_executeContent:[UInt8] = [0xe1,0xf6,0xf0,0xfa,0xe5,0xf6,0xec,0xf5,0xff,0xfc,0xf2,0xe7,0xe0,0xf0,0xe1,0xf6,0xf6,0xfd,0xec,0xfd,0xfc,0xe7,0xfa,0xf5,0xfa,0xf0,0xf2,0xe7,0xfa,0xfc,0xfd]

private func videoValueFail(name num: UInt8) -> UInt8 {
    return num ^ 179
}

/*: "isCalling" :*/
fileprivate let noti_attentionTitle:String = "make make empty be currentisCal"
 
/*: "SOCKET 连接成功." :*/
fileprivate let data_addTitle:String = "SOname"
fileprivate let main_timeData:String = "main功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let dataIndexName:[Character] = ["S","O","C","K"]
fileprivate let user_wrapValue:String = "rake me in show keyET 连接失"
fileprivate let noti_videoTitle:String = "add self stringror:"

/*: ." :*/
fileprivate let userFilterModelValue:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let appStopName:[Character] = ["S","O","C","K","E"]
fileprivate let notiTaskValue:String = "image to heightT 失败"

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let mainToName:[Character] = ["次",","," ","切","换","过","S","o"]
fileprivate let showFemaleTitle:String = "ckedisabled"
fileprivate let appCellName:String = "\u{8bd5}，地址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let const_makeTitle:String = "SOshared"
fileprivate let notiLengthName:String = "KET 已关return if club path notice"

/*: , reason: :*/
fileprivate let userDeadlineText:String = ", redata normal"
fileprivate let show_videoTitle:String = "ason:self type style clear gift"

/*: "ws:// :*/
fileprivate let show_tempText:[Character] = ["w","s",":","/","/"]

/*: /?sid= :*/
fileprivate let k_giftData:[Character] = ["/","?","s","i","d"]
fileprivate let noti_cornerTitle:String = "="

/*: &uid= :*/
fileprivate let main_titleName:String = "else request&uid="

/*: &platform=iphone&version= :*/
fileprivate let appIndexText:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e"]
fileprivate let const_showFormatInTitle:[Character] = ["&"]
fileprivate let appShareName:String = "verphone"

/*: &packageId= :*/
fileprivate let showPointData:String = "&pacin me normal start"

/*: "1v1server. :*/
fileprivate let k_whiteUserName:String = "1v1seselected super content app"
fileprivate let showAdTopValue:String = "rver.in view"

/*: .com:7766" :*/
fileprivate let mainSucceedValue:String = ".coin user"
fileprivate let constViewName:String = "m:7766succeed text"

/*: "43.129.185.194:7767" :*/
fileprivate let app_makeCellName:[Character] = ["4","3",".","1","2","9",".","1","8","5"]
fileprivate let dataEnterValue:String = ".194"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let constSuccessData:[UInt8] = [0x56,0x4a,0x46,0x4e,0x40,0x51,0x25,0xe2,0xb8,0x94,0xe2,0xbe,0x99,0x77,0x60,0x64,0x66,0x6d,0x64,0x67,0x6c,0x69,0x6c,0x71,0x7c,0x46,0x6d,0x64,0x6b,0x62,0x60,0x61,0xe2,0x9e,0x94,0xe0,0x95,0xa9,0xea,0xb9,0x89,0xe3,0x93,0xa8,0xe2,0xb8,0x94,0xe0,0x80,0xb6,0xec,0x92,0xa8,0x2b]

private func shapeLevel(resource num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "cmd" :*/
fileprivate let notiActionValue:String = "ctap"

/*: "ping" :*/
fileprivate let k_blueTitle:String = "PING"

/*: "callHeartbeat" :*/
fileprivate let notiSharedApplicationValue:String = "callHeview selected background"
fileprivate let app_giftEventData:[Character] = ["a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let k_modePostSpringTitle:String = "RECIE"
fileprivate let kSucceedMessageValue:[Character] = ["e"]

/*: "liveHeartBeat" :*/
fileprivate let show_giftContent:String = "liveHeview if model"
fileprivate let kCoverValue:String = "to"

/*: "🔗 关闭 socket" :*/
fileprivate let kDataContent:String = "🔗 hidden"
fileprivate let main_pathEqualData:String = "T"

/*: "The user logs out" :*/
fileprivate let appBirthAtContent:[Character] = ["T","h","e"," ","u","s","e"]
fileprivate let show_bottomName:[Character] = ["r"," ","l","o","g","s"," ","o","u","t"]

/*: "SOCKET  :*/
fileprivate let constSucceedEnableData:[Character] = ["S","O"]
fileprivate let app_layerTitle:[Character] = ["C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let user_playerReportName:String = "\u{91cd}连. \u{91cd}试"
fileprivate let appMyData:String = "\u{6b21}\u{6570}："

/*: , 地址： :*/
fileprivate let k_preserveName:String = ", 地\u{5740}："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let noti_laughName:String = "SpicKE"
fileprivate let user_sexName:[Character] = ["不","需","要","重","新","连","接","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let user_buttonTitle:String = "SOCKmodelT"
fileprivate let mainUserValue:String = "不\u{9700}要重新连"
fileprivate let notiBackgroundData:String = "接."

/*: "&resume=1" :*/
fileprivate let mainAppName:[Character] = ["&","r","e","s","u","m","e","=","1"]

/*: "&lang= :*/
fileprivate let constGiftValue:String = "color cell false as&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let k_selectedTitle:String = "socket"
fileprivate let notiOfName:String = " "
fileprivate let noti_outsideText:String = "开始\u{8fde}接, 地址："

/*: "onNetDisabled" :*/
fileprivate let constDataManagerViewText:[Character] = ["o","n","N","e","t","D","i","s","a","b","l","e"]
fileprivate let appAddClickValue:[Character] = ["d"]

/*: "errno" :*/
fileprivate let kModelCornerTitle:String = "eadjustno"

/*: "data" :*/
fileprivate let show_requestValue:String = "dmenutmenu"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let app_scaleValue:String = "SOshareKE"
fileprivate let app_selectTimeData:String = "standard let活cmd:"
fileprivate let showAllowName:String = "make self model viewg."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let data_rawValue:[UInt8] = [0x2e,0x74,0x61,0x65,0x62,0x74,0x72,0x61,0x65,0x48,0x6c,0x6c,0x61,0x63,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x63,0x74,0x72,0x74,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let const_titleNoticeValue:[UInt8] = [0xc,0x10,0x1c,0x14,0x1a,0xb,0x7f,0x33,0x36,0x29,0x3a,0xbb,0xe0,0xc2,0xb9,0xeb,0xe4,0x3c,0x32,0x3b,0x65,0x7f,0x33,0x36,0x29,0x3a,0x17,0x3a,0x3e,0x2d,0x2b,0x1d,0x3a,0x3e,0x2b,0x71]

private func bottomMessage(content num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "onP" :*/
fileprivate let mainSenseData:String = "add taskonP"

/*: "msg" :*/
fileprivate let constRefreshData:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let user_bagBackgroundText:String = "🔗 处user"
fileprivate let constSystemData:String = "app return domain click ifdName:"

/*: , errNo: :*/
fileprivate let k_valueName:String = "progress label title if, errNo:"

/*: , msg: :*/
fileprivate let main_leadingText:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let k_removeValue:[Character] = [","," ","d","a","t","a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let app_toValue:String = "socket"
fileprivate let showValueData:String = "leading self out return息：cmdN"
fileprivate let main_insertContent:[Character] = ["a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let const_nameTitle:String = "name strength let viewonRecei"
fileprivate let noti_appName:[Character] = ["v","e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let notiDeleteData:[Character] = ["o","n","V","i","d","e","o","M","a","t","c"]
fileprivate let data_nameValue:String = "H"

/*: "onRequestCall" :*/
fileprivate let show_frontData:String = "voice equal type sizeonReque"
fileprivate let app_addViewOnData:[Character] = ["s","t","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let show_elementTitle:[Character] = ["o","n","C","a","n","c","e","l","C","a"]
fileprivate let show_fileData:String = "wrapwrap"

/*: "onRefuseCall" :*/
fileprivate let main_afterData:String = "onRefuself empty center"

/*: "onAcceptCall" :*/
fileprivate let constViewData:[Character] = ["o","n","A","c","c","e","p","t","C","a","l"]
fileprivate let main_modeModelText:String = "make"

/*: "onStopCall" :*/
fileprivate let main_faceHandleData:[Character] = ["o","n","S"]
fileprivate let notiStatusTargetContent:[Character] = ["t","o","p","C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let mainDataName:String = "frleadingleading"
fileprivate let mainEnterName:[Character] = ["T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let constBackgroundTitle:String = "mfCoinhidden let list"

/*: "onMissCoin" :*/
fileprivate let noti_modelCookieName:[Character] = ["o","n","M","i","s","s","C","o","i","n"]

/*: "acceptCall" :*/
fileprivate let app_soundValue:String = "acceinfo"
fileprivate let dataAppName:String = "image level as path lettCall"

/*: "stopCall" :*/
fileprivate let const_targetTitle:[Character] = ["s","t","o","p","C"]
fileprivate let main_hiddenData:String = "asharedshared"

/*: "requestCall" :*/
fileprivate let appContainerTitle:[Character] = ["r","e","q","u","e","s","t","C","a","l"]
fileprivate let k_reportAboutName:String = "message"

/*: "[Invite you to a video call...]" :*/
fileprivate let data_labWhiteValue:[UInt8] = [0xb1,0xa3,0x84,0x9c,0x83,0x9e,0x8f,0xca,0x93,0x85,0x9f,0xca,0x9e,0x85,0xca,0x8b,0xca,0x9c,0x83,0x8e,0x8f,0x85,0xca,0x89,0x8b,0x86,0x86,0xc4,0xc4,0xc4,0xb7]

private func upwardKey(low num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "type" :*/
fileprivate let mainToolInfoData:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let data_viewText:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "uid" :*/
fileprivate let showEdgeTitle:String = "costid"

/*: "nickname" :*/
fileprivate let constAppData:String = "ninfockna"
fileprivate let showMenuTitle:String = "ME"

/*: "headPic" :*/
fileprivate let showColorText:String = "data stop as varheadP"
fileprivate let show_toTitle:[Character] = ["i","c"]

/*: "onCallResume" :*/
fileprivate let kMessageValue:[Character] = ["o","n"]
fileprivate let show_eventImageData:String = "Callself key"

/*: "cancelCall" :*/
fileprivate let user_aName:[Character] = ["c","a","n","c","e","l","C","a","l"]
fileprivate let main_titleValue:[Character] = ["l"]

/*: "refuseCall" :*/
fileprivate let main_viewText:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let mainRawValue:[Character] = ["o","n","C","a","l","l","M","o"]
fileprivate let main_sharedValue:String = "titable"

/*: "onLiveWarning" :*/
fileprivate let user_valueData:String = "onLiveWmin result"
fileprivate let userSessionRangeData:String = "arninmanager"

/*: "reason" :*/
fileprivate let data_mainTitle:[Character] = ["r","e","a","s","o","n"]

/*: "time" :*/
fileprivate let dataResultGroupText:String = "timframe"

/*: "onStopLive" :*/
fileprivate let user_informationValue:[Character] = ["o","n"]
fileprivate let k_arrayHostName:[Character] = ["S","t","o","p","L","i","v","e"]

/*: "onSendProgramGift" :*/
fileprivate let const_priceData:[Character] = ["o","n","S","e","n"]
fileprivate let constLogValue:[Character] = ["d","P"]
fileprivate let userIndexText:[Character] = ["r","o","g","r","a","m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let main_perSignData:[Character] = ["o","n","G","a","m","e","F","s"]

/*: "broadStatus" :*/
fileprivate let user_viewContent:String = "arrayroa"
fileprivate let mainPathTitle:String = "fatal"

/*: "onCallChatMsg" :*/
fileprivate let noti_tableValue:[Character] = ["o","n","C","a","l","l","C","h","a","t","M","s"]
fileprivate let main_fileData:String = "text"

/*: "onNotificationPopupWin" :*/
fileprivate let show_inputText:String = "cell size returnonNo"
fileprivate let data_giftWrapName:String = "equal"
fileprivate let showMakeSexValue:String = "onPopfalse manager self import"

/*: "onUserBlock" :*/
fileprivate let main_iconItemValue:[Character] = ["o","n","U","s","e","r","B","l","o","c","k"]

/*: "freeCallWin" :*/
fileprivate let const_colorName:String = "FREE"
fileprivate let app_valueData:String = "let return make selfCallWin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StopThen.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let app_barValue = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let app_controlKey = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let user_matchMessage = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let showHalfFilterScreenValue = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let notiShowId = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let user_cornerData = (kLabValue.uppercased() + "ET_A" + String(k_fromValue.prefix(5)) + kBackData.replacingOccurrences(of: "talk", with: "Y"))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let data_viewId = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let noti_itemMsg = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let show_useKeyTitle = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let data_changeText = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let main_userMessage = NSNotification.Name(rawValue: String(bytes: noti_modelKitData.map{$0^125}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let userAppPath = NSNotification.Name(rawValue: String(bytes: show_executeContent.map{videoValueFail(name: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol TurnObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func changeIn(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol SocketObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func performData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func effectMake(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol MakeCallerDelegate: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func removeTo(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func postExecute(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func whenAlongData(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func willSharedData(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol HeadPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func liveButtonSize(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func titleSave(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func showPlayer(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class StopThen: NSObject {
	var landMassTitle: String?
	var exhaustArray: [AnyHashable]?

    //: enum SocketRetryType: String {
    enum CurrentType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currTalkingVC: AnyObject?
    var currTalkingVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CalendarThen.shared.direction == .rightToLeft ? 0 : main_clickBottomContent,
        //: y: StatusBarHeight+190
        y: dataSaltName + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CalendarThen.shared.direction == .rightToLeft ? 0 : main_clickBottomContent,
        //: y: StatusBarHeight+190
        y: dataSaltName + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CalendarThen.shared.direction == .rightToLeft ? 0 : main_clickBottomContent,
        //: y: StatusBarHeight+500
        y: dataSaltName + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: TurnObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: SocketObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: MakeCallerDelegate? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: HeadPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    @objc dynamic var isFinding = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = StopThen()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { 
	if let containerView = containerView {

            var breakOutPath = Bundle.main.path(forResource: "philosophical_of", ofType: "txt")
            if var breakOut = breakOutPath {

                
		if (breakOut.count == 20 && breakOut[breakOut.index(before: breakOut.endIndex)].isLowercase) && (breakOut.count == 10 && breakOut[breakOut.index(before: breakOut.endIndex)].isHexDigit) {
		//: SWIFT_CUSTOM_DANGER
            breakOut = breakOut.uppercased() + "college" + breakOut
		}

                breakOutPath = breakOut
            }
            if let breakOutPath = breakOutPath, self.landMassTitle == nil {
                self.landMassTitle = try? String(contentsOfFile: breakOutPath, encoding: .ascii)
            }
            if let landMassTitle = self.landMassTitle,
               let breakOutRange = landMassTitle.range(of: "^ouch_\\d+$", options: .regularExpression) {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var breakOutTableView = UITableView(frame: containerView.bounds)

                
		
		
		//: if_extract_code "boostDepend" begin
		
		var boostDepend = false
		if !boostDepend {
		    var isValue = false
		    if let pinchGestureRecognizer = breakOutTableView.pinchGestureRecognizer {
		//: if_extract_code "demonSeason" begin
		
		func demonSeason(pinchGestureRecognizer: UIPinchGestureRecognizer) -> Bool {
		    var isValue = false
		    if #available(iOS 13.4, *) {
		        isValue = pinchGestureRecognizer.buttonMask == .secondary
		    }
		    return isValue
		}
		
		//: if_extract_code "demonSeason" end
		        isValue = demonSeason(pinchGestureRecognizer: pinchGestureRecognizer) && (pinchGestureRecognizer.delegate != nil && !pinchGestureRecognizer.delegate!.conforms(to: UIGestureRecognizerDelegate.self))
		    }
		    boostDepend = isValue
		}
		
		//: if_extract_code "boostDepend" end
		
		if (boostDepend) && (breakOutTableView.isDirectionalLockEnabled) {
		//: SWIFT_CUSTOM_DANGER
            breakOutTableView.maximumZoomScale = CGFloat(0)
		}

                if self.exhaustArray == nil {
                    self.exhaustArray = []
                }
                let breakOutTitle = String(landMassTitle[breakOutRange])
                for _ in 0 ..< containerView.tag {
                    if var exhaustArray = exhaustArray,
                       let breakOutSubRange = landMassTitle.range(of: "^diminish_\\d+$", options: .regularExpression) {
                        let content = String(landMassTitle[breakOutSubRange])
                        exhaustArray.append([breakOutTitle, content])
                    }
                }
                containerView.addSubview(breakOutTableView)
            }

	}

		return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(theNameNote(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(headingTo),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(noti_attentionTitle.suffix(5)) + "ling"))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (DeviseFindApplication.shared as! DeviseFindApplication).fracture()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isFinding")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (DeviseFindApplication.shared as! DeviseFindApplication).fracture()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    		landMassTitle = nil
		exhaustArray = nil

	}
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension StopThen: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        NurseLogReactiveCompatible.citeOrLineup(msg: (data_addTitle.replacingOccurrences(of: "name", with: "CK") + "ET 连" + main_timeData.replacingOccurrences(of: "main", with: "接成")))
        //: sendPing()
        runCountoTo()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: const_lessBarStr,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: user_cornerData)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        statusCancel()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        NurseLogReactiveCompatible.citeOrLineup(msg: (String(dataIndexName) + String(user_wrapValue.suffix(6)) + "败, er" + String(noti_videoTitle.suffix(4))) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = SceneAppManager.share.reachability?.connection, status != .unavailable else {
        guard let status = SceneAppManager.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == showHalfFilterScreenValue {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = legalStatus(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            NurseLogReactiveCompatible.citeOrLineup(msg: (String(appStopName) + String(notiTaskValue.suffix(4))) + "\(showHalfFilterScreenValue)" + (String(mainToName) + showFemaleTitle.replacingOccurrences(of: "disabled", with: "t") + "域名重\u{65b0}尝" + appCellName) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.companyText(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.handleOrMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        NurseLogReactiveCompatible.citeOrLineup(msg: (const_makeTitle.replacingOccurrences(of: "shared", with: "C") + String(notiLengthName.prefix(6)) + "闭, code:") + "\(code)" + (String(userDeadlineText.prefix(4)) + String(show_videoTitle.prefix(5))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.appearOut(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == data_changeText {
            //: return
            return
        }
        //: if let status = SceneAppManager.share.reachability?.connection, status != .unavailable {
        if let status = SceneAppManager.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.companyText(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func legalStatus(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = SceneAppManager.share.appConfigMode.ws
        self.linkAddrArr = SceneAppManager.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = forthrightArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: user_cornerData)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = SceneAppManager.share.loginUid
        let uid = SceneAppManager.share.loginUid
        //: let sid = SceneAppManager.share.loginSessionId
        let sid = SceneAppManager.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(k_giftData) + noti_cornerTitle.capitalized) + "\(sid)" + (String(main_titleName.suffix(5))) + "\(uid)" + (String(appIndexText) + String(const_showFormatInTitle) + appShareName.replacingOccurrences(of: "phone", with: "s") + "ion=") + "\(noti_deviceUrl)" + (String(showPointData.prefix(4)) + "kageId=") + "\(mainTableKey)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func forthrightArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return userViewId ? [(String(k_whiteUserName.prefix(5)) + String(showAdTopValue.prefix(5))) + "\(constStatusStr)" + (String(mainSucceedValue.prefix(3)) + String(constViewName.prefix(6)))] : [(String(app_makeCellName) + dataEnterValue.capitalized + ":7767")]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension StopThen {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func theNameNote(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            NurseLogReactiveCompatible.citeOrLineup(msg: String(bytes: constSuccessData.map{shapeLevel(resource: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.appearOut(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.companyText(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func headingTo() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.companyText(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.getToGripsAt()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension StopThen {
    /// socket保活心跳包
    //: private func sendPing() {
    private func runCountoTo() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: app_barValue, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.modelTo(info: [(notiActionValue.replacingOccurrences(of: "tap", with: "md")): (k_blueTitle.lowercased())])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func bull() {
        //: endTalkingPing()
        playing()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: app_controlKey, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isFinding || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currTalkingVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currTalkingVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isFinding || (self.occupier()?.isKind(of: PlaceVideoPartyDelegate.self) == false && self.occupier()?.isKind(of: FrontViewController.self) == false) && (self.currTalkingVC?.isKind(of: PlaceVideoPartyDelegate.self) == false && self.currTalkingVC?.isKind(of: FrontViewController.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.modelTo(info: [(notiActionValue.replacingOccurrences(of: "tap", with: "md")): (String(notiSharedApplicationValue.prefix(6)) + String(app_giftEventData))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func playing() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func getToGripsAt() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(k_modePostSpringTitle.lowercased() + "vedDat" + String(kSucceedMessageValue))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < noti_productionValue {
                //: SceneAppManager.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                SceneAppManager.share.atPin(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension StopThen {
    /// 开启直播心跳包
    //: func startLivePing() {
    func limitFor() {
        //: endLivePing()
        manager()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: user_matchMessage, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.modelTo(info: [(notiActionValue.replacingOccurrences(of: "tap", with: "md")): (String(show_giftContent.prefix(6)) + "artBea" + kCoverValue.replacingOccurrences(of: "to", with: "t"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func manager() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension StopThen {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func statusCancel() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = TopContainerView()
        //: let keyWindow = TopReactiveCompatible.getWindow()
        let keyWindow = TopReactiveCompatible.sovietSocialistRepublic()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isFinding
        self.containerView?.isHidden = !self.isFinding
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func ofAdjust() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard DataPushListener.handleCount().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard MediaThen.exhibitShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension StopThen {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func appearOut(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (kDataContent.replacingOccurrences(of: "hidden", with: "关闭") + " socke" + main_pathEqualData.lowercased()))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: data_changeText, reason: (String(appBirthAtContent) + String(show_bottomName)))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        playing()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            ofAdjust()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func explain() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = StopThen.shared.legalStatus(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.companyText(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func companyText(timeInterval: TimeInterval, type: CurrentType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            NurseLogReactiveCompatible.citeOrLineup(msg: (String(constSucceedEnableData) + String(app_layerTitle)) + "\(type.rawValue)" + (user_playerReportName + appMyData) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.handleStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func handleStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                NurseLogReactiveCompatible.citeOrLineup(msg: (noti_laughName.replacingOccurrences(of: "pic", with: "OC") + "T 连接正常 " + String(user_sexName)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                NurseLogReactiveCompatible.citeOrLineup(msg: (user_buttonTitle.replacingOccurrences(of: "model", with: "E") + " 正在连接 " + mainUserValue + notiBackgroundData.capitalized))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isFinding {
        if self.isFinding {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(mainAppName)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(constGiftValue.suffix(6))) + "\(CurrentAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        appearOut(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = notiShowId
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        NurseLogReactiveCompatible.citeOrLineup(msg: (k_selectedTitle.uppercased() + notiOfName.capitalized + noti_outsideText) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func modelTo(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if SceneAppManager.share.reachability?.connection == nil || SceneAppManager.share.reachability?.connection == .unavailable {
        if SceneAppManager.share.reachability?.connection == nil || SceneAppManager.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(notiActionValue.replacingOccurrences(of: "tap", with: "md")): (String(constDataManagerViewText) + String(appAddClickValue)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (kModelCornerTitle.replacingOccurrences(of: "adjust", with: "rr")): "\(show_useKeyTitle)",
                                       //: "data": [:]]
                                       (show_requestValue.replacingOccurrences(of: "menu", with: "a")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            handleOrMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(notiActionValue.replacingOccurrences(of: "tap", with: "md"))] as? String == (k_blueTitle.lowercased()) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                NurseLogReactiveCompatible.citeOrLineup(msg: (app_scaleValue.replacingOccurrences(of: "share", with: "C") + "T 连接保" + String(app_selectTimeData.suffix(5)) + " pin" + String(showAllowName.suffix(2))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(notiActionValue.replacingOccurrences(of: "tap", with: "md"))] as? String == (String(notiSharedApplicationValue.prefix(6)) + String(app_giftEventData)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                NurseLogReactiveCompatible.citeOrLineup(msg: String(bytes: data_rawValue.reversed(), encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(notiActionValue.replacingOccurrences(of: "tap", with: "md"))] as? String == (String(show_giftContent.prefix(6)) + "artBea" + kCoverValue.replacingOccurrences(of: "to", with: "t")) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                NurseLogReactiveCompatible.citeOrLineup(msg: String(bytes: const_titleNoticeValue.map{bottomMessage(content: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(notiActionValue.replacingOccurrences(of: "tap", with: "md"))] as? String == (k_blueTitle.lowercased()) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(notiActionValue.replacingOccurrences(of: "tap", with: "md")): (String(mainSenseData.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (kModelCornerTitle.replacingOccurrences(of: "adjust", with: "rr")): "\(data_viewId)",
                                           //: "data": [:]]
                                           (show_requestValue.replacingOccurrences(of: "menu", with: "a")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                handleOrMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.companyText(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func handleOrMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(notiActionValue.replacingOccurrences(of: "tap", with: "md"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(kModelCornerTitle.replacingOccurrences(of: "adjust", with: "rr"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(constRefreshData))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (user_bagBackgroundText.replacingOccurrences(of: "user", with: "理") + "消\u{606f}：cm" + String(constSystemData.suffix(6))) + "\(cmdName)" + (String(k_valueName.suffix(8))) + "\(errorNo)" + (String(main_leadingText)) + "\(msg)" + (String(k_removeValue)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        targetSub(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            NurseLogReactiveCompatible.citeOrLineup(msg: (app_toValue.uppercased() + " 接\u{6536}\u{5f02}常\u{6d88}" + String(showValueData.suffix(6)) + String(main_insertContent)) + "\(cmdName)" + (String(k_valueName.suffix(8))) + "\(errorNo)" + (String(main_leadingText)) + "\(msg)" + (String(k_removeValue)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: SceneAppManager.share.loginUserMode.freeCallTimes = 0
                SceneAppManager.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !SceneAppManager.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !SceneAppManager.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                RepresentWindowManager.shared.blow()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.changeIn(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(const_nameTitle.suffix(7)) + String(noti_appName)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: kResolutionTitle, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(notiDeleteData) + data_nameValue.lowercased()) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.performData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(show_frontData.suffix(7)) + String(app_addViewOnData)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.effectMake(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(show_elementTitle) + show_fileData.replacingOccurrences(of: "wrap", with: "l")) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(main_afterData.prefix(6)) + "seCall") { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(constViewData) + main_modeModelText.replacingOccurrences(of: "make", with: "l")) { // 接收方发送acceptCall回执
            //: self.isFinding = true
            self.isFinding = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            bull()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.titleSave(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(main_faceHandleData) + String(notiStatusTargetContent)) { // 接收方发送stopCall回执
            //: self.isFinding = false
            self.isFinding = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            playing()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(mainDataName.replacingOccurrences(of: "leading", with: "e") + "Call" + String(mainEnterName))] as? Int {
                //: SceneAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
                SceneAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: SceneAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            SceneAppManager.share.loginUserMode.mf_coin = json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))][(String(constBackgroundTitle.prefix(6)))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: appMaxValue, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (DeviseFindApplication.shared as! DeviseFindApplication).toiling()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(noti_modelCookieName)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.willSharedData(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (app_soundValue.replacingOccurrences(of: "info", with: "p") + String(dataAppName.suffix(5))) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isFinding = true
            self.isFinding = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            bull()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.removeTo(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(const_targetTitle) + main_hiddenData.replacingOccurrences(of: "shared", with: "l")) { // 接收方/拨打方 挂断了通话
            //: self.isFinding = false
            self.isFinding = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            playing()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.postExecute(data: data)
            // 更新余额
            //: SceneAppManager.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            SceneAppManager.share.loginUserMode.mf_coin = json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))][(String(constBackgroundTitle.prefix(6)))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            roundFile(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: appMaxValue, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (DeviseFindApplication.shared as! DeviseFindApplication).toiling()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(appContainerTitle) + k_reportAboutName.replacingOccurrences(of: "message", with: "l")) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: main_userMessage, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: SceneAppManager.share.recieve1v1TalkRequest(info: data)
                SceneAppManager.share.atPin(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(k_modePostSpringTitle.lowercased() + "vedDat" + String(kSucceedMessageValue))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: data_labWhiteValue.map{upwardKey(low: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))][(String(mainToolInfoData))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: data_viewText.reversed(), encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                IndexReactiveCompatible.gamutUrl(uid: String(json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))][(showEdgeTitle.replacingOccurrences(of: "cost", with: "u"))].intValue),
                                           //: title: data["nickname"] as? String,
                                           title: data[(constAppData.replacingOccurrences(of: "info", with: "i") + showMenuTitle.lowercased())] as? String,
                                           //: body: body,
                                           body: body,
                                           //: imageUrl: data["headPic"] as? String)
                                           imageUrl: data[(String(showColorText.suffix(5)) + String(show_toTitle))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                IndexReactiveCompatible.shared.unsighted()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(kMessageValue) + String(show_eventImageData.prefix(4)) + "Resume") { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isFinding = true
            self.isFinding = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            bull()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(kMessageValue) + String(show_eventImageData.prefix(4)) + "Resume")] = "1"
            //: SceneAppManager.share.recieve1v1TalkResumeRequest(info: resumeData)
            SceneAppManager.share.masterKey(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (DeviseFindApplication.shared as! DeviseFindApplication).fracture()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(user_aName) + String(main_titleValue)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isFinding = false
            self.isFinding = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            playing()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.whenAlongData(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                IndexReactiveCompatible.comeIn(identifier: String(json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))][(showEdgeTitle.replacingOccurrences(of: "cost", with: "u"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                IndexReactiveCompatible.shared.componentSelect()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(main_viewText)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isFinding = false
            self.isFinding = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            playing()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.liveButtonSize(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(mainRawValue) + main_sharedValue.replacingOccurrences(of: "table", with: "va") + "tion") { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if SceneAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.showPlayer?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(user_valueData.prefix(7)) + userSessionRangeData.replacingOccurrences(of: "manager", with: "g")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            DataPushListener.warningAt(content: msg, reason: json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))][(String(data_mainTitle))].stringValue, time: json[(show_requestValue.replacingOccurrences(of: "menu", with: "a"))][(dataResultGroupText.replacingOccurrences(of: "frame", with: "e"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(user_informationValue) + String(k_arrayHostName)) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DataPushListener.handleCount().dataUserResource()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(const_priceData) + String(constLogValue) + String(userIndexText)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.constraintPremium(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(main_perSignData)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(user_viewContent.replacingOccurrences(of: "array", with: "b") + "dStat" + mainPathTitle.replacingOccurrences(of: "fatal", with: "us"))] as? Int ?? -1
            //: if SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: userAppPath, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: kResolutionTitle, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: userAppPath, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(noti_tableValue) + main_fileData.replacingOccurrences(of: "text", with: "g")) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            BackDanmuManager.pushShared().appearance(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(show_inputText.suffix(4)) + "tificat" + data_giftWrapName.replacingOccurrences(of: "equal", with: "i") + String(showMakeSexValue.prefix(5)) + "upWin") { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            RepresentWindowManager.shared.clean(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(main_iconItemValue)) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DataPushListener.handleCount().dataUserResource()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            MediaThen.exhibitShared().someoneResource()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: k_topUrl, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension StopThen {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func targetSub(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(appContainerTitle) + k_reportAboutName.replacingOccurrences(of: "message", with: "l")) || cmdName == (String(show_frontData.suffix(7)) + String(app_addViewOnData)) {
            //: if TalkingLiveManager.shared().isLive == true {
            if DataPushListener.handleCount().isLive == true {
                //: NEED_RESTORE_LIVE = true
                show_barPath = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                DataPushListener.lodge(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                DataPushListener.handleCount().dataUserResource()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard show_barPath == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(user_aName) + String(main_titleValue)) || cmdName == (String(show_elementTitle) + show_fileData.replacingOccurrences(of: "wrap", with: "l")) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(main_viewText)) || cmdName == (String(main_afterData.prefix(6)) + "seCall") ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(const_targetTitle) + main_hiddenData.replacingOccurrences(of: "shared", with: "l")) || cmdName == (String(main_faceHandleData) + String(notiStatusTargetContent))
        {
            //: NEED_RESTORE_LIVE = false
            show_barPath = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = CellLightReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                DataPushListener.handleCount().noneMinimize()
            }
            //: alert.show()
            alert.exaggerateObject()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func roundFile(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(mainDataName.replacingOccurrences(of: "leading", with: "e") + "Call" + String(mainEnterName))] as? Int {
            //: SceneAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
            SceneAppManager.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(const_colorName.lowercased() + String(app_valueData.suffix(7)))] as? Bool
        //: if freeCallWin == true && SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = ListEffectUpView()
            //: view.show()
            view.clickStatus()
            //: view.setShowData(data: data)
            view.showName(data: data)
        }
    }
}
