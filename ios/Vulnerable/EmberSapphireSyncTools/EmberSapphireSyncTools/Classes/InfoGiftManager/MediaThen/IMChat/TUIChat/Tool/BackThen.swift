
//: Declare String Begin

/*: "msgType" :*/
fileprivate let dataTableContent:String = "msgTypename button pad"

/*: "tips" :*/
fileprivate let showColorName:String = "tfatalps"

/*: "extra" :*/
fileprivate let user_buttonLabelTitle:String = "etitlera"

/*: "msgInfo" :*/
fileprivate let main_normalTitle:[Character] = ["m"]
fileprivate let kToText:String = "sgInfodouble white layer image as"

/*: "user" :*/
fileprivate let mainTalkOfValueData:String = "usecolor"

/*: "uid" :*/
fileprivate let kCustomData:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let user_bottomTitle:[UInt8] = [0xb7,0xb0,0xba,0xb2,0xb7,0xb8,0xb4,0xbc]

private func sizeLayer(list num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "headPic" :*/
fileprivate let user_exhibitText:[UInt8] = [0x9,0x4,0x0,0x5,0x31,0x8,0x2]

private func managerWith(icon num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "age" :*/
fileprivate let notiUserValue:[UInt8] = [0x79,0x7f,0x7d]

private func toImage(title num: UInt8) -> UInt8 {
    return num ^ 24
}

/*: "gift" :*/
fileprivate let noti_donContent:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let noti_requestName:[Character] = ["i","s","G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let constTapText:String = "adjust half ifWantGif"
fileprivate let data_valueTitle:String = "result"

/*: "txt" :*/
fileprivate let main_popName:String = "tmodel"

/*: "img" :*/
fileprivate let k_peerContent:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let notiRawBlackWhereData:String = "vminieo"

/*: "audio" :*/
fileprivate let k_frameName:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let show_createValue:String = "actualss"
fileprivate let data_cornerName:String = "inside actionageType"

/*: "contentType" :*/
fileprivate let appPlayerData:String = "CONTEN"
fileprivate let k_oldName:String = "error colortType"

/*: "ImgMsg" :*/
fileprivate let kIndexCustomName:String = "ImgMsgshare clear"

/*: "imageUri" :*/
fileprivate let notiShareName:String = "playerm"
fileprivate let show_layerText:String = "place equalageUri"

/*: "type" :*/
fileprivate let show_pageData:String = "tyof"

/*: "pic" :*/
fileprivate let show_playerTitle:String = "loadic"

/*: "isBurn" :*/
fileprivate let user_partyCountName:String = "isBurnview post self app"

/*: "giftId" :*/
fileprivate let appTranslateValue:String = "giftIdimage filter"

/*: "params" :*/
fileprivate let userAcceptData:[Character] = ["p","a","r","a","m","s"]

/*: "audioLength" :*/
fileprivate let notiEnableTitle:[Character] = ["a","u","d","i","o","L","e"]
fileprivate let data_colorViewName:[Character] = ["n","g","t","h"]

/*: "len" :*/
fileprivate let appChangeName:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let mainGestureText:String = "hiddenud"
fileprivate let main_buttonTitleContent:[Character] = ["i","o","S","a","n","d","b","o","x"]

/*: "senderId" :*/
fileprivate let const_voiceTitle:[Character] = ["s","e","n","d","e","r","I","d"]

/*: "AudioMsg" :*/
fileprivate let k_userToText:String = "Audiview show when"

/*: "content" :*/
fileprivate let user_convertData:[Character] = ["c","o","n","t","e","n","t"]

/*: "TxtMsg" :*/
fileprivate let main_clearValue:String = "TxtMsgtitle main super self let"

/*: "message" :*/
fileprivate let app_cellTitle:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let user_pageName:String = "QUO"
fileprivate let dataMakeMyIconContent:String = "manager white var background shareteMsgId"

/*: "quoteMsgInfo" :*/
fileprivate let main_infoIndexData:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "rYMsgType" :*/
fileprivate let mainCreateTitle:String = "count"
fileprivate let notiLayerNextName:[Character] = ["Y","M","s","g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let appDataName:String = "GJ:Sechange if"
fileprivate let appThirdValue:[Character] = ["r","e","e"]
fileprivate let user_reData:String = "data array giftt:Txt"

/*: "changeDirection" :*/
fileprivate let notiModelValue:String = "changeDimedium stop"
fileprivate let app_infoTableValue:String = "rimagetion"

/*: "system" :*/
fileprivate let constBurnValue:String = "clearstem"

/*: "title" :*/
fileprivate let mainPicName:[Character] = ["t","i","t","l","e"]

/*: "id" :*/
fileprivate let showLabelText:String = "contentd"

/*: "icon" :*/
fileprivate let const_labValue:String = "icresponsen"

/*: "all" :*/
fileprivate let showHandleLayerName:String = "amodelmodel"

/*: "icon_chats_pcr" :*/
fileprivate let noti_kindContent:[Character] = ["i","c","o","n","_","c","h","a","t","s","_","p","c","r"]

/*: "status" :*/
fileprivate let const_arrayRawFitValue:String = "sactiontus"

/*: "msgIncome" :*/
fileprivate let show_appearData:[Character] = ["m"]
fileprivate let kErrorModelTitle:String = "sgIncomemodel data image model left"

/*: "msgId" :*/
fileprivate let notiManagerData:[Character] = ["m","s","g","I","d"]

/*: "FrontTranslateStatus" :*/
fileprivate let noti_addTopData:String = "full toolTMsg"
fileprivate let k_videoCellKitName:String = "ateStatuto model up height tag"
fileprivate let kListTitle:[Character] = ["s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let dataWithName:String = "up gift color ifTMsgTrans"
fileprivate let app_colorViewText:[Character] = ["l","a","t","e","d","C","o","n","t","e","n","t"]

/*: "formatType" :*/
fileprivate let const_assetData:String = "flabelrm"
fileprivate let userMakeValue:[Character] = ["p","e"]

/*: "plain" :*/
fileprivate let app_infoValue:String = "plasectionn"

/*: "imageData" :*/
fileprivate let show_atValue:String = "imageDif text view"
fileprivate let main_modeData:[Character] = ["a","t","a"]

/*: "VideoMsg" :*/
fileprivate let dataLeadingName:String = "block"
fileprivate let app_addValue:String = "ideoMsgfor block"

/*: "match" :*/
fileprivate let show_contentData:String = "layertch"

/*: "mf" :*/
fileprivate let appAtValue:[Character] = ["m","f"]

/*: "userIntro" :*/
fileprivate let kBlockSharedContent:String = "userInapp begin equal"
fileprivate let show_partyValue:String = "trview"

/*: "targetId" :*/
fileprivate let app_clickLoadContent:[Character] = ["t","a","r","g","e","t","I"]
fileprivate let kTextData:[Character] = ["d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let user_countText:String = "return hidden erroryyyy-"
fileprivate let data_angleTableValue:String = " HH:min share video color"

/*: "sendTime" :*/
fileprivate let userListName:String = "value"
fileprivate let user_emptyName:String = "endTimevalue self item"

/*: "isRead" :*/
fileprivate let main_targetTitle:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let user_playerViewGiftTitle:[Character] = ["a","u","d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let kFinishName:[Character] = ["a","u","d","i","o","U","r"]
fileprivate let show_linkData:String = "request"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BackThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum HandbasketPropertyProtocol: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - BackThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class BackThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func delay() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> IndexEventCellData? {
    public class func partTip(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> IndexEventCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = containerPath(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: BackConversationListener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            BackConversationListener.shared.funcRequest(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> IndexEventCellData? {
    public class func containerPath(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> IndexEventCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(dataTableContent.prefix(7)))] = (showColorName.replacingOccurrences(of: "fatal", with: "i"))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(user_buttonLabelTitle.replacingOccurrences(of: "title", with: "xt")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = errorAllow(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = AddCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = QuantityeractionModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? ToModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(main_normalTitle) + String(kToText.prefix(6)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(mainTalkOfValueData.replacingOccurrences(of: "color", with: "r"))] = [
            //: "uid": SceneAppManager.share.loginUserMode.userID,
            (String(kCustomData)): SceneAppManager.share.loginUserMode.userID,
            //: "nickname": SceneAppManager.share.loginUserMode.nickname,
            String(bytes: user_bottomTitle.map{sizeLayer(list: $0)}, encoding: .utf8)!: SceneAppManager.share.loginUserMode.nickname,
            //: "headPic": SceneAppManager.share.loginUserMode.headPic,
            String(bytes: user_exhibitText.map{managerWith(icon: $0)}, encoding: .utf8)!: SceneAppManager.share.loginUserMode.headPic,
            //: "age": SceneAppManager.share.loginUserMode.age,
            String(bytes: notiUserValue.map{toImage(title: $0)}, encoding: .utf8)!: SceneAppManager.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(user_buttonLabelTitle.replacingOccurrences(of: "title", with: "xt")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = errorAllow(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(dataTableContent.prefix(7)))] as? String == (String(noti_donContent)) {
            //: var data: IndexEventCellData
            var data: IndexEventCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_requestName))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ImageCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = DirectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = DirectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? QuantityeractionModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(dataTableContent.prefix(7)))] as? String == (String(constTapText.suffix(7)) + data_valueTitle.replacingOccurrences(of: "result", with: "t")) {
            //: let data = ScreenCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ScreenCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? QuantityeractionModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(dataTableContent.prefix(7)))] as? String == (main_popName.replacingOccurrences(of: "model", with: "xt")) {
            //: var data: IndexEventCellData
            var data: IndexEventCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_requestName))] as? String == "1" { // 群聊
                //: data = AtCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = AtCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ExceptReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ExceptReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? QuantityeractionModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(dataTableContent.prefix(7)))] as? String == (String(k_peerContent)) {
            //: let data = DayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DayCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? QuantityeractionModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(dataTableContent.prefix(7)))] as? String == (notiRawBlackWhereData.replacingOccurrences(of: "mini", with: "id")) {
            //: let data = ToCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ToCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? QuantityeractionModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(dataTableContent.prefix(7)))] as? String == (String(k_frameName)) {
            //: let data = BirdFeederCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = BirdFeederCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? QuantityeractionModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(noti_donContent))] != nil && (msgInfo[(show_createValue.replacingOccurrences(of: "actual", with: "me") + String(data_cornerName.suffix(7)))] as? Int) == 4 {
            //: let data = ExceptReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ExceptReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? QuantityeractionModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: IndexEventCellData) -> IndexEventCellData? {
    public class func stylePhotoData(withData cellData: IndexEventCellData) -> IndexEventCellData? {
        // 图片
        //: if let reCellData = cellData as? DayCellData {
        if let reCellData = cellData as? DayCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(TopReactiveCompatible.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(TopReactiveCompatible.totalimate())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(appPlayerData.lowercased() + String(k_oldName.suffix(5)))] = (String(kIndexCustomName.prefix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(dataTableContent.prefix(7)))] = (String(k_peerContent))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(notiShareName.replacingOccurrences(of: "player", with: "i") + String(show_layerText.suffix(6)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(main_normalTitle) + String(kToText.prefix(6)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(show_pageData.replacingOccurrences(of: "of", with: "pe"))] = (String(kIndexCustomName.prefix(6)))
            //: params["pic"] = filePath
            params[(show_playerTitle.replacingOccurrences(of: "load", with: "p"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(user_partyCountName.prefix(6)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(user_partyCountName.prefix(6)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(appTranslateValue.prefix(6)))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(appTranslateValue.prefix(6)))] = "0"
            }

            //: dic["params"] = params
            dic[(String(userAcceptData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = containerPath(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? ToCellData {
        if let reCellData = cellData as? ToCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? BirdFeederCellData {
        if let reCellData = cellData as? BirdFeederCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = PlayerClickThen.toFilter(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(notiEnableTitle) + String(data_colorViewName))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(appChangeName))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(mainGestureText.replacingOccurrences(of: "hidden", with: "a") + String(main_buttonTitleContent))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(mainGestureText.replacingOccurrences(of: "hidden", with: "a") + String(main_buttonTitleContent))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(dataTableContent.prefix(7)))] = (String(k_frameName))
            //: msgInfo["senderId"] = SceneAppManager.share.loginUserMode.userID
            msgInfo[(String(const_voiceTitle))] = SceneAppManager.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(main_normalTitle) + String(kToText.prefix(6)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(show_pageData.replacingOccurrences(of: "of", with: "pe"))] = (String(k_userToText.prefix(4)) + "oMsg")
            //: dic["params"] = params
            dic[(String(userAcceptData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = containerPath(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ExceptReactiveCompatible {
        if let reCellData = cellData as? ExceptReactiveCompatible {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(user_convertData))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(dataTableContent.prefix(7)))] = (main_popName.replacingOccurrences(of: "model", with: "xt"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(main_normalTitle) + String(kToText.prefix(6)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(show_pageData.replacingOccurrences(of: "of", with: "pe"))] = (String(main_clearValue.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(app_cellTitle))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(user_pageName.lowercased() + String(dataMakeMyIconContent.suffix(7)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(main_infoIndexData))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(String(userAcceptData))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = containerPath(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> IndexEventCellData? {
    public class func freshMake(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> IndexEventCellData? {
        //: var cellData: IndexEventCellData?
        var cellData: IndexEventCellData?
        //: guard let extraDict = dic["extra"] as? [String: Any],
        let extraDict = dic[(user_buttonLabelTitle.replacingOccurrences(of: "title", with: "xt"))] as? [String: Any]
              //: let msgInfoDict = extraDict["msgInfo"] as? [String: Any] else {
        let msgInfoDict = extraDict?[(String(main_normalTitle) + String(kToText.prefix(6)))] as? [String: Any]

        //: let msgInfo = msgInfoDict
        let msgInfo = msgInfoDict
        //: let user = extraDict["user"] as? [String: Any] ?? [:]
        let user = dic[(mainTalkOfValueData.replacingOccurrences(of: "color", with: "r"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? QuantityeractionModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == SceneAppManager.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == SceneAppManager.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == SceneAppManager.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == SceneAppManager.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(mainCreateTitle.replacingOccurrences(of: "count", with: "r") + String(notiLayerNextName))] as? String == (String(appDataName.prefix(5)) + "ss:AutoG" + String(appThirdValue) + String(user_reData.suffix(5)) + "Custom") {
            //: isSelf = msgInfo["changeDirection"] as? Bool ?? false
            isSelf = msgInfo?[(String(notiModelValue.prefix(8)) + app_infoTableValue.replacingOccurrences(of: "image", with: "ec"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = inputMessage(dic)

        // 系统消息
        //: if extraDict["msgType"] as? String == "system" ||
        if extraDict?[(String(dataTableContent.prefix(7)))] as? String == (constBurnValue.replacingOccurrences(of: "clear", with: "sy")) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = QuantityeractionModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? ToModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AddCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(user_convertData))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = DayCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DayCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = ToCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ToCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ImageCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = DirectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = DirectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = atomicNumber85(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(user_buttonLabelTitle.replacingOccurrences(of: "title", with: "xt"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(main_normalTitle) + String(kToText.prefix(6)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? MaxModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = ScreenCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ScreenCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = BirdFeederCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = BirdFeederCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? BirdFeederCellData {
            if let audioCellData = cellData as? BirdFeederCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = tag(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = SumryMsgTable.withConfirm(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = SumryMsgTable.bring(behindMail(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ExceptToCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict["title"] as? String ?? ""
            let title = extraDict?[(String(mainPicName))] as? String ?? ""
            //: let content = extraDict["content"] as? String ?? ""
            let content = extraDict?[(String(user_convertData))] as? String ?? ""
            //: let img = extraDict["img"] as? String ?? ""
            let img = extraDict?[(String(k_peerContent))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = InfoCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = AtCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = AtCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ExceptReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ExceptReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfo) { (_, new) in new }
            userDict.merge(msgInfo ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? MailModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(const_labValue.replacingOccurrences(of: "response", with: "o"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: SceneAppManager.share.loginUserMode.headPic ?? ""
                        SceneAppManager.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = MaxModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(user_convertData))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(user_buttonLabelTitle.replacingOccurrences(of: "title", with: "xt"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = BallToModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (showHandleLayerName.replacingOccurrences(of: "model", with: "l"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(noti_kindContent))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.nameJsonDictionaryWithImage(withJsonString: customStr) ?? [:]
        //: let status = TMsgStatus(rawValue: UInt(customDic["status"] as? Int ?? 0)) ?? .Msg_Status_Init
        let statusStr = customDic["status"] as? String ?? "0"
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(show_appearData) + String(kErrorModelTitle.prefix(8)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfo["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfo?[(show_createValue.replacingOccurrences(of: "actual", with: "me") + String(data_cornerName.suffix(7)))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(notiManagerData))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(noti_addTopData.suffix(4)) + "Transl" + String(k_videoCellKitName.prefix(8)) + String(kListTitle))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = FrontTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(dataWithName.suffix(9)) + String(app_colorViewText))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = EqualValueModel.loadLocalIntimatePhotoDict()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = EqualValueModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = SoundMakeModel.loadLocalIntimateVideoDict()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = SoundMakeModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = PlayUserModel.loadAskForGiftArr()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func inputMessage(_ extraDic: [String: Any]) -> HandbasketPropertyProtocol {
        //: guard let dic = extraDic["extra"] as? [String: Any],
       let dic = extraDic[(user_buttonLabelTitle.replacingOccurrences(of: "title", with: "xt"))] as? [String: Any]
      //: let msgInfo = dic["msgInfo"] as? [String: Any],
      let msgInfo = dic?[(String(main_normalTitle) + String(kToText.prefix(6)))] as? [String: Any]
              //: let model = try? AbTalkingChatMsgBaseModel(dictionary: dic)
        guard let model = try? QuantityeractionModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic["formatType"] as? String == "plain" {
        if dic?[(const_assetData.replacingOccurrences(of: "label", with: "o") + "atTy" + String(userMakeValue))] as? String == (app_infoValue.replacingOccurrences(of: "section", with: "i")) {
            //: let imageUri = dic["imageUri"] as? String ?? ""
            let imageUri = dic?[(notiShareName.replacingOccurrences(of: "player", with: "i") + String(show_layerText.suffix(6)))] as? String ?? ""
            //: let imageData = dic["imageData"] as? String ?? ""
            let imageData = dic?[(String(show_atValue.prefix(6)) + String(main_modeData))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (dataLeadingName.replacingOccurrences(of: "block", with: "V") + String(app_addValue.prefix(7))) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo)
        let isVoiceStyle = atFinish(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic["match"] as? Int) == 1 || (msgInfo["match"] as? Int) == 1 {
        if (dic?[(show_contentData.replacingOccurrences(of: "layer", with: "ma"))] as? Int) == 1 || (msgInfo?[(show_contentData.replacingOccurrences(of: "layer", with: "ma"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic["msgType"] as? String == "tips" || (dic["messageType"] as? Int) == 17 {
        if dic?[(String(dataTableContent.prefix(7)))] as? String == (showColorName.replacingOccurrences(of: "fatal", with: "i")) || (dic?[(show_createValue.replacingOccurrences(of: "actual", with: "me") + String(data_cornerName.suffix(7)))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic["formatType"] as? String == "mf") && ((dic["messageType"] as? Int) == 15 || (dic["messageType"] as? Int) == 16) {
        if (dic?[(const_assetData.replacingOccurrences(of: "label", with: "o") + "atTy" + String(userMakeValue))] as? String == "mf") && ((dic?[(show_createValue.replacingOccurrences(of: "actual", with: "me") + String(data_cornerName.suffix(7)))] as? Int) == 15 || (dic?[(show_createValue.replacingOccurrences(of: "actual", with: "me") + String(data_cornerName.suffix(7)))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic["msgType"] as? String == "userIntro" {
        if dic?[(String(dataTableContent.prefix(7)))] as? String == (String(kBlockSharedContent.prefix(6)) + show_partyValue.replacingOccurrences(of: "view", with: "o")) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo["msgType"] as? String == "WantGift" || (msgInfo["messageType"] as? Int) == 25 {
        if msgInfo?[(String(dataTableContent.prefix(7)))] as? String == (String(constTapText.suffix(7)) + data_valueTitle.replacingOccurrences(of: "result", with: "t")) || (msgInfo?[(show_createValue.replacingOccurrences(of: "actual", with: "me") + String(data_cornerName.suffix(7)))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func behindMail(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.nameJsonDictionaryWithImage(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(user_buttonLabelTitle.replacingOccurrences(of: "title", with: "xt"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(main_normalTitle) + String(kToText.prefix(6)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(mainTalkOfValueData.replacingOccurrences(of: "color", with: "r"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(const_voiceTitle))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == SceneAppManager.share.loginUserMode.userID {
        if senderId == SceneAppManager.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(const_voiceTitle))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(app_clickLoadContent) + String(kTextData))] = message.userID ?? (user?[(String(app_clickLoadContent) + String(kTextData))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(const_voiceTitle))] = "\(msgInfo?[(String(kCustomData))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(app_clickLoadContent) + String(kTextData))] = "\(msgInfo?[(String(kCustomData))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(notiManagerData))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(notiManagerData))] = "\(msgInfo?[(String(notiManagerData))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.nameJsonDictionaryWithImage(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(notiManagerData))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(notiManagerData))] = "\(customDic[(String(notiManagerData))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(notiManagerData))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((mainGestureText.replacingOccurrences(of: "hidden", with: "a") + String(main_buttonTitleContent))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(mainGestureText.replacingOccurrences(of: "hidden", with: "a") + String(main_buttonTitleContent))] = "\(msgInfo?[(mainGestureText.replacingOccurrences(of: "hidden", with: "a") + String(main_buttonTitleContent))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(notiEnableTitle) + String(data_colorViewName))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(notiEnableTitle) + String(data_colorViewName))] = "\(msgInfo?[(String(notiEnableTitle) + String(data_colorViewName))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(user_countText.suffix(5)) + "MM-dd" + String(data_angleTableValue.prefix(5)) + "m:ss")
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(userListName.replacingOccurrences(of: "value", with: "s") + String(user_emptyName.prefix(7)))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == SceneAppManager.share.loginUserMode.userID ? "1" : "0"
        info?[(String(main_targetTitle))] = senderId == SceneAppManager.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func tag(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = behindMail(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(notiManagerData))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func atomicNumber85(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func errorAllow(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func atFinish(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(dataTableContent.prefix(7)))] as? String, msgType == (String(k_frameName)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(appPlayerData.lowercased() + String(k_oldName.suffix(5)))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(k_userToText.prefix(4)) + "oMsg")
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(user_playerViewGiftTitle))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(kFinishName) + show_linkData.replacingOccurrences(of: "request", with: "i"))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
