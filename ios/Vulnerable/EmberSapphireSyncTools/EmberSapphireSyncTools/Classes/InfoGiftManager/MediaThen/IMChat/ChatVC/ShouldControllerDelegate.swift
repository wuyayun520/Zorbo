
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showEditData:[UInt8] = [0x80,0x85,0x80,0x8b,0x3f,0x7a,0x86,0x7b,0x7c,0x89,0x51,0x40,0x37,0x7f,0x78,0x8a,0x37,0x85,0x86,0x8b,0x37,0x79,0x7c,0x7c,0x85,0x37,0x80,0x84,0x87,0x83,0x7c,0x84,0x7c,0x85,0x8b,0x7c,0x7b]

fileprivate func centerReturnOf(size num: UInt8) -> UInt8 {
    let value = Int(num) + 233
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "roomId" :*/
fileprivate let userManagerTitle:String = "roomIto input"
fileprivate let dataPublishContent:String = "D"

/*: "type" :*/
fileprivate let user_cellLabelName:[UInt8] = [0xad,0xa0,0xa9,0xbc]

/*: "extra" :*/
fileprivate let const_succeedData:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let noti_matchValue:[UInt8] = [0x5f,0x40,0x58,0x45,0x7e,0x50,0x4c]

/*: "Send Gift" :*/
fileprivate let const_attributeData:[Character] = ["S","e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let data_makeModelName:String = "cookie self selfView i"
fileprivate let kFiniteTimeData:[Character] = ["n","f","o"]

/*: "Report" :*/
fileprivate let show_sizeData:String = "Reportmodel color"

/*: "uid" :*/
fileprivate let dataAtIconForName:[UInt8] = [0xa4,0xb8,0xb5]

private func appFor(click num: UInt8) -> UInt8 {
    return num ^ 209
}

/*: "name" :*/
fileprivate let showCapData:[UInt8] = [0xbb,0xae,0xba,0xb2]

fileprivate func socialistLaborParty(video num: UInt8) -> UInt8 {
    let value = Int(num) + 179
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't send blank message" :*/
fileprivate let const_toName:String = "Can\'t "
fileprivate let dataCameraColorContentTitle:String = "ank main mode self in"

/*: "user" :*/
fileprivate let showModelName:String = "lineer"

/*: "content" :*/
fileprivate let appWillName:[UInt8] = [0x15,0x21,0x20,0x26,0x17,0x20,0x26]

fileprivate func fullPhaseOfTheMoon(input num: UInt8) -> UInt8 {
    let value = Int(num) - 178
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "msgType" :*/
fileprivate let mainToName:String = "color text equalmsgType"

/*: "txt" :*/
fileprivate let const_labValue:String = "TXT"

/*: "isGroup" :*/
fileprivate let dataTimePathOtherName:String = "imove"
fileprivate let k_appName:String = "message image return source keyGroup"

/*: "jumps" :*/
fileprivate let app_mainMakeDirectionData:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let kYouTitle:[UInt8] = [0xe9,0xe1,0xf7,0xf7,0xe5,0xe3,0xe1]

private func varSelected(begin num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "TxtMsg" :*/
fileprivate let kTabData:String = "layer viewTxtMs"
fileprivate let constTitleName:String = "view"

/*: "toUid" :*/
fileprivate let appModelDismissData:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let showStartName:String = "mtable"
fileprivate let mainCoverName:String = "if make last type heightgInfo"

/*: "params" :*/
fileprivate let main_groupValue:[UInt8] = [0x29,0x1a,0x2b,0x1a,0x26,0x2c]

fileprivate func appReturn(send num: UInt8) -> UInt8 {
    let value = Int(num) - 185
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "contentType" :*/
fileprivate let const_inputText:[UInt8] = [0xdf,0xd3,0xd2,0xc8,0xd9,0xd2,0xc8,0xe8,0xc5,0xcc,0xd9]

/*: "ImgMsg" :*/
fileprivate let notiProgressData:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let mainDeadlineInfoValue:String = "appmg"

/*: "imageUri" :*/
fileprivate let noti_videoTitle:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "pic" :*/
fileprivate let userSaveerName:String = "padd"

/*: "msgId" :*/
fileprivate let constSharedValue:String = "to collection make model rankmsgId"

/*: "totalMfCoin" :*/
fileprivate let k_actionArrayContent:[Character] = ["t"]
fileprivate let mainContentData:String = "otalMfrecord manager view self"

/*: "giftId" :*/
fileprivate let k_centerName:String = "dot togiftId"

/*: "giftNum" :*/
fileprivate let k_popularInviteData:String = "index heightgiftNum"

/*: "pkgItemsetId" :*/
fileprivate let appSendName:String = "pkgItemerror frame video"
fileprivate let appSizeTitle:String = "setIdimport search back"

/*: "msg" :*/
fileprivate let constCellObjectData:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let userDirectionName:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let k_iconData:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let notiAfterTitle:[Character] = ["n","i","c","k","n","a"]
fileprivate let const_writingUserText:String = "mawake"

/*: "headPic" :*/
fileprivate let dataCommentName:[UInt8] = [0xf7,0xf4,0xf0,0xf3,0xdf,0xf8,0xf2]

fileprivate func titleIn(equal num: UInt8) -> UInt8 {
    let value = Int(num) + 113
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "All Numbers" :*/
fileprivate let data_replyName:String = "All Nstring red self"

/*: "sendNum" :*/
fileprivate let data_insideValue:String = "sendNlabel date clear leading"
fileprivate let noti_reportSaveData:String = "content"

/*: "toUser" :*/
fileprivate let main_ofCoverName:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let k_toIndexData:String = "msgIncolet data model"
fileprivate let userMAttributeData:String = "colore"

/*: "isConnection" :*/
fileprivate let userCameraEqualMomentValue:[Character] = ["i"]
fileprivate let dataToBottomTitle:[Character] = ["s","C","o","n","n","e","c","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ShouldControllerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class ShouldControllerDelegate: EqualViewController {
	var sheetPoneDictionary: [AnyHashable: String]?
	var fileNoticeName: String?
	var clotheArray: [AnyHashable]?
	var dryName: String?
	var wortArray: [AnyHashable]?

    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = FaceModelType() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = MakeConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = FaceModelType.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showEditData.map{centerReturnOf(size: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SceneAppManager.share.appConfigMode.disableShootScreen {
            if SceneAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = LargeShieldView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        endUser()
        //: createUIConstraints()
        tight()
        //: bindInteraction()
        itemIntervaleraction()
        //: addTapGestureRecognizer()
        containerDesign()
        //: func__chatRoom_insertTipMsg()
        home()
        //: chatSettings()
        sessionSendSettings()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            subjectMatter()
        }
    
            var boyScoutPath = Bundle.main.path(forResource: "deck_read", ofType: "txt")
            if var boyScout = boyScoutPath {

                
		if (boyScout.count == 20 && !boyScout[boyScout.startIndex].isASCII) && (boyScout.suffix(from: boyScout.startIndex) == boyScout.uppercased() + "compose") {
		//: SWIFT_CUSTOM_DANGER
            var national = boyScout.startIndex
            boyScout.formIndex(after: &national)
            boyScout.append(boyScout[national])
		}

                boyScoutPath = boyScout
            }
            if let boyScoutPath = boyScoutPath, self.dryName == nil {
                self.dryName = try? String(contentsOfFile: boyScoutPath, encoding: .ascii)
            }
            if let dryName = self.dryName,
               let boyScoutRange = dryName.range(of: "^nationwide_\\d+$", options: .regularExpression) {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var boyScoutTableView = UITableView(frame: someoneView.bounds)

                
		if (boyScoutTableView.delegate != nil) && (boyScoutTableView.frame.origin.y == 60.24) {
		//: SWIFT_CUSTOM_DANGER
            boyScoutTableView.indicatorStyle = .default
		}

                if self.wortArray == nil {
                    self.wortArray = []
                }
                let boyScoutTitle = String(dryName[boyScoutRange])
                for _ in 0 ..< Int(someoneView.bounds.origin.x) {
                    if var wortArray = wortArray,
                       let boyScoutSubRange = dryName.range(of: "^threshold_\\d+$", options: .regularExpression) {
                        let content = String(dryName[boyScoutSubRange])
                        wortArray.append([boyScoutTitle, content])
                    }
                }
                someoneView.addSubview(boyScoutTableView)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    
            var anyonePath = Bundle.main.path(forResource: "dead_residence", ofType: "lottie")
            if var anyone = anyonePath {

                
		if (anyone.count == 17 && anyone[anyone.startIndex].hexDigitValue != nil) && (anyone.count == 14 && anyone[anyone.index(before: anyone.endIndex)].isHexDigit) {
		//: SWIFT_CUSTOM_DANGER
            anyone += anyone
		}

                anyonePath = anyone
            }
            if let anyonePath = anyonePath,
               self.sheetPoneDictionary == nil,
               let anyoneDictionary = NSDictionary(contentsOfFile: anyonePath) as? Dictionary<AnyHashable, String> {
                self.sheetPoneDictionary = anyoneDictionary
            }
            if let anyoneText = self.sheetPoneDictionary?["rainImpress"],
               let anyonePlaceholder = self.sheetPoneDictionary?["intervalDoze"] {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var anyoneTextField = UITextField()
                anyoneTextField.frame = CGRect(x: CGFloat(0), y: 0, width: 0, height: 0)
                anyoneTextField.text = anyoneText
                anyoneTextField.placeholder = anyonePlaceholder

                
		if (anyoneTextField.constraintsAffectingLayout(for: .horizontal).count == 72) && (anyoneTextField.layer.mask != nil) {
		//: SWIFT_CUSTOM_DANGER
            UIView.userInterfaceLayoutDirection(for: .playback, relativeTo: .rightToLeft)
		}

                self.view.addSubview(anyoneTextField)
            }

	}

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.periodicTable(join: false)
        //: self.destroyTipsTimer()
        self.picture()
    		sheetPoneDictionary = nil
		fileNoticeName = nil
		clotheArray = nil
		dryName = nil
		wortArray = nil

	}

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: TitleTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TitleTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(LabelMsgCell.self, forCellReuseIdentifier: LabelMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PressMsgCell.self, forCellReuseIdentifier: PressMsgCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(DisengageThen.self, forCellReuseIdentifier: DisengageThen.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(FrontCustomReactiveCompatible.self, forCellReuseIdentifier: FrontCustomReactiveCompatible.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ImageProductMessageCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(BackViewCell.self, forCellReuseIdentifier: ImageProductMessageCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: StopView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = StopView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: TableAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TableAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: WithThen = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = WithThen()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: IndexEqualTitleView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = IndexEqualTitleView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: UpView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = UpView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension ShouldControllerDelegate {
    /// 群聊设置
    //: private func chatSettings() {
    private func sessionSendSettings() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        AssociablenessChatManager.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        periodicTable(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func periodicTable(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(userManagerTitle.prefix(5)) + dataPublishContent.lowercased()): self.conversationData.groupID, String(bytes: user_cellLabelName.map{$0^217}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: LabelUpReactiveCompatible.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        LabelUpReactiveCompatible.discontinue(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - ToShow

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension ShouldControllerDelegate: ToShow {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> IndexEventCellData! {
    func messageController(_: TitleTableViewController!, onNewMessage data: V2TIMMessage!) -> IndexEventCellData! {
        //: let dic = DataChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = DataChatManager.visibleViewData(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = BackThen.freshMake(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: IndexEventCellData!) -> ImageProductMessageCell! {
    func messageController(_: TitleTableViewController!, onShowMessageData data: IndexEventCellData!) -> ImageProductMessageCell! {
        //: if data.isKind(of: AtCellData.self) {
        if data.isKind(of: AtCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = LabelMsgCell(style: .default, reuseIdentifier: LabelMsgCell.className())
            //: let cellData = data as! AtCellData
            let cellData = data as! AtCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.attributeApp(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.translateMsg(with: cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: DayCellData.self) {
        if data.isKind(of: DayCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PressMsgCell(style: .default, reuseIdentifier: PressMsgCell.className())
            //: let cellData = data as! DayCellData
            let cellData = data as! DayCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: ImageCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = DisengageThen(style: .default, reuseIdentifier: DisengageThen.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! ImageCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: AddCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = FrontCustomReactiveCompatible(style: .default, reuseIdentifier: FrontCustomReactiveCompatible.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! AddCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ImageProductMessageCell!) {
    func messageController(_: TitleTableViewController!, onSelectMessageAvatar cell: ImageProductMessageCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(SceneAppManager.share.loginUid) {
            if userModel.uid == Int(SceneAppManager.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                attributeApp(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                billOfFare(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ImageProductMessageCell!) {
    func messageController(_: TitleTableViewController!, onSelectMessageContent cell: ImageProductMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PressMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! DayCellData
            let imageCellData = cell.messageData as! DayCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(constVoiceMsg)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = TalkingBackStochasticProcessMeasurable()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = ClickMomentModel()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [TalkingBackStochasticProcessMeasurable] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = EparchyViewController(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.occupier()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: LabelMsgCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! LabelMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = DataChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = DataChatManager.visibleViewData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(const_succeedData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: noti_matchValue.map{$0^53}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArrayMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: AccumulationThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    AccumulationThen.share.toppleAction(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: FrontCustomReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! FrontCustomReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = DataChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = DataChatManager.visibleViewData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(const_succeedData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: noti_matchValue.map{$0^53}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArrayMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: AccumulationThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    AccumulationThen.share.toppleAction(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ImageProductMessageCell!) {
    func messageController(_: TitleTableViewController!, onRetryMessageContent cell: ImageProductMessageCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        service(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension ShouldControllerDelegate {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func picture() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func billOfFare(user: MailModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = EventViewDelegate(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.miniBy(cellTitleList: [(String(const_attributeData)).localized, "@", (String(data_makeModelName.suffix(6)) + String(kFiniteTimeData)).localized, (String(show_sizeData.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.prixFixeUser(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.prixFixe(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.attributeApp(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.carteReportAction(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func prixFixeUser(user: MailModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.littleness()
        //: let model = TalkingChatRoomMemberModel()
        let model = PlaceMemberModel()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.should(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func prixFixe(user: MailModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: dataAtIconForName.map{appFor(click: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: showCapData.map{socialistLaborParty(video: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.header(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func attributeApp(uid: Int) {
        //: AccumulationThen.share.func__pushToUserDetailVC(uid: String(uid))
        AccumulationThen.share.character(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func carteReportAction(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = DreckThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.sinceCon(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension ShouldControllerDelegate {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func asset(msgStr: String, quoteModel _: ParcelModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            exceptRakeMsg(showMsg: (const_toName + "send bl" + String(dataCameraColorContentTitle.prefix(4)) + "message").localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: noti_matchValue.map{$0^53}, encoding: .utf8)!: (showModelName.replacingOccurrences(of: "line", with: "us")), String(bytes: dataAtIconForName.map{appFor(click: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: appWillName.map{fullPhaseOfTheMoon(input: $0)}, encoding: .utf8)!: temStr, (String(mainToName.suffix(7))): (const_labValue.lowercased()), (dataTimePathOtherName.replacingOccurrences(of: "move", with: "s") + String(k_appName.suffix(5))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(app_mainMakeDirectionData))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: kYouTitle.map{varSelected(begin: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: user_cellLabelName.map{$0^217}, encoding: .utf8)!: (String(kTabData.suffix(5)) + constTitleName.replacingOccurrences(of: "view", with: "g")),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(userManagerTitle.prefix(5)) + dataPublishContent.lowercased()): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(appModelDismissData))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(showStartName.replacingOccurrences(of: "table", with: "s") + String(mainCoverName.suffix(5))): msgInfo, String(bytes: main_groupValue.map{appReturn(send: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            service(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func equalImg(img: UIImage) {
        //: let imagePath = DayCellData.getChatImagePath(image: img)
        let imagePath = DayCellData.transition(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: const_inputText.map{$0^188}, encoding: .utf8)!: (String(notiProgressData)), (String(mainToName.suffix(7))): (mainDeadlineInfoValue.replacingOccurrences(of: "app", with: "i")), String(bytes: noti_videoTitle.reversed(), encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(userSaveerName.replacingOccurrences(of: "add", with: "ic")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: user_cellLabelName.map{$0^217}, encoding: .utf8)!: (String(notiProgressData)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(userManagerTitle.prefix(5)) + dataPublishContent.lowercased()): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(showStartName.replacingOccurrences(of: "table", with: "s") + String(mainCoverName.suffix(5))): msgInfo, String(bytes: main_groupValue.map{appReturn(send: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            service(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: IndexEventCellData, reSend: Bool) {
    private func service(msgData: IndexEventCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptRakeMsg(showMsg: k_liveFormat)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.sendCustomMessage(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((userSaveerName.replacingOccurrences(of: "add", with: "ic"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(userSaveerName.replacingOccurrences(of: "add", with: "ic"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(userSaveerName.replacingOccurrences(of: "add", with: "ic"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: LabelUpReactiveCompatible.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        LabelUpReactiveCompatible.blockOfVideo(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.featureFrom(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(showStartName.replacingOccurrences(of: "table", with: "s") + String(mainCoverName.suffix(5)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(msgData, msgId: msgInfo[(String(constSharedValue.suffix(5)))] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(k_actionArrayContent) + String(mainContentData.prefix(6)) + "Coin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(k_actionArrayContent) + String(mainContentData.prefix(6)) + "Coin")] as! NSNumber
                //: SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func featureFrom(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard SceneAppManager.share.loginUserMode.status != 1 else {
            guard SceneAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    exceptRakeMsg(showMsg: errorStr)
                }
                //: return
                return
            }

            //: AccumulationThen.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            AccumulationThen.share.requestIn(clickEvent: mainScreenData, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.textPathView()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            mentalFacultyLocal(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = AttentionPopView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage))
            //: view.show()
            view.addOfShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                exceptRakeMsg(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension ShouldControllerDelegate: LowThen {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func askfor(giftModel _: ToModelType, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func camera() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func equalTime(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = ToViewController()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.header(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func ad(actionType: FrontSubscriptType) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            brake()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func itemTable(giftModel _: ToModelType, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func rangeModel(giftModel: ToModelType, giftNum: String, model: PlaceMemberModel) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptRakeMsg(showMsg: k_liveFormat)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(appModelDismissData))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(k_centerName.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(k_popularInviteData.suffix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(userManagerTitle.prefix(5)) + dataPublishContent.lowercased())] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(appSendName.prefix(7)) + String(appSizeTitle.prefix(5)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: LabelUpReactiveCompatible.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        LabelUpReactiveCompatible.viewDay(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.featureFrom(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.belowConstraint(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.atSub(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func tipBottom(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func listShowHeight(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func roundColor(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.roundColor(tap)
    }

    //: func func__starRecordBtnDown() {}
    func addPage() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func toTime(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ShouldControllerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func brake() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        GenerateReactiveCompatible.withBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = CompartmentPickTool.fictitiousText(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: AccumulationThen.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                AccumulationThen.share.saveer()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.equalImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func componentCamera() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        GenerateReactiveCompatible.springtime(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.equalImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension ShouldControllerDelegate {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func byInfo(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(userManagerTitle.prefix(5)) + dataPublishContent.lowercased())] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension ShouldControllerDelegate {
    //: func func__TipsNotification() {
    func isNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(delineation(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: main_senseFormat,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func delineation(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: AddCellData = userinfo[(String(constCellObjectData))] as! AddCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func mentalFacultyLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: appWillName.map{fullPhaseOfTheMoon(input: $0)}, encoding: .utf8)!: tipStr, (String(mainToName.suffix(7))): (String(userDirectionName))]
        //: var message: IndexEventCellData?
        var message: IndexEventCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = BackThen.containerPath(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func home() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard SceneAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            guard SceneAppManager.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: SceneAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.mentalFacultyLocal(tipStr: SceneAppManager.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension ShouldControllerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func atSub(extral: [String: Any?], model: PlaceMemberModel, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(showStartName.replacingOccurrences(of: "table", with: "s") + String(mainCoverName.suffix(5)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(dataTimePathOtherName.replacingOccurrences(of: "move", with: "s") + String(k_appName.suffix(5)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(mainToName.suffix(7)))] = (String(k_iconData))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(showStartName.replacingOccurrences(of: "table", with: "s") + String(mainCoverName.suffix(5)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: dataAtIconForName.map{appFor(click: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(notiAfterTitle) + const_writingUserText.replacingOccurrences(of: "awake", with: "e")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: dataCommentName.map{titleIn(equal: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(data_replyName.prefix(5)) + "umbers").localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(k_iconData))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(k_iconData))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(notiAfterTitle) + const_writingUserText.replacingOccurrences(of: "awake", with: "e"))] = "\(giftdic[(String(data_insideValue.prefix(5)) + noti_reportSaveData.replacingOccurrences(of: "content", with: "um"))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(main_ofCoverName))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = BackThen.containerPath(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(k_toIndexData.prefix(7)) + userMAttributeData.replacingOccurrences(of: "color", with: "m"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(k_toIndexData.prefix(7)) + userMAttributeData.replacingOccurrences(of: "color", with: "m"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(constSharedValue.suffix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        divert(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(k_actionArrayContent) + String(mainContentData.prefix(6)) + "Coin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(k_actionArrayContent) + String(mainContentData.prefix(6)) + "Coin")] as! NSNumber
            //: SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: SceneAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.queryMagnitudeeraction(needReload: false, mf_coin: SceneAppManager.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.low()
    }

    //: func initCachEffectData() {
    func subjectMatter() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.labelDetail(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func divert(giftDic: [String: Any], model: PlaceMemberModel) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.phone(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in InfoGiftManager.share.getStop() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [PromotionArrModel]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.progress(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.accord(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func fromLog(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in InfoGiftManager.share.getStop() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [PromotionArrModel]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.progress(model: giftModel as! ToTransformable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.dower(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension ShouldControllerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func endUser() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func tight() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(constCellUrl)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(constCellUrl)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - constStatusContent)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func itemIntervaleraction() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = ToViewController()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! IndexEventCellData).isKind(of: AtCellData.self) {
                    if (cellData as! IndexEventCellData).isKind(of: AtCellData.self) {
                        //: let groupCellData = cellData as! AtCellData
                        let groupCellData = cellData as! AtCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(constSharedValue.suffix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: BackConversationListener.shared.rx
        BackConversationListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(userCameraEqualMomentValue) + String(dataToBottomTitle)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
