
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let mainPageValue:String = "view gift minimizeIM退出"
fileprivate let showObjectTitle:String = "to let toode:"

/*: , desc: :*/
fileprivate let dataFrameName:String = ", desc:time center extra"

/*: ." :*/
fileprivate let k_transactionContent:String = "size"

/*: "IM登录失败，code: :*/
fileprivate let appManagerName:String = "IM\u{767b}录\u{5931}败\u{ff0c}c"
fileprivate let k_numberData:String = "ode:background message make make"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let show_countData:String = "IM获取\u{672a}读消"
fileprivate let user_contentValue:String = "息\u{603b}数失败，"
fileprivate let kStyleValue:[Character] = ["c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let appNameDayIfText:[Character] = ["c","r","u","s","h","B","a","d","g","e"]

/*: "badNumber" :*/
fileprivate let kNowData:[UInt8] = [0xc9,0xc8,0xcb,0xb5,0xdc,0xd4,0xc9,0xcc,0xd9]

fileprivate func textGift(hide num: UInt8) -> UInt8 {
    let value = Int(num) + 153
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "plaza" :*/
fileprivate let show_shareJumpValue:String = "plsearcha"

/*: "roomNum" :*/
fileprivate let appManagerToData:[Character] = ["r","o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let user_overMakeTitle:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let kTouchData:String = "oviewly"
fileprivate let appToValidValue:String = "text iconoomId"

/*: "LIVE-CHAT" :*/
fileprivate let dataPlayTitle:[Character] = ["L"]
fileprivate let constTheText:[Character] = ["I","V","E","-","C","H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let constWrapValue:String = "PAkeyT"

/*: "extra" :*/
fileprivate let app_leadingData:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let main_headTitle:String = "size"
fileprivate let k_giftData:String = "sgTypevideo user mutual to"

/*: "newPkgGift" :*/
fileprivate let app_statusName:String = "model content tonewP"

/*: "MF:ChatMentionMsg" :*/
fileprivate let mainChangeName:String = "in viewMF:Cha"
fileprivate let kTopInputTitle:[Character] = ["t","M","e","n","t","i","o","n","M","s","g"]

/*: "uid" :*/
fileprivate let userListenViewName:[UInt8] = [0xd3,0xcf,0xc2]

private func centerPath(data num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "onlineStatus" :*/
fileprivate let data_requestValue:[UInt8] = [0x32,0x33,0x31,0x34,0x33,0x38,0xe,0x29,0x3c,0x29,0x28,0x2e]

private func resultDocument(deed num: UInt8) -> UInt8 {
    return num ^ 93
}

/*: "isNewUser" :*/
fileprivate let kMediaData:[UInt8] = [0x72,0x65,0x73,0x55,0x77,0x65,0x4e,0x73,0x69]

/*: "c2c_ :*/
fileprivate let dataSkipName:String = "c2c_true style"

/*: "IM获取会话失败，convId: :*/
fileprivate let kCellText:String = "IM\u{83b7}\u{53d6}"
fileprivate let const_addTitle:String = "，convIsend title gesture title post"
fileprivate let show_backgroundValue:[Character] = ["d",":"]

/*: , code: :*/
fileprivate let user_ofName:String = ", codreturn hidden view"
fileprivate let appMainViewName:String = "e:player top cell"

/*: "IM插入消息失败，targetId: :*/
fileprivate let notiChangeData:String = "IM插入消息make price"
fileprivate let show_limitContent:String = "rgetId:model current"

/*: "changeDirection" :*/
fileprivate let noti_cellData:String = "CHA"
fileprivate let appScreenValue:[Character] = ["n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let mainTopText:String = "or tool talk view blockrYM"
fileprivate let k_regularTitle:String = "sgTypetool text view image frame"

/*: "GJ:CallCustom" :*/
fileprivate let data_mainContent:String = "GJ:Callevel cover case button"

/*: "IM获取会话列表失败，code: :*/
fileprivate let dataTransitionValue:String = "IM\u{83b7}取会"
fileprivate let noti_fillToValue:[Character] = ["，","c","o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let showHiddenName:[Character] = ["m","s","g","I","n","f","o"]

/*: "needFold" :*/
fileprivate let showFailPushFrameData:String = "needFoldscreen origin path pair"

/*: "group_ :*/
fileprivate let kAppName:String = "group_of count let"

/*: "IM删除会话失败，convID: :*/
fileprivate let constShowTitle:String = "to bottom local else viewIM删除"
fileprivate let user_resultTitle:String = "selection user kit map，con"

/*: "IM置顶会话失败，convId: :*/
fileprivate let kMatchValue:[Character] = ["I","M","置","顶","会","话","失","败","，","c","o","n","v","I"]
fileprivate let const_colorOfData:String = "color make else content eyed:"

/*: "SDK 正在连接到服务器" :*/
fileprivate let userViewTitle:String = "drop"
fileprivate let appShareClearData:[Character] = ["D","K"," ","正","在","连","接","到","服","务","\u{5668}"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let main_contentName:String = "image gift tool messageSDK "
fileprivate let user_viewName:String = "接到\u{670d}\u{52a1}器"

/*: "IM连接服务器失败，code: :*/
fileprivate let constSizeTopData:[Character] = ["I","M","连","接","服","务","器","失","\u{8d25}","，"]
fileprivate let const_labelContent:String = "code:sound line dismiss frame"

/*: , error: :*/
fileprivate let dataImageMediumName:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let user_toName:[Character] = ["I"]
fileprivate let app_modeValue:String = "M账号被up equal mini gift m"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let k_currentName:[UInt8] = []

fileprivate func recordType(send num: UInt8) -> UInt8 {
    let value = Int(num) - 94
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Offline Notification" :*/
fileprivate let main_galleryText:String = "bottom falseOffl"
fileprivate let show_aspectContent:String = "fime"

/*: "Cancel" :*/
fileprivate let notiManagerText:String = "title"
fileprivate let dataViewName:String = "auserel"

/*: "Re Login" :*/
fileprivate let user_touchName:String = "height as extension background printRe Logi"
fileprivate let user_shareData:[Character] = ["n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BackConversationListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol FaceObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func titleData(data: [MakeConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func counteractionModel(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func fromMsg(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum EventDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class BackConversationListener: NSObject {
@objcMembers public class BackConversationListener: NSObject {
    //: public static let shared = BackConversationListener()
    public static let shared = BackConversationListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [MakeConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [MakeConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [MakeConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [MakeConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [MakeConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: MakeConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.enableBar()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func topple() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(data_barStr, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func enableBar() {
        //: func__removeAllDelegate()
        funcDataMaleDelegate()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.south()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func south() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = MakeConversationModel.femaleCurrent(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = MakeConversationModel.femaleCurrent(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func startResume() -> [MakeConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if SceneAppManager.share.appStatus == AppSkinStatus.special.rawValue {
        if SceneAppManager.share.appStatus == HighStatusValueConvertible.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == TopReactiveCompatible.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == TopReactiveCompatible.tied() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension BackConversationListener {
extension BackConversationListener {
    //: func func__removeAllDelegate() {
    func funcDataMaleDelegate() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func theTap(_ delegate: FaceObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func indexDown(_ delegate: FaceObjectProtocol) {
        //: self.func__invokeDelegate {
        self.liveDelegate {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func liveDelegate(_ invocation: (FaceObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! FaceObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension BackConversationListener {
extension BackConversationListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func andThen() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            NurseLogReactiveCompatible.citeOrLineup(msg: (String(mainPageValue.suffix(4)) + "登录失败，c" + String(showObjectTitle.suffix(4))) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.enableBar()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func notBegin(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = SceneAppManager.share.appUserConfigMode.uSig
        let uSigStr = SceneAppManager.share.appUserConfigMode.uSig
        //: let uidStr = SceneAppManager.share.loginUserMode.userID
        let uidStr = SceneAppManager.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.rent()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            NurseLogReactiveCompatible.citeOrLineup(msg: (appManagerName + String(k_numberData.prefix(4))) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func modelList() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || SceneAppManager.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || SceneAppManager.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.exceptRakeMsg(showMsg: k_liveFormat)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension BackConversationListener {
extension BackConversationListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func rent() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            NurseLogReactiveCompatible.citeOrLineup(msg: (show_countData + user_contentValue + String(kStyleValue)) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func curCommit() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: LabelUpReactiveCompatible.req_sysMessageList { _, result in
        LabelUpReactiveCompatible.bottomPacket { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(String(appNameDayIfText))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: userBottomViewStr, object: self, userInfo: [String(bytes: kNowData.map{textGift(hide: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func toComment(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: LabelUpReactiveCompatible.req_getChatRoomInfo { succeed, result, _ in
        LabelUpReactiveCompatible.bottomMake { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(show_shareJumpValue.replacingOccurrences(of: "search", with: "az"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(appManagerToData))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = MakeConversationModel.femaleCurrent(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(user_overMakeTitle))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(kTouchData.replacingOccurrences(of: "view", with: "n") + "OneR" + String(appToValidValue.suffix(5)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension BackConversationListener: V2TIMAdvancedMsgListener {
extension BackConversationListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(dataPlayTitle) + String(constTheText))) || msg.groupID.contains((constWrapValue.replacingOccurrences(of: "key", with: "R") + "Y-CHAT"))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            TotalDeviceDanmuManager.behindFrom().dismissSize(msg: msg)
            //: return
            return
        }
        //: if msg.sender == SceneAppManager.share.loginUserMode.userID { return }
        if msg.sender == SceneAppManager.share.loginUserMode.userID { return }

        //: let dic = DataChatManager.parseTXMessageData(data: msg.customElem.data)
        let dic = DataChatManager.visibleViewData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(app_leadingData))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? QuantityeractionModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: show_nameTitle, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(main_headTitle.replacingOccurrences(of: "size", with: "m") + String(k_giftData.prefix(6)))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(app_statusName.suffix(4)) + "kgGift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: showEnterData, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if DataChatManager.checkVoiceMessage(message: msg) {
            if DataChatManager.strength(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = BackThen.behindMail(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                SumryMsgTable.bring(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.attributeFun(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            CaptureThen.shared.fillUpMessage(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            CaptureThen.shared.enrichmentIn(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(mainChangeName.suffix(6)) + String(kTopInputTitle)) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            AssociablenessChatManager.share.objectRemember(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: WithChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: WithChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != TopReactiveCompatible.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != TopReactiveCompatible.withOf() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.liveDelegate { $0.fromMsg?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            UpNotificationManager.shared.alongEqual(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: userListenViewName.map{centerPath(data: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: data_requestValue.map{resultDocument(deed: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: kMediaData.reversed(), encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: showObjectFormat,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            PlayerClickThen.examplePathTime(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            WithChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: notiNoValue, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: IndexEventCellData, isPrivete: Bool) {
    @objc public func funcRequest(targetId: String, msgCellData: IndexEventCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.retention(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(dataSkipName.prefix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.requestObject(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.equalGroup(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
                self.writeLog(convId: convId, code: code, desc: desc)
            }
        }
    }
    
    func writeLog(convId: String, code :Int32, desc: String?) {
        NurseLogReactiveCompatible.citeOrLineup(msg: (kCellText + "会话失败" + String(const_addTitle.prefix(6)) + String(show_backgroundValue)) + "\(convId)" + (String(user_ofName.prefix(5)) + String(appMainViewName.prefix(2))) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func retention(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = SceneAppManager.share.loginUserMode.userID
        let senderStr = SceneAppManager.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                self.writeLogData(targetId: targetId, code: code, desc: desc)
            }
        }
    }
    
    func writeLogData(targetId: String, code :Int32, desc: String?) {
        NurseLogReactiveCompatible.citeOrLineup(msg: (String(notiChangeData.prefix(6)) + "失败，ta" + String(show_limitContent.prefix(7))) + "\(targetId)" + (String(user_ofName.prefix(5)) + String(appMainViewName.prefix(2))) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func attributeFun(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(noti_cellData.lowercased() + String(appScreenValue))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && AccumulationThen.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(mainTopText.suffix(3)) + String(k_regularTitle.prefix(6)))].stringValue == (String(data_mainContent.prefix(6)) + "lCustom") && isSelf && AccumulationThen.share.saveer()!.isKind(of: BackTipsProtocol.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: showNearbyText) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: showNearbyText)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension BackConversationListener: V2TIMConversationListener {
extension BackConversationListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.requestObject(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.equalGroup(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.requestObject(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.equalGroup(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: SceneAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        SceneAppManager.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.liveDelegate {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.counteractionModel?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func equalGroup(conVersationList: [MakeConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if PlayerClickThen.needTalk().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                PlayerClickThen.needTalk().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.liveDelegate {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.titleData?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func via(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [MakeConversationModel] = []

        // V2TIMConversation->MakeConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = MakeConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.receiveList(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [MakeConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = SizeInfoTable.last(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.duringUp(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension BackConversationListener {
extension BackConversationListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func bottom(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.via(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.equalGroup(conVersationList: arr as? [MakeConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            NurseLogReactiveCompatible.citeOrLineup(msg: (dataTransitionValue + "话列表失败" + String(noti_fillToValue)) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func chatId(targetId: String) -> MakeConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func requestObject(convList: [V2TIMConversation]) -> [MakeConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [MakeConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = MakeConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.receiveList(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.duringUp(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func duringUp(convModel: MakeConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.south()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: EventDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - SceneAppManager.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - SceneAppManager.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = PlayerClickThen.constraintStatusId(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = DataChatManager.parseTXMessageData(data: elemData)
            let dict = DataChatManager.visibleViewData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(app_leadingData))][(String(showHiddenName))][(String(showFailPushFrameData.prefix(8)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.captureType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.captureType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func captureType(convList: [MakeConversationModel], dataType: EventDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func input(targetId: String, dataType: EventDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(dataSkipName.prefix(4))) + "\(targetId)" : (String(kAppName.prefix(6))) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            PlayerClickThen.sumeraction(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(SceneAppManager.share.loginUserMode.userID, toUid: targetId)
            SumryMsgTable.enableSub(SceneAppManager.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.rent()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            self.writeLogData(convID: convID, code: code, desc: desc)
        }
    }
    
    func writeLogData(convID: String, code :Int32, desc: String?) {
        NurseLogReactiveCompatible.citeOrLineup(msg: (String(constShowTitle.suffix(4)) + "会话失败" + String(user_resultTitle.suffix(4)) + "vID:") + "\(convID)" + (String(user_ofName.prefix(5)) + String(appMainViewName.prefix(2))) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
    }
    
    func writeModel(convID: String, code :Int32, desc: String?) {
        NurseLogReactiveCompatible.citeOrLineup(msg: (String(kMatchValue) + String(const_colorOfData.suffix(2))) + "\(convID)" + (String(user_ofName.prefix(5)) + String(appMainViewName.prefix(2))) + "\(code)" + (String(dataFrameName.prefix(7))) + "\(String(describing: desc)).")
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func trademarkEqual(convModel: MakeConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
            self.writeModel(convID: convModel.conversationID, code: code, desc: desc)
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.captureType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.captureType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension BackConversationListener: V2TIMSDKListener {
extension BackConversationListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (userViewTitle.replacingOccurrences(of: "drop", with: "S") + String(appShareClearData)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(main_contentName.suffix(4)) + "\u{5df2}经成\u{529f}\u{8fde}" + user_viewName))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        NurseLogReactiveCompatible.citeOrLineup(msg: (String(constSizeTopData) + String(const_labelContent.prefix(5))) + "\(code)" + (String(dataImageMediumName)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        NurseLogReactiveCompatible.citeOrLineup(msg: (String(user_toName) + String(app_modeValue.prefix(4)) + "挤下线。"))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        StopThen.shared.appearOut(userLoginOut: true)
        //: BackConversationListener.shared.isConnection = false
        BackConversationListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (DeviseFindApplication.shared as! DeviseFindApplication).cohabit()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: k_currentName.map{recordType(send: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = NumerateAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.ar.rawValue || CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.fontJump(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        ShowThen.atomicTotal85Awake()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        ShowThen.atomicTotal85Awake(title: (String(main_galleryText.suffix(4)) + "ine Noti" + show_aspectContent.replacingOccurrences(of: "me", with: "ca") + "tion").localized, message: msg, leftBtnTitle: (notiManagerText.replacingOccurrences(of: "title", with: "C") + dataViewName.replacingOccurrences(of: "user", with: "nc")).localized, rightBtnTitle: (String(user_touchName.suffix(7)) + String(user_shareData)).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            DataPushListener.handleCount().dataUserResource()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            MediaThen.exhibitShared().someoneResource()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            BeanThen.add { t in
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: k_topUrl, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            BeanThen.nameCompletion { t in
                //: if t {
                if t {
                    //: BackConversationListener.shared.func__LogingIn { _ in
                    BackConversationListener.shared.notBegin { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if DataPushListener.handleCount().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(DataPushListener.handleCount().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    StopThen.shared.explain()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
