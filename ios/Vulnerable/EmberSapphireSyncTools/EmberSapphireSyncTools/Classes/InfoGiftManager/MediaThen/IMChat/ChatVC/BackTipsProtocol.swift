
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainWithCommentName:[UInt8] = [0xcc,0xd1,0xcc,0xd7,0x8b,0xc6,0xd2,0xc7,0xc8,0xd5,0x9d,0x8c,0x83,0xcb,0xc4,0xd6,0x83,0xd1,0xd2,0xd7,0x83,0xc5,0xc8,0xc8,0xd1,0x83,0xcc,0xd0,0xd3,0xcf,0xc8,0xd0,0xc8,0xd1,0xd7,0xc8,0xc7]

fileprivate func labelView(input num: UInt8) -> UInt8 {
    let value = Int(num) + 157
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "F5F5F5" :*/
fileprivate let data_jumpValue:[Character] = ["F","5","F","5","F","5"]

/*: "#128CFF" :*/
fileprivate let k_taskData:String = "#player"
fileprivate let kLastName:[Character] = ["2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let notiWithData:String = "view layer dataFeedback"

/*: "#8370FF" :*/
fileprivate let appToValue:String = "make sex table model#8370F"
fileprivate let constThoughtData:String = "label"

/*: "Quick Greeting" :*/
fileprivate let k_afterData:String = "Quick self let regular pair time"
fileprivate let app_skipData:String = "Greetingtop model model"

/*: "Beauty Photos" :*/
fileprivate let notiLeadingContent:String = "Beautyself ret tap equal"

/*: "url" :*/
fileprivate let k_frameValue:String = "pointrl"

/*: "unlockGift" :*/
fileprivate let show_resourceSizeTitle:[Character] = ["u"]
fileprivate let main_whenValue:[Character] = ["n","l","o","c","k","G","i","f","t"]

/*: "%d" :*/
fileprivate let constNameData:[Character] = ["%","d"]

/*: "isBurn" :*/
fileprivate let dataAtName:[UInt8] = [0x97,0x8d,0xbc,0x8b,0x8c,0x90]

private func selectTarget(cell num: UInt8) -> UInt8 {
    return num ^ 254
}

/*: "giftId" :*/
fileprivate let constScaleData:String = "giftIdsource m"

/*: "rYMsgType" :*/
fileprivate let dataLeadingName:[Character] = ["r","Y","M","s","g","T","y"]
fileprivate let constOfTitle:String = "ppath"

/*: "GJ:CallCustom" :*/
fileprivate let constUserValue:String = "flat"
fileprivate let kHandleValue:[Character] = ["J",":","C","a","l","l","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let noti_resultData:String = "chalast"
fileprivate let userRangeName:String = "label me main leteDire"
fileprivate let constNextTitle:String = "CTION"

/*: "extra" :*/
fileprivate let noti_bagModelThenTitle:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let user_cellDateData:[Character] = ["m","s","g","I","n"]
fileprivate let noti_moveData:String = "environmento"

/*: "toUid" :*/
fileprivate let app_frameText:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let mainMicAddCheckedTitle:[Character] = ["w"]
fileprivate let user_listenMomentText:String = "let viewantGift"

/*: "jumpKey" :*/
fileprivate let const_viewData:String = "interval gesture page equal typejumpKey"

/*: "truePersonAuth" :*/
fileprivate let mainEventName:[Character] = ["t","r","u","e","P","e","r"]
fileprivate let noti_sourceText:String = "sonAuthmodel format type top register"

/*: "logType" :*/
fileprivate let show_managerItemName:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let show_startTitle:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "txt" :*/
fileprivate let app_targetName:String = "curxcur"

/*: "type" :*/
fileprivate let constEqualData:String = "ttope"

/*: "QuickMsg" :*/
fileprivate let noti_topName:[Character] = ["Q","u","i","c","k","M","s"]
fileprivate let data_infoName:[Character] = ["g"]

/*: "params" :*/
fileprivate let notiSizeValue:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "content" :*/
fileprivate let app_dataContent:[UInt8] = [0x12,0x1e,0x1f,0x5,0x14,0x1f,0x5]

private func collectionResult(photo num: UInt8) -> UInt8 {
    return num ^ 113
}

/*: "id" :*/
fileprivate let noti_labelContent:String = "ibar"

/*: "txtId" :*/
fileprivate let dataBorderTitle:String = "txtIdreturn less card frame"

/*: "message" :*/
fileprivate let k_challengeValue:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let mainColorValue:[Character] = ["T","x","t","M","s","g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let mainTimeData:[UInt8] = [0x42,0x5b,0x55,0x4f,0x51,0xc,0x54,0x51,0x58,0x58,0x5b,0xc,0x50,0x5b,0x63,0x5a,0x58,0x5b,0x4d,0x50,0xc,0x52,0x4d,0x55,0x58,0x51,0x50,0x1a,0xc,0x3c,0x5e,0x5b,0x4f,0x51,0x50,0x61,0x5e,0x51]

fileprivate func giftEnd(tab num: UInt8) -> UInt8 {
    let value = Int(num) + 20
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let app_modelCenterValue:String = "lsystemgth"

/*: "audio" :*/
fileprivate let noti_managerViewName:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let noti_translateAddData:String = "anumberio"
fileprivate let show_shareTitle:String = "path imageId"

/*: "audioLength" :*/
fileprivate let userModeName:[UInt8] = [0x38,0x2c,0x3d,0x30,0x36,0x15,0x3c,0x37,0x3e,0x2d,0x31]

private func statusReport(layer num: UInt8) -> UInt8 {
    return num ^ 89
}

/*: "audioSandbox" :*/
fileprivate let k_valueName:[Character] = ["a","u","d","i","o","S"]
fileprivate let showPhotoData:String = "A"
fileprivate let dataItemToolValue:String = "filterdbox"

/*: "senderId" :*/
fileprivate let mainRawValue:[UInt8] = [0xf6,0xe8,0xf1,0xe7,0xe8,0xf5,0xcc,0xe7]

fileprivate func dataText(count num: UInt8) -> UInt8 {
    let value = Int(num) - 131
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let k_backgroundData:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let k_equalModelName:String = "Can\'t"
fileprivate let notiFirstData:[Character] = ["a","n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let constArcFrameTitle:[Character] = ["q","u","o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let dataModeContent:String = "image make resultquoteM"
fileprivate let showObjectLogName:String = "fmake"

/*: "contentType" :*/
fileprivate let const_countValue:[UInt8] = [0xe8,0xe4,0xe5,0xff,0xee,0xe5,0xff,0xdf,0xf2,0xfb,0xee]

private func dataInfo(exhibit num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "ImgMsg" :*/
fileprivate let k_pathName:String = "ImgMsgcase else feature else rake"

/*: "img" :*/
fileprivate let dataDirectionName:String = "imthe"

/*: "imageUri" :*/
fileprivate let const_segmentValue:[UInt8] = [0x78,0x7c,0x70,0x76,0x74,0x44,0x63,0x78]

/*: "customSend" :*/
fileprivate let k_indexValue:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let const_infoFlushTitle:String = "mainic"

/*: "VideoMsg" :*/
fileprivate let k_actualName:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let app_titleText:String = "vivalueeo"

/*: "coverImg" :*/
fileprivate let dataModelName:[UInt8] = [0x0,0xc,0x15,0x6,0x11,0x2a,0xe,0x4]

/*: "videoUrl" :*/
fileprivate let app_enterName:[UInt8] = [0x84,0x9b,0x96,0x97,0x9d,0xa7,0x80,0x9e]

private func loadS(line num: UInt8) -> UInt8 {
    return num ^ 242
}

/*: "duration" :*/
fileprivate let k_rawQuoteName:[UInt8] = [0x6e,0x6f,0x69,0x74,0x61,0x72,0x75,0x64]

/*: "len" :*/
fileprivate let user_upValue:[UInt8] = [0xc1,0xba,0xc3]

fileprivate func userPlayer(raw num: UInt8) -> UInt8 {
    let value = Int(num) - 85
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Documents/User/voice/" :*/
fileprivate let const_sourceImageValue:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s"]
fileprivate let user_showText:String = "action"
fileprivate let mainPointTitle:String = "size view type make elser/vo"

/*: "Documents/User/record/" :*/
fileprivate let showSizeTitle:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e"]
fileprivate let constContainerTitle:String = "r/recself head handle request self"

/*: "msgIncome" :*/
fileprivate let main_activeValue:String = "model selectedmsgIncome"

/*: "mfBean" :*/
fileprivate let main_equalData:[UInt8] = [0x17,0x10,0xec,0xf,0xb,0x18]

fileprivate func sumTable(month num: UInt8) -> UInt8 {
    let value = Int(num) - 170
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "messageType" :*/
fileprivate let showLiveValue:String = "MESS"
fileprivate let app_firstValue:[Character] = ["y","p","e"]

/*: "WantGift" :*/
fileprivate let noti_containerData:String = "WantGiftmodel bar guard main suite"

/*: _PrivateChatTipsTitle_ :*/
fileprivate let user_messageName:[Character] = ["_","P","r","i","v","a","t","e","C","h","a"]
fileprivate let notiGoSizeData:String = "intimate vartTip"

/*: "msgId" :*/
fileprivate let data_sharedName:String = "msgIdmake type"

/*: "totalMfCoin" :*/
fileprivate let user_equalValue:[Character] = ["t","o","t","a","l","M","f","C","o","i"]
fileprivate let userRemoveTitle:String = "table"

/*: "attachSysMsg" :*/
fileprivate let constItemText:String = "request push self balanceattachS"
fileprivate let notiInfoName:String = "path modelysMsg"

/*: "intimatePhoto" :*/
fileprivate let user_indexText:[Character] = ["i","n","t","i","m","a","t","e","P","h","o","t","o"]

/*: _ :*/
fileprivate let appPathData:[Character] = ["_"]

/*: "totalIntimate" :*/
fileprivate let k_videoValue:String = "TOTA"
fileprivate let k_transformValue:String = "imaindex"

/*: "uid" :*/
fileprivate let user_makeData:[UInt8] = [0xaf,0xb3,0xbe]

/*: "intimate" :*/
fileprivate let main_backgroundTitle:[UInt8] = [0xbd,0xc2,0xc8,0xbd,0xc1,0xb5,0xc8,0xb9]

fileprivate func weakCaseVideo(result num: UInt8) -> UInt8 {
    let value = Int(num) - 84
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let userPointData:[UInt8] = [0x56,0x74,0x81,0x3a,0x87,0x33,0x88,0x86,0x78,0x33,0x87,0x7b,0x78,0x33,0x79,0x88,0x81,0x76,0x87,0x7c,0x82,0x81,0x33,0x77,0x88,0x78,0x33,0x87,0x82,0x33,0x8c,0x82,0x88,0x85,0x33,0x7f,0x74,0x76,0x7e,0x33,0x82,0x79,0x33,0x7c,0x81,0x87,0x7c,0x80,0x74,0x76,0x8c,0x33,0x7f,0x78,0x89,0x78,0x7f,0x41]

fileprivate func indexSum(size num: UInt8) -> UInt8 {
    let value = Int(num) - 19
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let constRawName:[Character] = ["g"]
fileprivate let showModelValue:String = "iftNumflash rank handle content"

/*: "pkgItemsetId" :*/
fileprivate let k_borderVideoData:String = "type sound uppkgIt"

/*: "from" :*/
fileprivate let appMagnitudeName:String = "frovalue"

/*: "wantGiftId" :*/
fileprivate let mainNameData:[Character] = ["w","a","n","t","G","i"]
fileprivate let k_withData:[Character] = ["f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let dataMakeTitle:String = "WantGiextension color extension guard view"
fileprivate let app_keyTitle:String = "target string frame tip formatftMsg"

/*: "gift" :*/
fileprivate let kFoundationValue:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let kEqualText:String = "var string view system detailView "
fileprivate let main_infoData:[Character] = ["i","n","f","o"]

/*: "Notes" :*/
fileprivate let noti_voiceData:String = "Notesview view size"

/*: "Report" :*/
fileprivate let mainEqualText:String = "Reportof bar"

/*: "View read receipts?" :*/
fileprivate let show_backgroundData:[Character] = ["V","i","e","w"," ","r","e","a","d"," ","r","e","c","e","i","p","t","s","?"]

/*: "He" :*/
fileprivate let mainAddToData:String = "top false withHe"

/*: "She" :*/
fileprivate let mainGalleryTitle:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let user_playerContent:String = "%@ is true launch make error always"
fileprivate let k_topValue:String = "g tooresponse case the app"
fileprivate let user_indexData:[Character] = ["v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let main_shareTitle:String = "meeting data error in dataShoot "
fileprivate let k_titleName:String = " leasgift player storage"
fileprivate let user_addData:String = "secoinfos"

/*: "Upload failed" :*/
fileprivate let kHiddenName:[Character] = ["U"]
fileprivate let userSizeSignTitle:String = "pviewad"

/*: "播放状态: :*/
fileprivate let kTableData:[Character] = ["播","\u{653e}","状","态",":"]

/*: "c2c_" :*/
fileprivate let notiIndexValue:String = "shadow effect titlec2c_"

/*: "msg" :*/
fileprivate let showEqualData:String = "mscollect"

/*: "tips" :*/
fileprivate let const_recordMaxStartData:String = "tipscreen"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kReportData:[UInt8] = [0xc2,0xf4,0xee,0xbb,0xf8,0xfa,0xf5,0xbb,0xee,0xe8,0xfe,0xbb,0xef,0xf3,0xfe,0xbb,0xed,0xf2,0xff,0xfe,0xf4,0xbb,0xf8,0xf3,0xfa,0xef,0xbb,0xfd,0xfe,0xfa,0xef,0xee,0xe9,0xfe,0xbb,0xef,0xf4,0xbb,0xfc,0xf2,0xed,0xfe,0xbb,0xbe,0xdb,0xbb,0xfa,0xbb,0xf8,0xfa,0xf7,0xf7]

private func detectClear(data num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "cellData" :*/
fileprivate let show_equalAreaViewValue:[Character] = ["c","e","l","l","D","a","t","a"]

/*: "System notification" :*/
fileprivate let showRowName:[Character] = ["S","y","s","t","e","m"," ","n"]
fileprivate let notiFaceData:[Character] = ["o","t"]
fileprivate let constUserText:String = "ificatexton"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BackTipsProtocol.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum RecordAlbumFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class BackTipsProtocol: EqualViewController {
	var offensiveText: String?
	var aggregationTitle: String?
	var foundationDictionary: [AnyHashable: String]?

    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = ProductTransformable()
    private var userWrap = ProductTransformable() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = RecordAlbumFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ProductTransformable = ProductTransformable()) {
    init(chatID: String, isFrom: RecordAlbumFromEnum = .Normal, userWrap: ProductTransformable = ProductTransformable()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        BackTipsProtocol.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        WithChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == TopReactiveCompatible.getXiaoMiID() {
        if targetId == TopReactiveCompatible.withOf() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == TopReactiveCompatible.getCustomerServiceID() {
        } else if targetId == TopReactiveCompatible.tied() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainWithCommentName.map{labelView(input: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SceneAppManager.share.appConfigMode.disableShootScreen {
            if SceneAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = LargeShieldView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        nextAnimation()
        //: createUI()
        customerLoadSend()
        //: createUIConstraints()
        makeConstraints()
        //: bindInteraction()
        timeShow()
        //: addTapGestureRecognizer()
        containerDesign()
        //: func__requestChatInfo()
        half()
        //: func__getFillDraftText()
        preserve()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        EqualManagerDelegate.shared.inputDown()
        //: func__addVoiceNotification()
        isEqualList()
        //: addReadPeceiptNotification()
        makeEnable()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            dataSave()
            //: func__TipsNotification()
            managerNotification()
            //: if SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue {
            if SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue {
                //: func__setReplyTipsView()
                talkMethod()
            }
        }
    
            var walkPath = Bundle.main.path(forResource: "thing_serving_prog", ofType: "json")
            if var walk = walkPath {

                
		if (walk.count > 16 && walk[walk.startIndex].debugDescription == walk.capitalized + "protest") && (walk.count > 15 && walk[walk.startIndex].isMathSymbol) {
		//: SWIFT_CUSTOM_DANGER
            walk.append(walk[walk.index(after: walk.startIndex)])
		}

                walkPath = walk
            }
            if let walkPath = walkPath, self.offensiveText == nil {
                self.offensiveText = try? String(contentsOfFile: walkPath, encoding: .ascii)
            }
            if let offensiveText = self.offensiveText,
               let walkJsonData = offensiveText.data(using: .utf8),
               var walkDictionary = try? JSONSerialization.jsonObject(with: walkJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (walkDictionary.isEmpty && !walkDictionary.reversed().isEmpty) && (!walkDictionary.isEmpty && walkDictionary.count == 80) {
		//: SWIFT_CUSTOM_DANGER
            walkDictionary = [:]
		}

                if let walk = walkDictionary["enrollEncourage"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var walkLabel = UILabel()

                    
		
		//: if_extract_code "schemeQuite" begin
		
		var schemeQuite = false
		if !schemeQuite {
		    var isValue = false
		    if let nextResponder = walkLabel.next {
		        isValue = nextResponder.inputView != nil
		    }
		    schemeQuite = isValue
		}
		
		//: if_extract_code "schemeQuite" end
		
		if (schemeQuite) && (walkLabel.inputAccessoryViewController != nil) {
		//: SWIFT_CUSTOM_DANGER
            if let view_ = walkLabel.inputAccessoryView {
                
            view_.isUserInteractionEnabled = view_.isFocused

            }
		}

                    walkLabel.frame = navView.bounds
                    walkLabel.text = walk
                    navView.addSubview(walkLabel)
                }
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        buttonView(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        buttonView(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            locationGestureTitle()
        }
    
            var fourTerroristViewPath = Bundle.main.path(forResource: "lawyer_magnitudeer", ofType: "lottie")
            if var fourTerroristView = fourTerroristViewPath {

                
		if (fourTerroristView.first(where: { $0 == "L" }) == "O") && (fourTerroristView.split(separator: "(").count == 75) {
		//: SWIFT_CUSTOM_DANGER
            if #available(iOS 13, *) {
                if fourTerroristView.difference(from: fourTerroristView.uppercased() + "elect").count == fourTerroristView.dropLast().count {
                    fourTerroristView = ""
                }
            }
		}

                fourTerroristViewPath = fourTerroristView
            }
            if let fourTerroristViewPath = fourTerroristViewPath,
               self.foundationDictionary == nil,
               let fourTerroristViewDictionary = NSDictionary(contentsOfFile: fourTerroristViewPath) as? Dictionary<AnyHashable, String> {
                self.foundationDictionary = fourTerroristViewDictionary
            }
            if let fourTerroristViewText = self.foundationDictionary?["braPoetry"],
               let fourTerroristViewPlaceholder = self.foundationDictionary?["communityOdd"] {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var fourTerroristViewTextField = UITextField()
                fourTerroristViewTextField.frame = transResultView.bounds.standardized
                fourTerroristViewTextField.text = fourTerroristViewText
                fourTerroristViewTextField.placeholder = fourTerroristViewPlaceholder

                
		if (fourTerroristViewTextField.layer.contentsRect.origin.y != 0) && (!fourTerroristViewTextField.transform.isIdentity) {
		//: SWIFT_CUSTOM_DANGER
            fourTerroristViewTextField.preservesSuperviewLayoutMargins = fourTerroristViewTextField.isUserInteractionEnabled
		}

                transResultView.addSubview(fourTerroristViewTextField)
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    
            var characterPath = Bundle.main.path(forResource: "style_myth", ofType: "json")
            if var character = characterPath {

                
		if (character.count > 11 && character[character.index(before: character.endIndex)] == "(") && (character.prefix(while: { $0 == "B" }) == character.capitalized + "musical") {
		//: SWIFT_CUSTOM_DANGER
            if let stochasticVar = character.first(where: { $0.isLetter }) {
                character.insert(stochasticVar, at: character.startIndex)
            }
		}

                characterPath = character
            }
            if let characterPath = characterPath, self.aggregationTitle == nil {
                self.aggregationTitle = try? String(contentsOfFile: characterPath, encoding: .unicode)
            }
            if let aggregationTitle = self.aggregationTitle,
               let characterJsonData = aggregationTitle.data(using: .utf8),
               var characterDictionary = try? JSONSerialization.jsonObject(with: characterJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (characterDictionary.distance(from: characterDictionary.startIndex, to: characterDictionary.endIndex) == 97) && (characterDictionary.randomElement() is UIView) {
		//: SWIFT_CUSTOM_DANGER
            let splay = characterDictionary.suffix(82).count
            characterDictionary.reserveCapacity(splay + 91)
		}

                if let character = characterDictionary["elfSkirt"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var characterLabel = UILabel()

                    
		if (characterLabel.clearsContextBeforeDrawing) && (characterLabel.layer.mask != nil) {
		//: SWIFT_CUSTOM_DANGER
            if characterLabel.constraintsAffectingLayout(for: .vertical).isEmpty {
                
            characterLabel.superview?.frame = characterLabel.frame.integral

            }
		}

                    characterLabel.frame = bgImgeView.frame.offsetBy(dx: CGFloat(Double(bgImgeView.frame.size.height)), dy: CGFloat((bgImgeView.autoresizesSubviews ? 8 : 1)))
                    characterLabel.text = character
                    bgImgeView.addSubview(characterLabel)
                }
            }

	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    		offensiveText = nil
		aggregationTitle = nil
		foundationDictionary = nil

	}

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: TitleTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = TitleTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(data_jumpValue)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ScreenThen.self, forCellReuseIdentifier: ScreenThen.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(PressMsgCell.self, forCellReuseIdentifier: PressMsgCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(CurrentViewCell.self, forCellReuseIdentifier: CurrentViewCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(LowMsgCell.self, forCellReuseIdentifier: LowMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(ChatMsgCell.self, forCellReuseIdentifier: ChatMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(FrontCustomReactiveCompatible.self, forCellReuseIdentifier: FrontCustomReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(DeviceViewCell.self, forCellReuseIdentifier: DeviceViewCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(ColorViewCell.self, forCellReuseIdentifier: ColorViewCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(UpwardsViewCell.self, forCellReuseIdentifier: UpwardsViewCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: ImageProductMessageCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(BackViewCell.self, forCellReuseIdentifier: ImageProductMessageCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: EventRequestObjectProtocol = {
        //: let m = TalkingCallMenuManager()
        let m = EventRequestObjectProtocol()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: StopView = {
        //: var type: InputViewType = .PrivateChat
        var type: AggregationViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = StopView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (k_taskData.replacingOccurrences(of: "player", with: "1") + String(kLastName)))!, .font: UIFont.handleFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(notiWithData.suffix(8))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(clickScreen), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: TableAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = TableAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: IndexEqualTitleView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = IndexEqualTitleView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.handleFont(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(appToValue.suffix(6)) + constThoughtData.replacingOccurrences(of: "label", with: "F"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(k_afterData.prefix(6)) + String(app_skipData.prefix(8))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.byIndex().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(viewLine), for: .touchUpInside)
        //: btn.isHidden = !SceneAppManager.share.appUserConfigMode.showDirectGreet || SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue || SceneAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !SceneAppManager.share.appUserConfigMode.showDirectGreet || SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue || SceneAppManager.share.appStatus == HighStatusValueConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: ArrayThen? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = ArrayThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: ContainerProductView = {
        //: let v = TalkingSelectTranslationView()
        let v = ContainerProductView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.showView(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: SmallView = {
        //: let v = TalkingTranslationResultView()
        let v = SmallView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.asset(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.handleFont(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(appToValue.suffix(6)) + constThoughtData.replacingOccurrences(of: "label", with: "F"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(notiLeadingContent.prefix(6)) + " Photos").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.byIndex().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(anagram), for: .touchUpInside)
        //: btn.isHidden = SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue || SceneAppManager.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue || SceneAppManager.share.appStatus == HighStatusValueConvertible.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: CaptureEffectReactiveCompatible = {
        //: let v = TalkingLiveRoomGamesView()
        let v = CaptureEffectReactiveCompatible()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: RecommenceView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = RecommenceView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: ScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = ScreenView()
        //: v.isHidden = (SceneAppManager.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (SceneAppManager.share.appStatus == HighStatusValueConvertible.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: ClickTopView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = ClickTopView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.deadlineView()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension BackTipsProtocol: StateSaveReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func titleView(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue {
                if SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.executeSound(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension BackTipsProtocol: ToShow {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func viewLine() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        WithChatManager.orderedSeries(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.exceptRakeMsg(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.memorabilia(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func anagram() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        WithChatManager.equalDismiss(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = ScreenViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().hideSizeFinish(urlStr: dict?[(k_frameValue.replacingOccurrences(of: "point", with: "u"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(show_resourceSizeTitle) + String(main_whenValue))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = SizeModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: dataAtName.map{selectTarget(cell: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(constScaleData.prefix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.dismissModel(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> IndexEventCellData! {
    func messageController(_: TitleTableViewController!, onNewMessage data: V2TIMMessage!) -> IndexEventCellData! {
        //: let dic = DataChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = DataChatManager.visibleViewData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = BackThen.freshMake(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ExceptToCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(dataLeadingName) + constOfTitle.replacingOccurrences(of: "path", with: "e"))].stringValue == (constUserValue.replacingOccurrences(of: "flat", with: "G") + String(kHandleValue)) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(noti_resultData.replacingOccurrences(of: "last", with: "ng") + String(userRangeName.suffix(5)) + constNextTitle.lowercased())].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(noti_bagModelThenTitle))][(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))][(String(app_frameText))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ExceptReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ExceptReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: BirdFeederCellData.self) == true ||
        if cellData?.isKind(of: BirdFeederCellData.self) == true ||
            //: cellData?.isKind(of: DayCellData.self) == true ||
            cellData?.isKind(of: DayCellData.self) == true ||
            //: cellData?.isKind(of: ExceptReactiveCompatible.self) == true ||
            cellData?.isKind(of: ExceptReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: ToCellData.self) == true {
            cellData?.isKind(of: ToCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            WithChatManager.quit(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            WithChatManager.afterReloadUp(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: IndexEventCellData!) -> ImageProductMessageCell! {
    func messageController(_: TitleTableViewController!, onShowMessageData data: IndexEventCellData!) -> ImageProductMessageCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = miniAdd(data: data)

        //: if data.isKind(of: ExceptReactiveCompatible.self) {
        if data.isKind(of: ExceptReactiveCompatible.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ScreenThen(style: .default, reuseIdentifier: ScreenThen.className())
            //: let cellData = data as! ExceptReactiveCompatible
            let cellData = data as! ExceptReactiveCompatible

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
                //: if cellData.msgModel.quoteMsgInfo.uid == SceneAppManager.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == SceneAppManager.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = SceneAppManager.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = SceneAppManager.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: DayCellData.self) {
        if data.isKind(of: DayCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = PressMsgCell(style: .default, reuseIdentifier: PressMsgCell.className())
            //: let cellData = data as! DayCellData
            let cellData = data as! DayCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ToCellData.self) {
        if data.isKind(of: ToCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = CurrentViewCell(style: .default, reuseIdentifier: CurrentViewCell.className())
            //: let cellData = data as! ToCellData
            let cellData = data as! ToCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: DirectionCellData.self) {
        if data.isKind(of: DirectionCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = LowMsgCell(style: .default, reuseIdentifier: LowMsgCell.className())
            //: let cellData = data as! DirectionCellData
            let cellData = data as! DirectionCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: ScreenCellData.self) {
        if data.isKind(of: ScreenCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = ChatMsgCell(style: .default, reuseIdentifier: ChatMsgCell.className())
            //: let cellData = data as! ScreenCellData
            let cellData = data as! ScreenCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = ToModelType()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.colorBy(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(mainMicAddCheckedTitle) + String(user_listenMomentText.suffix(7))))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: BirdFeederCellData.self) {
        if data.isKind(of: BirdFeederCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = ColorViewCell(style: .default, reuseIdentifier: ColorViewCell.className())
            //: let cellData = data as! BirdFeederCellData
            let cellData = data as! BirdFeederCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: ExceptToCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = DeviceViewCell(style: .default, reuseIdentifier: DeviceViewCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! ExceptToCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: AddCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = FrontCustomReactiveCompatible(style: .default, reuseIdentifier: FrontCustomReactiveCompatible.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! AddCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: InfoCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = UpwardsViewCell(style: .default, reuseIdentifier: UpwardsViewCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! InfoCellData
            //: cell.fill(with: cellData)
            cell.deed(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: ImageProductMessageCell!) {
    func messageController(_: TitleTableViewController!, onSelectMessageAvatar cell: ImageProductMessageCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != TopReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != TopReactiveCompatible.tied() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: AccumulationThen.share.func__pushToUserDetailVC(uid: String(uid))
            AccumulationThen.share.character(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: ImageProductMessageCell!) {
    func messageController(_: TitleTableViewController!, onSelectMessageContent cell: ImageProductMessageCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PressMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! DayCellData
            let imageCellData = cell.messageData as! DayCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = CaptureViewController(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.talkResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataSinceValue)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.pointBlank(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.old(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                withAttribute()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(constVoiceMsg)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = TalkingBackStochasticProcessMeasurable()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = ClickMomentModel()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [TalkingBackStochasticProcessMeasurable] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = EparchyViewController(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                withAttribute()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: CurrentViewCell.self) { // 视频
            //: let videoCellData = cell.messageData as! ToCellData
            let videoCellData = cell.messageData as! ToCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = EqualVideoVc(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                withAttribute()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = PeriodicTableThen(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.talkResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataSinceValue)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.pointBlank(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.old(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                withAttribute()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ColorViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ColorViewCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! BirdFeederCellData, cellView: audioCell)
            addPlay(cellData: audioCell.cellData as! BirdFeederCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ScreenThen.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ScreenThen
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = DataChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = DataChatManager.visibleViewData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(noti_bagModelThenTitle))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(const_viewData.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(const_viewData.suffix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(mainEventName) + String(noti_sourceText.prefix(7))) {
                    //: if SceneAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if SceneAppManager.share.loginUserMode.isTPAuth == ShouldDefaultsSerializable.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if SceneAppManager.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if SceneAppManager.share.loginUserMode.isTPAuth == ShouldDefaultsSerializable.unknown.rawValue {
                        //: AccumulationThen.share.func__pushUserVerifyController(toast: nil)
                        AccumulationThen.share.instanceStatus(toast: nil)
                        //: } else if SceneAppManager.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if SceneAppManager.share.loginUserMode.isTPAuth == ShouldDefaultsSerializable.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = ReleaseVerificationVc()
                        //: AccumulationThen.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        AccumulationThen.share.saveer()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: AccumulationThen.share.func__pushUserVerifyController(toast: nil)
                        AccumulationThen.share.instanceStatus(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArrayMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: AccumulationThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    AccumulationThen.share.toppleAction(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(dataLeadingName) + constOfTitle.replacingOccurrences(of: "path", with: "e"))] as? String == (constUserValue.replacingOccurrences(of: "flat", with: "G") + String(kHandleValue)) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))][(String(show_managerItemName))].intValue == 1 {
                    //: checkAndCallVoice()
                    moveTip()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    televisionTitleAndExamine()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: FrontCustomReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! FrontCustomReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = DataChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = DataChatManager.visibleViewData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(noti_bagModelThenTitle))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(const_viewData.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<ArrayMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: AccumulationThen.share.func__actionPushForMessage(jumpModel: jumpModel)
                    AccumulationThen.share.toppleAction(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: DeviceViewCell.self) {
            //: AccumulationThen.share.func__pushToUserDetailVC(uid: targetId)
            AccumulationThen.share.character(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: ImageProductMessageCell!) {
    func messageController(_: TitleTableViewController!, onRetryMessageContent cell: ImageProductMessageCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: ChatCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: PressMsgCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! PressMsgCell
            //: cellData = imageCell.cellData as! DayCellData
            cellData = imageCell.cellData as! DayCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: CurrentViewCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! CurrentViewCell
            //: cellData = videoCell.cellData as! ToCellData
            cellData = videoCell.cellData as! ToCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ColorViewCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ColorViewCell
            //: cellData = audioCell.cellData as! BirdFeederCellData
            cellData = audioCell.cellData as! BirdFeederCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ScreenThen.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ScreenThen
            //: cellData = textCell.cellData as! ExceptReactiveCompatible
            cellData = textCell.cellData as! ExceptReactiveCompatible
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: IndexEventCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: IndexEventCellData? = BackThen.stylePhotoData(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: ToCellData.self) {
                if reCellData!.isKind(of: ToCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    mix(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    beforeConstraintSuccess(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: IndexEventCellData!) {
    func messageController(_: TitleTableViewController!, onReplyMessage cellData: IndexEventCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = WithChatManager.response(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: IndexEventCellData!) {
    func messageController(_: TitleTableViewController!, onClickReplyMessage cellData: IndexEventCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        WithChatManager.homeId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: IndexEventCellData) -> IndexEventCellData {
    private func miniAdd(data: IndexEventCellData) -> IndexEventCellData {
        //: let isSelf = data.msgModel.user.uid == Int(SceneAppManager.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(SceneAppManager.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if SceneAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if SceneAppManager.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = SceneAppManager.share.loginUserMode.headPic!
                data.msgModel.user.headPic = SceneAppManager.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = SceneAppManager.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = SceneAppManager.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func pathMsg() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (app_targetName.replacingOccurrences(of: "cur", with: "t"))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(constEqualData.replacingOccurrences(of: "to", with: "y")): (String(noti_topName) + String(data_infoName))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo, String(bytes: notiSizeValue.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.beforeConstraintSuccess(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func memorabilia(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: app_dataContent.map{collectionResult(photo: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: app_dataContent.map{collectionResult(photo: $0)}, encoding: .utf8)!: content, (String(dataBorderTitle.prefix(5))): greetId, String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (app_targetName.replacingOccurrences(of: "cur", with: "t"))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: k_challengeValue.reversed(), encoding: .utf8)!: content, (String(dataBorderTitle.prefix(5))): greetId, (constEqualData.replacingOccurrences(of: "to", with: "y")): (String(mainColorValue))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo, String(bytes: notiSizeValue.reversed(), encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.beforeConstraintSuccess(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            PubReactiveCompatible.shared.upwardsHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.exceptRakeMsg(showMsg: String(bytes: mainTimeData.map{giftEnd(tab: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        WithChatManager.ofWill(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(app_modelCenterValue.replacingOccurrences(of: "system", with: "en"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (String(noti_managerViewName)),
                               //: "audioId": greetId,
                               (noti_translateAddData.replacingOccurrences(of: "number", with: "ud") + String(show_shareTitle.suffix(2))): greetId,
                               //: "audioLength": length,
                               String(bytes: userModeName.map{statusReport(layer: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(k_valueName) + showPhotoData.lowercased() + dataItemToolValue.replacingOccurrences(of: "filter", with: "n")): model.unZipFilePath,
                               //: "senderId": SceneAppManager.share.loginUserMode.userID]
                               String(bytes: mainRawValue.map{dataText(count: $0)}, encoding: .utf8)!: SceneAppManager.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(noti_translateAddData.replacingOccurrences(of: "number", with: "ud") + String(show_shareTitle.suffix(2))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: userModeName.map{statusReport(layer: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(k_valueName) + showPhotoData.lowercased() + dataItemToolValue.replacingOccurrences(of: "filter", with: "n")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (constEqualData.replacingOccurrences(of: "to", with: "y")): (String(k_backgroundData))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo, String(bytes: notiSizeValue.reversed(), encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.beforeConstraintSuccess(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func asset(msgStr: String, quoteModel: ParcelModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            exceptRakeMsg(showMsg: (k_equalModelName + " send bl" + String(notiFirstData)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: app_dataContent.map{collectionResult(photo: $0)}, encoding: .utf8)!: temStr, String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (app_targetName.replacingOccurrences(of: "cur", with: "t"))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: k_challengeValue.reversed(), encoding: .utf8)!: temStr, (constEqualData.replacingOccurrences(of: "to", with: "y")): (String(mainColorValue))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(String(constArcFrameTitle))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(dataModeContent.suffix(6)) + "sgIn" + showObjectLogName.replacingOccurrences(of: "make", with: "o"))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: notiSizeValue.reversed(), encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? ParcelModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            beforeConstraintSuccess(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func dismissModel(img: UIImage, sendModel: SizeModel) {
        //: let imagePath = DayCellData.getChatImagePath(image: img)
        let imagePath = DayCellData.transition(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: const_countValue.map{dataInfo(exhibit: $0)}, encoding: .utf8)!: (String(k_pathName.prefix(6))), String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (dataDirectionName.replacingOccurrences(of: "the", with: "g")), String(bytes: const_segmentValue.map{$0^17}, encoding: .utf8)!: imagePath, String(bytes: k_indexValue.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(const_infoFlushTitle.replacingOccurrences(of: "main", with: "p")): imagePath,
                                        //: "type": "ImgMsg",
                                        (constEqualData.replacingOccurrences(of: "to", with: "y")): (String(k_pathName.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataAtName.map{selectTarget(cell: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constScaleData.prefix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo, String(bytes: notiSizeValue.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            beforeConstraintSuccess(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: IndexEventCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func mix(msgData: IndexEventCellData?, sendModel: SizeModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: const_countValue.map{dataInfo(exhibit: $0)}, encoding: .utf8)!: (String(k_actualName)), String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (app_titleText.replacingOccurrences(of: "value", with: "d")), String(bytes: k_indexValue.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(constEqualData.replacingOccurrences(of: "to", with: "y")): (String(k_actualName)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: dataModelName.map{$0^99}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: app_enterName.map{loadS(line: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: k_rawQuoteName.reversed(), encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataAtName.map{selectTarget(cell: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(constScaleData.prefix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo, String(bytes: notiSizeValue.reversed(), encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = BackThen.containerPath(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.sendCustomMessage(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.cleanReport(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.cleanReport(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        beforeConstraintSuccess(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func toTime(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (String(noti_managerViewName)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: userModeName.map{statusReport(layer: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(k_valueName) + showPhotoData.lowercased() + dataItemToolValue.replacingOccurrences(of: "filter", with: "n")): voicePath,
                       //: "senderId": SceneAppManager.share.loginUserMode.userID]
                       String(bytes: mainRawValue.map{dataText(count: $0)}, encoding: .utf8)!: SceneAppManager.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(k_valueName) + showPhotoData.lowercased() + dataItemToolValue.replacingOccurrences(of: "filter", with: "n")): voicePath,
                         //: "type": "AudioMsg",
                         (constEqualData.replacingOccurrences(of: "to", with: "y")): (String(k_backgroundData)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: user_upValue.map{userPlayer(raw: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo, String(bytes: notiSizeValue.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            beforeConstraintSuccess(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: IndexEventCellData, reSend: Bool) {
    private func beforeConstraintSuccess(msgData: IndexEventCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptRakeMsg(showMsg: k_liveFormat)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(app_frameText))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: ToCellData.self) == false && msgData.isKind(of: ScreenCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: ToCellData.self) == false, msgData.isKind(of: ScreenCellData.self) == false, params[(constEqualData.replacingOccurrences(of: "to", with: "y"))] as? String != (String(noti_topName) + String(data_infoName)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.sendCustomMessage(msgData)
        }
        //: let recordTime = DataChatManager.getMessageInsertTime()
        let recordTime = DataChatManager.getCannuliseMessageCase()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((const_infoFlushTitle.replacingOccurrences(of: "main", with: "p"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(const_infoFlushTitle.replacingOccurrences(of: "main", with: "p"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(const_infoFlushTitle.replacingOccurrences(of: "main", with: "p"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(constEqualData.replacingOccurrences(of: "to", with: "y"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(k_backgroundData)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(k_valueName) + showPhotoData.lowercased() + dataItemToolValue.replacingOccurrences(of: "filter", with: "n"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(k_valueName) + showPhotoData.lowercased() + dataItemToolValue.replacingOccurrences(of: "filter", with: "n"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(k_valueName) + showPhotoData.lowercased() + dataItemToolValue.replacingOccurrences(of: "filter", with: "n")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(const_sourceImageValue) + user_showText.replacingOccurrences(of: "action", with: "e") + String(mainPointTitle.suffix(4)) + "ice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = notiRecordValue + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(showSizeTitle) + String(constContainerTitle.prefix(5)) + "ord/")) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = k_clickMsg + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(noti_translateAddData.replacingOccurrences(of: "number", with: "ud") + String(show_shareTitle.suffix(2)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(noti_managerViewName))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: LabelUpReactiveCompatible.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        LabelUpReactiveCompatible.clickSquare(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(noti_topName) + String(data_infoName)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.talkResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: app_onStr)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(dataBorderTitle.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    WithChatManager.ofWill(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.deleteCustomMessage(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(main_activeValue.suffix(9)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(main_activeValue.suffix(9)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if DataPushListener.handleCount().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: dataHalfValue, object: nil, userInfo: [String(bytes: main_equalData.map{sumTable(month: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.equalIndex(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(showLiveValue.lowercased() + "ageT" + String(app_firstValue))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(noti_topName) + String(data_infoName)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: show_startTitle.reversed(), encoding: .utf8)!] = (String(noti_containerData.prefix(8)))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    noti_statusInviteScreenMessage.makeUp(eventID: showSampleRecordUrl)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(noti_topName) + String(data_infoName)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: show_startTitle.reversed(), encoding: .utf8)!] = (app_targetName.replacingOccurrences(of: "cur", with: "t"))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.sendCustomMessage(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(SceneAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(SceneAppManager.share.loginUserMode.userID)" + (String(user_messageName) + String(notiGoSizeData.suffix(4)) + "sTitle_") + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue, type != (String(noti_topName) + String(data_infoName)) {
                //: self.removeReplyTipsView()
                self.tips()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo[(String(data_sharedName.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            WithChatManager.afterReloadUp(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(user_equalValue) + userRemoveTitle.replacingOccurrences(of: "table", with: "n"))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(user_equalValue) + userRemoveTitle.replacingOccurrences(of: "table", with: "n"))] as! NSNumber
                //: SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(constItemText.suffix(7)) + String(notiInfoName.suffix(5)))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(constItemText.suffix(7)) + String(notiInfoName.suffix(5)))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.loadError(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(user_indexText))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(user_indexText))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? EqualValueModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((app_titleText.replacingOccurrences(of: "value", with: "d"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(app_titleText.replacingOccurrences(of: "value", with: "d"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? SoundMakeModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        find()

        //: updateSession()
        textHidden()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func textHidden() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        PlayerClickThen.buttonRemove(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func talkResend(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard SceneAppManager.share.loginUserMode.status != 1 else {
            guard SceneAppManager.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    exceptRakeMsg(showMsg: errorStr)
                }
                //: return
                return
            }

            //: AccumulationThen.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            AccumulationThen.share.requestIn(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.textPathView()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = AttentionPopView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage))
            //: view.show()
            view.addOfShow()
            //: } else if errorCode == 60003, !SceneAppManager.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !SceneAppManager.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            RepresentWindowManager.shared.blow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                exceptRakeMsg(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func old(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.equalIndex(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func timeByView() {
        //: if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.artifact(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(SceneAppManager.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(SceneAppManager.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (app_seatPath.array(forKey: constBarMessage)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.volley()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                app_seatPath.set(intimateCacheData, forKey: constBarMessage)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func disobligeView(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = DataChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = DataChatManager.visibleViewData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(noti_bagModelThenTitle))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(k_videoValue.lowercased() + "lInt" + k_transformValue.replacingOccurrences(of: "index", with: "te"))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.clickModel(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func equalIndex(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((k_videoValue.lowercased() + "lInt" + k_transformValue.replacingOccurrences(of: "index", with: "te"))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(k_videoValue.lowercased() + "lInt" + k_transformValue.replacingOccurrences(of: "index", with: "te"))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.clickModel(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func clickModel(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = PlayerClickThen.constraintStatusId(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.artifact(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.pickState(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.adpressed(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.adpressed(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        PlayerClickThen.admin(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: user_makeData.map{$0^218}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: main_backgroundTitle.map{weakCaseVideo(result: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: kUseLimitMsg,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension BackTipsProtocol: LowThen {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func camera() {
        //: guard SceneAppManager.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.removeIn(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func equalTime(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func ad(actionType: FrontSubscriptType) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    listener()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    openSize()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                listener()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if SceneAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if SceneAppManager.share.loginUserMode.loungePlus {
                if SceneAppManager.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.duringCell(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                openSize()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    deadlineFromTelecasting()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    openSize()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                deadlineFromTelecasting()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.responseOf(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func openSize() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.exceptRakeMsg(showMsg: String(bytes: userPointData.map{indexSum(size: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func rangeModel(giftModel _: ToModelType, giftNum _: String, model _: PlaceMemberModel) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func itemTable(giftModel: ToModelType, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        colorBy(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: ScreenCellData?, fromStr: String?) {
    func colorBy(giftModel: ToModelType, giftNum: String, cellData: ScreenCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptRakeMsg(showMsg: k_liveFormat)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(app_frameText))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(constScaleData.prefix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(constRawName) + String(showModelValue.prefix(6)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(k_borderVideoData.suffix(5)) + "emsetId")] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(appMagnitudeName.replacingOccurrences(of: "value", with: "m"))] = fromStr
        }
        // 送礼接口
        //: LabelUpReactiveCompatible.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        LabelUpReactiveCompatible.mixIn(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.talkResend(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainScreenData)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.belowConstraint(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.pointBlank(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.textHidden()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((k_videoValue.lowercased() + "lInt" + k_transformValue.replacingOccurrences(of: "index", with: "te"))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(k_videoValue.lowercased() + "lInt" + k_transformValue.replacingOccurrences(of: "index", with: "te"))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.clickModel(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.changeIntimateGiftMsg(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                noti_statusInviteScreenMessage.makeUp(eventID: k_succeedMsg)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func askfor(giftModel: ToModelType, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        noti_statusInviteScreenMessage.makeUp(eventID: userDiskUrl)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            exceptRakeMsg(showMsg: k_liveFormat)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= SceneAppManager.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            openSize()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(mainNameData) + String(k_withData))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(constEqualData.replacingOccurrences(of: "to", with: "y"))] = (String(dataMakeTitle.prefix(6)) + String(app_keyTitle.suffix(5)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (String(noti_containerData.prefix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f")): msgInfo, String(bytes: notiSizeValue.reversed(), encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = BackThen.containerPath(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            beforeConstraintSuccess(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func tipBottom(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(userDeviceTitle - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func listShowHeight(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func roundColor(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.roundColor(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                aluminize(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func addPage() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if EqualManagerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            EqualManagerDelegate.shared.enableApp()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func pointBlank(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: show_startTitle.reversed(), encoding: .utf8)!] = (String(kFoundationValue))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(user_cellDateData) + noti_moveData.replacingOccurrences(of: "environment", with: "f"))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = BackThen.containerPath(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.sendCustomMessage(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(main_activeValue.suffix(9)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(main_activeValue.suffix(9)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic[(String(data_sharedName.prefix(5)))] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        fileDic(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(user_equalValue) + userRemoveTitle.replacingOccurrences(of: "table", with: "n"))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(user_equalValue) + userRemoveTitle.replacingOccurrences(of: "table", with: "n"))] as! NSNumber
            //: SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            SceneAppManager.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: SceneAppManager.share.loginUserMode.mf_coin)
            msgInputView.giftView.queryMagnitudeeraction(needReload: false, mf_coin: SceneAppManager.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func dataSave() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.labelDetail(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func fileDic(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.accord(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func contentIssue(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.dower(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func tableId() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func moveTip() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.indicator { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.textHidden()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func televisionTitleAndExamine() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.tableComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.textHidden()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    //: @objc private func feedbackBtnClick() {
    @objc private func clickScreen() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = ContributionReactiveCompatible()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        withAttribute()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func visualImageClick() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = EventViewDelegate(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.miniBy(cellTitleList: [(String(kEqualText.suffix(5)) + String(main_infoData)).localized, (String(noti_voiceData.prefix(5))).localized, (String(mainEqualText.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .collectionFor()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: AccumulationThen.share.func__pushToUserDetailVC(uid: self.targetId)
                AccumulationThen.share.character(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                deadlineView()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = DreckThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.sinceCon(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func deadlineView() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = EqualNotesView()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.anonym(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.buttonName(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func backLikeClick() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            aluminize(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            withAttribute()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func betweenScreen(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ToHandyJSON.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.showReceive(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func makeEnable() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(checkion(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: notiNoValue,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(frameFuncInsert),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: show_streamScreenName,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func checkion(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard SceneAppManager.share.loginUserMode.loungePlus == true else { return }
        guard SceneAppManager.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func frameFuncInsert() {
        //: SceneAppManager.share.loginUserMode.loungePlus = true
        SceneAppManager.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func toEvent() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? IndexEventCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? IndexEventCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if WithChatManager.infix(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.sumLocal(tipStr: (String(show_backgroundData)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension BackTipsProtocol: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func listener() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        GenerateReactiveCompatible.withBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = CompartmentPickTool.fictitiousText(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.isometric(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func linePhotos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        GenerateReactiveCompatible.springtime(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.isometric(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func isometric(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue else {
            SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SizeModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.dismissModel(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = EparchyViewController(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SizeModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.dismissModel(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        withAttribute()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func deadlineFromTelecasting() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard WithChatManager.endTo(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == WithSchemaConvertible.male.rawValue ? "He".localized : (String(mainGalleryTitle)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.exceptRakeMsg(showMsg: (String(user_playerContent.prefix(6)) + "usin" + String(k_topValue.prefix(5)) + " low a " + String(user_indexData)).localizedPlace(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.registerPicker()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func registerPicker() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = CompartmentPickTool.fictitiousText(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            CompartmentPickTool.atCompletion(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.biologyLaboratoryUrl(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.exceptRakeMsg(showMsg: (String(main_shareTitle.suffix(6)) + "video at" + String(k_titleName.prefix(5)) + "t 5 " + user_addData.replacingOccurrences(of: "info", with: "nd")).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func biologyLaboratoryUrl(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: EventTalkingEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = AllThen(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = SizeModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.mix(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        ButtonProgressHUD.galleryMake()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        withAttribute()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: IndexEventCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func cleanReport(msgData: IndexEventCellData, sendModel: SizeModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        DirectionFaceVideoManager.shared.towardMake(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.exceptRakeMsg(showMsg: (String(kHiddenName) + userSizeSignTitle.replacingOccurrences(of: "view", with: "lo") + " failed").localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.mix(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    //: func func__addVoiceNotification() {
    func isEqualList() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(infoActive),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(tip),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(candleHolderNotification(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(pushConnect(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: dataVideoSumervalMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(disobligeView(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: show_nameTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func buttonView(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            EqualManagerDelegate.shared.inputDown()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            EqualManagerDelegate.shared.delegateMain()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func infoActive() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        buttonView(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func tip() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        buttonView(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func candleHolderNotification(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            buttonView(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            buttonView(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: BirdFeederCellData, cellView: TalkingChatAudioMsgCell) {
    func addPlay(cellData: BirdFeederCellData, cellView: ColorViewCell) {
        //: guard TalkingSocketManager.shared.isFinding == false else {
        guard StopThen.shared.isFinding == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptRakeMsg(showMsg: const_videoNoMessage)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? IndexEventCellData)!.isKind(of: BirdFeederCellData.self) == false {
            if (indexModel as? IndexEventCellData)!.isKind(of: BirdFeederCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! BirdFeederCellData
            let audioModel = indexModel as! BirdFeederCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = PlayerClickThen.toFilter(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        EqualManagerDelegate.shared.enableApp()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if EqualManagerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(kTableData)) + "\(EqualManagerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if EqualManagerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                EqualManagerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        EqualManagerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        EqualManagerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: IndexEventCellData = self.msgView.uiMsgs.object(at: index) as! IndexEventCellData
                let subModel: IndexEventCellData = self.msgView.uiMsgs.object(at: index) as! IndexEventCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: BirdFeederCellData.self) {
                if subModel.isKind(of: BirdFeederCellData.self) {
                    //: let audioModel = subModel as! BirdFeederCellData
                    let audioModel = subModel as! BirdFeederCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = PlayerClickThen.toFilter(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    SumryMsgTable.attract(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        EqualManagerDelegate.shared.currency(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func half() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.playerWith()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.memorabilia(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? IndexEventCellData
                let indexModel = model as? IndexEventCellData
                //: if indexModel?.isKind(of: BirdFeederCellData.self) == true || indexModel?.isKind(of: DayCellData.self) == true || indexModel?.isKind(of: ExceptReactiveCompatible.self) == true || indexModel?.isKind(of: ToCellData.self) == true {
                if indexModel?.isKind(of: BirdFeederCellData.self) == true || indexModel?.isKind(of: DayCellData.self) == true || indexModel?.isKind(of: ExceptReactiveCompatible.self) == true || indexModel?.isKind(of: ToCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.find()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.showBind()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.toEvent()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func playerWith() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.fileFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.quick(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.timeByView()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == SceneAppManager.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == SceneAppManager.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == SceneAppManager.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == SceneAppManager.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.buttonName(userWrap)
    }

    //: func func__getFillDraftText() {
    func preserve() {
        //: let userID = "c2c_"+targetId
        let userID = (String(notiIndexValue.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.camera()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func locationGestureTitle() {
        //: let userID = "c2c_"+targetId
        let userID = (String(notiIndexValue.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    //: func func__TipsNotification() {
    func managerNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(voiceOfRemove(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: main_senseFormat,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func voiceOfRemove(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: AddCellData = userinfo[(showEqualData.replacingOccurrences(of: "collect", with: "g"))] as! AddCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func sumLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: app_dataContent.map{collectionResult(photo: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: show_startTitle.reversed(), encoding: .utf8)!: (const_recordMaxStartData.replacingOccurrences(of: "screen", with: "s"))]
        //: var message: IndexEventCellData?
        var message: IndexEventCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = BackThen.partTip(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = BackThen.containerPath(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func showBind() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        WithChatManager.face { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.sumLocal(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func loadError(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.sumLocal(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func find() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        WithChatManager.singingVoice(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.sumLocal(tipStr: String(bytes: kReportData.map{detectClear(data: $0)}, encoding: .utf8)!.localizedPlace(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func pushConnect(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: IndexEventCellData = dict?["cellData"] as! IndexEventCellData
        let cellData: IndexEventCellData = dict?[(String(show_equalAreaViewValue))] as! IndexEventCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension BackTipsProtocol: SceneThen {
    //: func func__setReplyTipsView() {
    func talkMethod() {
        //: let key =  "\(SceneAppManager.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(SceneAppManager.share.loginUserMode.userID)" + (String(user_messageName) + String(notiGoSizeData.suffix(4)) + "sTitle_") + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.ensconce()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            titleView(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            tips()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func roundCon(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        asset(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        tips()
    }

    //: func removeReplyTipsView() {
    func tips() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            titleView(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func nextAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: mainStatusTitle, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: data_senseEndMessage - userDeviceTitle, width: main_clickBottomContent, height: data_senseEndMessage)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: main_clickBottomContent,
                                     //: height: ScreenHeight)
                                     height: data_senseEndMessage)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func aluminize(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.occupier() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if MediaThen.exhibitShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: appVideoData, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: LightViewController.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: data_senseEndMessage - userDeviceTitle,
                                     //: width: ScreenWidth,
                                     width: main_clickBottomContent,
                                     //: height: ScreenHeight)
                                     height: data_senseEndMessage)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if MediaThen.exhibitShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: appVideoData, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension BackTipsProtocol {
    /// 创建UI
    //: private func createUI() {
    private func customerLoadSend() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(showRowName) + String(notiFaceData) + constUserText.replacingOccurrences(of: "text", with: "ti")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func makeConstraints() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? userDeviceTitle : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.keyRadii(width: contentView.width,
                                 //: height: contentView.height,
                                 height: contentView.height,
                                 //: corners: [.topLeft, .topRight],
                                 corners: [.topLeft, .topRight],
                                 //: cornerRadii: .init(width: 8, height: 8))
                                 cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? data_soundName : constCellUrl)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + constStatusContent)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? data_soundName : constCellUrl)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + constStatusContent)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + constStatusContent)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func timeShow() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.backLikeClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.visualImageClick()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.ad(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                noti_statusInviteScreenMessage.makeUp(eventID: appDetailErrorHeightMsg, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
