
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_targetName:[UInt8] = [0x87,0x80,0x87,0x9a,0xc6,0x8d,0x81,0x8a,0x8b,0x9c,0xd4,0xc7,0xce,0x86,0x8f,0x9d,0xce,0x80,0x81,0x9a,0xce,0x8c,0x8b,0x8b,0x80,0xce,0x87,0x83,0x9e,0x82,0x8b,0x83,0x8b,0x80,0x9a,0x8b,0x8a]

private func nameFrame(cell num: UInt8) -> UInt8 {
    return num ^ 238
}

/*: "Followers" :*/
fileprivate let show_withData:[Character] = ["F","o","l","l","o"]
fileprivate let constEqualName:[Character] = ["w","e","r","s"]

/*: "Following" :*/
fileprivate let user_labelValue:[Character] = ["F","o","l","l","o","w","i","n","g"]

/*: "#E6E6E6" :*/
fileprivate let mainToFullFrameValue:String = "add string size item text#E6E6E6"

/*: "btn_message_more" :*/
fileprivate let k_messageName:String = "self selfbtn_"
fileprivate let main_requestValue:String = "game viewe_more"

/*: "UID:  :*/
fileprivate let show_labStyleTitle:String = "true progress res normal viewUID: "

/*: "level_ :*/
fileprivate let constNowValue:[Character] = ["l","e","v","e","l","_"]

/*: "Gift" :*/
fileprivate let data_onTitle:[UInt8] = [0x16,0x38,0x37,0x25]

private func colorBackground(value num: UInt8) -> UInt8 {
    return num ^ 81
}

/*: "Mike" :*/
fileprivate let main_sourceName:[UInt8] = [0x24,0x0,0x2,0xc]

/*: "Follow" :*/
fileprivate let data_normalName:[UInt8] = [0x38,0x61,0x5e,0x5e,0x61,0x69]

fileprivate func showPair(between num: UInt8) -> UInt8 {
    let value = Int(num) - 242
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift_voice_card" :*/
fileprivate let kContainerContent:String = "gift_with image selected interest error"
fileprivate let mainMeData:[Character] = ["c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let app_pageData:[Character] = ["b","t"]
fileprivate let k_frameValue:String = "n_partmake var range"
fileprivate let userEventData:String = "e_norif table attribute in"

/*: "btn_party_voice_pre" :*/
fileprivate let const_rawData:[Character] = ["b","t","n","_","p","a","r","t","y","_","v","o","i","c","e","_"]
fileprivate let noti_starName:String = "cellre"

/*: "attentionUid" :*/
fileprivate let userTextData:String = "AT"
fileprivate let mainLabStatusData:String = "TENTIO"

/*: "source" :*/
fileprivate let notiLinkTempValue:String = "eventurce"

/*: "Kick out" :*/
fileprivate let user_positionName:String = "Kickintimate case face user model"

/*: "Unmute" :*/
fileprivate let dataPinName:[Character] = ["U","n","m","u","t","e"]

/*: "Mute" :*/
fileprivate let mainLiveInfoValue:String = "Muteself message net hidden show"

/*: "Kick off" :*/
fileprivate let main_fatalValue:[Character] = ["K","i","c","k"," ","o","f"]
fileprivate let data_pingValue:[Character] = ["f"]

/*: "Block" :*/
fileprivate let appModelPlayerTitle:[Character] = ["B","l","o","c","k"]

/*: "Report" :*/
fileprivate let show_liveTitle:String = "self item modeReport"

/*: "#F6F6F6" :*/
fileprivate let notiAppArrayReturnValue:String = "#F6F6F6origin break for player"

/*: "Confirm to kick out the room?" :*/
fileprivate let userFloorViewName:String = "leading result main byConf"
fileprivate let const_valueName:String = "to kiresult result"
fileprivate let app_labSendToolText:[Character] = ["h","e"]
fileprivate let userIconData:String = " room?fail table selected"

/*: "Cancel" :*/
fileprivate let notiEqualLabelName:[Character] = ["C","a","n","c","e","l"]

/*: "Sure" :*/
fileprivate let constDetailExhibitText:[Character] = ["S","u","r","e"]

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let data_videoEachName:[UInt8] = [0x19,0x4a,0x3d,0xf8,0x51,0x47,0x4d,0xf8,0x4b,0x4d,0x4a,0x3d,0xf8,0x51,0x47,0x4d,0xf8,0x4f,0x39,0x46,0x4c,0xf8,0x4c,0x47,0xf8,0x3b,0x39,0x46,0x3b,0x3d,0x44,0xf8,0x4c,0x40,0x3d,0xf8,0x45,0x4d,0x4c,0x3d,0x17]

fileprivate func menuUser(since num: UInt8) -> UInt8 {
    let value = Int(num) - 216
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Confirm" :*/
fileprivate let dataTopImageTitle:[Character] = ["C","o","n","f","i","r","m"]

/*: "The mute has been lifted~" :*/
fileprivate let constProgressData:[Character] = ["T","h","e"," ","m","u","t","e"," ","h","a","s"," ","b"]
fileprivate let noti_listBackMakeText:String = "een liself leading self regular select"

/*: "Confirm to kick off the seat?" :*/
fileprivate let userKitData:[Character] = ["C","o","n","f","i","r","m"," ","t","o"," ","k","i","c","k"," ","o","f","f"," ","t","h","e"," ","s","e","a"]
fileprivate let kMediumCameraName:[Character] = ["t","?"]

/*: "OK" :*/
fileprivate let appSelectedName:String = "Oequal"

/*: "Shielding Success" :*/
fileprivate let app_styleValue:String = "index searchShield"
fileprivate let user_viewData:String = "titleccess"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StopReactiveCompatible.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol ColorTableObjectProtocol: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func dataNickname(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func standIn(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func common(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func beforeCell(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class StopReactiveCompatible: UIView {
    //: var popView: TalkingPopView?
    var popView: ChangeFormThen?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: ProgressEquatable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(MediaThen.exhibitShared().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = AppSceneInfoModel()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: ColorTableObjectProtocol?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == SceneAppManager.share.loginUserMode.userID {
        if uid == SceneAppManager.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        makeSubviews()
        //: setupSubViewsConstraint()
        messageConstraint()
        //: reqUserCardInfo()
        workIndividual()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_targetName.map{nameFrame(cell: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .handleFont(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.effectColor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(iconographyClick), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .handleFont(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.collectionFor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .handleFont(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.effectColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .effectColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(show_withData) + String(constEqualName)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.handleFont(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .collectionFor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.handleFont(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .effectColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(user_labelValue)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.handleFont(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .collectionFor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.handleFont(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(mainToFullFrameValue.suffix(7))))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.auditoryImageTitle(name: (String(k_messageName.suffix(4)) + "messag" + String(main_requestValue.suffix(6)))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(allSnap), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: FrontMuteView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = FrontMuteView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension StopReactiveCompatible {
    //: func reqUserCardInfo() {
    func workIndividual() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        DataPushListener.hollerithCardUserCompletion(uid: self.uid, streamerUid: liveUserId, roomId: MediaThen.exhibitShared().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<AppSceneInfoModel>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.metadata()
            }
        }
    }

    //: func setUserCardData() {
    func metadata() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(show_labStyleTitle.suffix(5))) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.quantityerleave(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.BundleImageNamed(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.auditoryImageTitle(name: (String(constNowValue)) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            turnValue(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func turnValue(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: data_onTitle.map{colorBackground(value: $0)}, encoding: .utf8)!)
            //: if liveUserId == SceneAppManager.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == SceneAppManager.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: main_sourceName.map{$0^105}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: data_normalName.map{showPair(between: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.byIndex(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: data_onTitle.map{colorBackground(value: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.auditoryImageTitle(name: (String(kContainerContent.prefix(5)) + "voice_" + String(mainMeData))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: main_sourceName.map{$0^105}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.auditoryImageTitle(name: (String(app_pageData) + String(k_frameValue.prefix(6)) + "y_voic" + String(userEventData.prefix(5)))), for: .normal)
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.auditoryImageTitle(name: (String(const_rawData) + noti_starName.replacingOccurrences(of: "cell", with: "p"))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangMediumFont(fontSize: 15)
                btn.titleLabel?.font = UIFont.fontJump(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(minimize(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(mainToFullFrameValue.suffix(7))))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func minimize(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: data_onTitle.map{colorBackground(value: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.standIn(uid: self.userModel.uid)
            //: self.dismiss()
            self.fixing()
        //: case "Mike":
        case String(bytes: main_sourceName.map{$0^105}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.common(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.fixing()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.dataNickname(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.fixing()
        //: case "Follow":
        case String(bytes: data_normalName.map{showPair(between: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            cellFrame(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func cellFrame(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        modelText(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.beforeCell(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func modelText(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(userTextData.lowercased() + mainLabStatusData.lowercased() + "nUid")] = uid
        //: dict["source"] = "2"
        dict[(notiLinkTempValue.replacingOccurrences(of: "event", with: "so"))] = "2"
        //: LabelUpReactiveCompatible.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        LabelUpReactiveCompatible.dropEnable(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func iconographyClick() {
        //: AccumulationThen.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        AccumulationThen.share.character(uid: self.userModel.uid)
        //: self.dismiss()
        self.fixing()
    }
}

//: extension TalkingVoiceRoomCardView {
extension StopReactiveCompatible {
    //: @objc func moreBtnClick() {
    @objc func allSnap() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == SceneAppManager.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == SceneAppManager.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(user_positionName.prefix(4)) + " out").localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(dataPinName)).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(mainLiveInfoValue.prefix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(main_fatalValue) + String(data_pingValue)).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(appModelPlayerTitle)).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(show_liveTitle.suffix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.UpshotDevice.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = CreateViewDelegate.asArray(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(notiAppArrayReturnValue.prefix(7))))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func drown() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = ChangeFormThen(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage)
        //: popView?.initWithView(view: self)
        popView?.attentionPause(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? TopReactiveCompatible.getWindow())
        popView?.listView(view: self.occupier()?.view ?? TopReactiveCompatible.sovietSocialistRepublic())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func fixing() {
        //: popView?.dismissView()
        popView?.ofUp()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension StopReactiveCompatible: LegalDocumentFullMoonDocketViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func quote(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(user_positionName.prefix(4)) + " out").localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            ShowThen.atomicTotal85Awake(title: nil, message: (String(userFloorViewName.suffix(4)) + "irm " + String(const_valueName.prefix(5)) + "ck out t" + String(app_labSendToolText) + String(userIconData.prefix(6))).localized, leftBtnTitle: (String(notiEqualLabelName)).localized, rightBtnTitle: (String(constDetailExhibitText)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                ButtonProgressHUD.endDismiss()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                AttentionReqTool.blackPop(targetUid: self.userModel.uid, roomId: MediaThen.exhibitShared().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    ButtonProgressHUD.galleryMake()
                    //: self.dismiss()
                    self.fixing()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(dataPinName)).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            ShowThen.atomicTotal85Awake(title: nil, message: String(bytes: data_videoEachName.map{menuUser(since: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(notiEqualLabelName)).localized, rightBtnTitle: (String(dataTopImageTitle)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                ButtonProgressHUD.endDismiss()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                AttentionReqTool.voiceInCompletion(targetUid: self.userModel.uid, roomID: MediaThen.exhibitShared().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    ButtonProgressHUD.galleryMake()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.constraintPremium(showMsg: (String(constProgressData) + String(noti_listBackMakeText.prefix(6)) + "fted~").localized)
                    }
                    //: self.dismiss()
                    self.fixing()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(mainLiveInfoValue.prefix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.toTotalimate()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.fixing()

        //: case "Kick off".localized: /// 踢下麦
        case (String(main_fatalValue) + String(data_pingValue)).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            ShowThen.atomicTotal85Awake(title: nil, message: (String(userKitData) + String(kMediumCameraName)).localized, leftBtnTitle: (String(notiEqualLabelName)).localized, rightBtnTitle: (String(constDetailExhibitText)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                MediaThen.exhibitShared().outWalkIn(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.fixing()
            }

        //: case "Report".localized:
        case (String(show_liveTitle.suffix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = DreckThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.sinceCon(view: nil)
        //: case "Block".localized:
        case (String(appModelPlayerTitle)).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            ShowThen.atomicTotal85Awake(title: nil, message: show_errorClickBeautyStr, leftBtnTitle: (String(notiEqualLabelName)).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                ShowThen.imageLine()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                ShowThen.imageLine()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                ContentReactiveCompatible.group(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.constraintPremium(showMsg: (String(app_styleValue.suffix(6)) + "ing S" + user_viewData.replacingOccurrences(of: "title", with: "u")).localized)
                        //: self.dismiss()
                        self.fixing()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension StopReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func makeSubviews() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func messageConstraint() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(main_clickBottomContent / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-main_clickBottomContent / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(main_clickBottomContent / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.occupier()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.occupier()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.fixing()
        }
    }
}
