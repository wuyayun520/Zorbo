
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_hiddenUsText:[UInt8] = [0xc1,0xc6,0xc1,0xcc,0x80,0xbb,0xc7,0xbc,0xbd,0xca,0x92,0x81,0x78,0xc0,0xb9,0xcb,0x78,0xc6,0xc7,0xcc,0x78,0xba,0xbd,0xbd,0xc6,0x78,0xc1,0xc5,0xc8,0xc4,0xbd,0xc5,0xbd,0xc6,0xcc,0xbd,0xbc]

fileprivate func imageTitle(color num: UInt8) -> UInt8 {
    let value = Int(num) - 88
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let noti_suspendTitle:[UInt8] = [0x62,0x7e,0x73]

/*: "headPic" :*/
fileprivate let dataRecordUserByText:String = "model self regular view otherheadPi"
fileprivate let data_kitAddTitle:String = "C"

/*: "nickname" :*/
fileprivate let app_cardName:[Character] = ["n","i","c","k","n","a"]
fileprivate let show_infoName:[Character] = ["m","e"]

/*: "birthday" :*/
fileprivate let data_userTitle:[Character] = ["b","i","r","t","h","d"]
fileprivate let show_stackTitle:String = "aby"

/*: "videoUrl" :*/
fileprivate let const_managerTitle:String = "videto"
fileprivate let user_modeButtonData:[Character] = ["U","r","l"]

/*: "location" :*/
fileprivate let notiViewSelectedName:[Character] = ["l","o","c","a","t","i","o"]
fileprivate let const_textLabValue:String = "manager"

/*: "cmd" :*/
fileprivate let noti_transactionText:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let kTempValue:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let main_actionContent:String = "ALL"

/*: "data" :*/
fileprivate let appStatusTopData:[UInt8] = [0xf6,0xf3,0x6,0xf3]

fileprivate func belowValue(kind num: UInt8) -> UInt8 {
    let value = Int(num) - 146
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let k_shouldValue:String = "return medium to coverCall"
fileprivate let data_mName:String = "voicevoiceu"
fileprivate let user_viewValue:[Character] = ["p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let showPicEffectName:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let showModelContent:[UInt8] = [0x5f,0x5c,0x54,0x7a,0x57]

/*: "cancelCall" :*/
fileprivate let showSizeData:String = "cancinfo"
fileprivate let showErrorData:String = "Callarray view empty gift top"

/*: "No answer" :*/
fileprivate let dataHeadName:String = "No anopen text video total type"
fileprivate let dataRequestContent:String = "socialwer"

/*: "duration" :*/
fileprivate let kEmptyValue:String = "duracolor"
fileprivate let main_pathName:String = "upon"

/*: "She has hung up" :*/
fileprivate let mainItemValue:[Character] = ["S","h","e"," ","h","a","s"," ","h","u","n","g"," ","u"]
fileprivate let appSoundValue:String = "the"

/*: "cancelSex" :*/
fileprivate let k_succeedData:String = "viewanview"

/*: "He has hung up" :*/
fileprivate let kMakeMainName:String = "female view up false asHe has"
fileprivate let showShareName:[Character] = ["u","p"]

/*: "endType" :*/
fileprivate let userCropNameTitle:String = "endTypeall self label"

/*: "stopSex" :*/
fileprivate let userErrorName:String = "stopSexself extension top add"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let constCurrentName:[UInt8] = [0x74,0x53,0x4e,0x48,0x5b,0x5b,0x54,0x5e,0x54,0x58,0x53,0x49,0x1d,0x5a,0x52,0x51,0x59,0x1d,0x5e,0x52,0x54,0x53,0x4e,0x11,0x1d,0x5e,0x5c,0x51,0x51,0x1d,0x55,0x5c,0x4e,0x1d,0x58,0x53,0x59,0x58,0x59]

private func sliceModel(manager num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "The other hung up the phone" :*/
fileprivate let notiMoveData:String = "guard right pathThe ot"
fileprivate let show_imageData:[Character] = ["g"," ","u","p"," ","t","h"]
fileprivate let show_arrayName:String = "e phonestyle color strength"

/*: "type" :*/
fileprivate let app_verticalText:String = "userpe"

/*: "jumpUrl" :*/
fileprivate let k_actionData:[Character] = ["j","u"]
fileprivate let noti_bottomPathValue:String = "try makempUrl"

/*: "Cancel" :*/
fileprivate let show_topValue:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let mainAppText:[Character] = ["G","e","t"," ","g"]
fileprivate let mainEqualJumpName:String = "label mainold coins"

/*: "addBean" :*/
fileprivate let k_bagName:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let kFrontTingTitle:[Character] = ["r","e","f","u","s","e","S"]
fileprivate let noti_tempData:String = "eview"

/*: "onAcceptCall" :*/
fileprivate let userToTitle:String = "self safe targetonAc"
fileprivate let kOfValue:String = "ainvite"

/*: "acceptCall" :*/
fileprivate let main_handleSizeInData:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let data_nameValue:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c"]
fileprivate let mainChinData:String = "eimageed"

/*: "The call has ended" :*/
fileprivate let userClickName:String = "tag levelThe c"
fileprivate let user_keepData:[Character] = [" ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FrontViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let dataEnterMsg = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class FrontViewController: EqualViewController {
	var friendlyDictionary: [AnyHashable: String]?
	var textTitle: String?
	var adviceArray: [AnyHashable]?
	var codDictionary: [AnyHashable: String]?

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: StartChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PlaceReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: CasementWindowView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        StopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StopThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_hiddenUsText.map{imageTitle(color: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! UpNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.sublimation()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            iconSend()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.ofData()
    
	if let miniView = miniView {

            var leadingPath = Bundle.main.path(forResource: "anal_noise", ofType: "lottie")
            if var leading = leadingPath {

                
		if (!leading.isEmpty && leading.distance(from: leading.startIndex, to: leading.index(before: leading.endIndex)) == 92) && (leading.count == 17 && leading[leading.index(before: leading.endIndex)].isLowercase) {
		//: SWIFT_CUSTOM_DANGER
            leading = String(leading.drop(while: { $0.isCased }))
		}

                leadingPath = leading
            }
            if let leadingPath = leadingPath,
               self.codDictionary == nil,
               let leadingDictionary = NSDictionary(contentsOfFile: leadingPath) as? Dictionary<AnyHashable, String> {
                self.codDictionary = leadingDictionary
            }
            if let leadingText = self.codDictionary?["driverConvenience"],
               let leadingPlaceholder = self.codDictionary?["divineCede"] {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var leadingTextField = UITextField()
                leadingTextField.frame = miniView.bounds
                leadingTextField.text = leadingText
                leadingTextField.placeholder = leadingPlaceholder

                
		if (leadingTextField.contentMode == .right) && (leadingTextField.bounds.origin.y == 45.91) {
		//: SWIFT_CUSTOM_DANGER
            if leadingTextField.needsUpdateConstraints() {
                leadingTextField.setNeedsUpdateConstraints()
            }
		}

                miniView.addSubview(leadingTextField)
            }

	}

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        keyDown()
        //: setupSubViewsConstraint()
        flickConstraint()
        //: initNotificationObserver()
        effectDownObserver()
        //: req_callGetUserInfo()
        getDown()
        //: timeoutRules()
        barTagBottom()
        //: resumeTalk()
        disk()
        //: setUpBGMPlayer()
        level()
    
	if let miniView = miniView {

            var elegantPath = Bundle.main.path(forResource: "hand_ling", ofType: "lottie")
            if var elegant = elegantPath {

                
		if (elegant.count > 14 && elegant[elegant.startIndex].isNewline) && (elegant.suffix(13) == elegant.lowercased() + "intact") {
		//: SWIFT_CUSTOM_DANGER
            for v in elegant.utf16 {
                if v.bitWidth > 4 {
                    elegant = ""
                }
            }
		}

                elegantPath = elegant
            }
            if let elegantPath = elegantPath,
               self.friendlyDictionary == nil,
               let elegantDictionary = NSDictionary(contentsOfFile: elegantPath) as? Dictionary<AnyHashable, String> {
                self.friendlyDictionary = elegantDictionary
            }
            if let elegantText = self.friendlyDictionary?["rememberChain"],
               let elegantPlaceholder = self.friendlyDictionary?["editionRunning"] {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var elegantTextField = UITextField()
                elegantTextField.frame = miniView.frame.integral
                elegantTextField.text = elegantText
                elegantTextField.placeholder = elegantPlaceholder

                
		if (elegantTextField.inputAccessoryView != nil) && (elegantTextField.backgroundColor != nil && elegantTextField.backgroundColor!.cgColor == UIColor.black.cgColor) {
		//: SWIFT_CUSTOM_DANGER
            if #available(iOS 14.0, *) {
                elegantTextField.isContextMenuInteractionEnabled = elegantTextField.canBecomeFocused
            }
		}

                miniView.addSubview(elegantTextField)
            }

	}

	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SceneAppManager.share.appConfigMode.disableShootScreen {
            if SceneAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = LargeShieldView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFinding = false
        StopThen.shared.isFinding = false
        //: self.stopVCAllActivties()
        self.userIn()
    		friendlyDictionary = nil
		textTitle = nil
		adviceArray = nil
		codDictionary = nil

	}

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: ClickThen = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ClickThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: CountervalThen = {
        //: let m = TalkingVoiceChatManager()
        let m = CountervalThen()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension FrontViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func getDown() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ShouldViewReactiveCompatible.supplyLine(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: noti_suspendTitle.map{$0^23}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(dataRecordUserByText.suffix(6)) + data_kitAddTitle.lowercased())].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(app_cardName) + String(show_infoName))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(data_userTitle) + show_stackTitle.replacingOccurrences(of: "by", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(const_managerTitle.replacingOccurrences(of: "to", with: "o") + String(user_modeButtonData))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(notiViewSelectedName) + const_textLabValue.replacingOccurrences(of: "manager", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.viewInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension FrontViewController {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func iconSend() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        GenerateReactiveCompatible.funcAwake(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.exceptRakeMsg(showMsg: user_partyStr)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (String(kTempValue) + main_actionContent.lowercased()), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: noti_suspendTitle.map{$0^23}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.eventSize()
                //: return
                return
            }

            //: self.stopBGM()
            self.fromRemove()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PlaceReactiveCompatible.bottomKind(type: .Response)
            //: self.startBGM()
            self.sublimation()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func userIn() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.fromRemove()
        //: TalkingSocketManager.shared.callerDelegate = nil
        StopThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        StopThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        StopThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func eventSize(animated: Bool = true) {
        //: self.stopBGM()
        self.fromRemove()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func down() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.actionId(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func disk() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            down()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension FrontViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func effectDownObserver() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(matchResource(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(windowByNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: const_lessBarStr,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        dataEnterMsg.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func matchResource(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            eventSize()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func windowByNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard StopThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        StopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StopThen.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.roundDisconnected(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension FrontViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.exceptRakeMsg(showMsg: (String(k_shouldValue.suffix(4)) + " inte" + data_mName.replacingOccurrences(of: "voice", with: "r") + String(user_viewValue)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (String(showPicEffectName)), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (showSizeData.replacingOccurrences(of: "info", with: "el") + String(showErrorData.prefix(4))), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (String(kTempValue) + main_actionContent.lowercased()), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: noti_suspendTitle.map{$0^23}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.contentEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                StopThen.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            eventSize()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.alterBy(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension FrontViewController {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func level() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? IndexSignedTotal.Call : IndexSignedTotal.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PlaceReactiveCompatible.bottomKind(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func sublimation() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.partyPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func fromRemove() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.barnburner()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension FrontViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func barTagBottom() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? const_pathSkipStr : self.chatModel?.mixUp()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            equalUp()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.equalUp()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func equalUp() {
        //: guard !TalkingSocketManager.shared.isFinding else { return }
        guard !StopThen.shared.isFinding else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.exceptRakeMsg(showMsg: (String(dataHeadName.prefix(5)) + dataRequestContent.replacingOccurrences(of: "social", with: "s")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (showSizeData.replacingOccurrences(of: "info", with: "el") + String(showErrorData.prefix(4))), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (String(kTempValue) + main_actionContent.lowercased()), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: noti_suspendTitle.map{$0^23}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        StopThen.shared.modelTo(info: socketDict)
        //: popCurrentViewController()
        eventSize()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension FrontViewController {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func fire(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(kEmptyValue.replacingOccurrences(of: "color", with: "t") + main_pathName.replacingOccurrences(of: "up", with: "i"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.underEmbarkTime(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.viewEqual()
            //: createTalkRoom()
            down()
            //: self.stopBGM()
            self.fromRemove()
        }
    }
}

// MARK: - MakeCallerDelegate【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension FrontViewController: MakeCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func whenAlongData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(mainItemValue) + appSoundValue.replacingOccurrences(of: "the", with: "p")).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(k_succeedData.replacingOccurrences(of: "view", with: "c") + "elSex")].stringValue == WithSchemaConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kMakeMainName.suffix(6)) + " hung " + String(showShareName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptRakeMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.contentEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        eventSize()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func removeTo(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.fromRemove()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.viewEqual()
        //: self.createTalkRoom()
        self.down()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func postExecute(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(userCropNameTitle.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(mainItemValue) + appSoundValue.replacingOccurrences(of: "the", with: "p")).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(userErrorName.prefix(7)))].stringValue == WithSchemaConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(kMakeMainName.suffix(6)) + " hung " + String(showShareName)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: constCurrentName.map{sliceModel(manager: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(notiMoveData.suffix(6)) + "her hun" + String(show_imageData) + String(show_arrayName.prefix(7))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptRakeMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.contentEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.titleDismiss()
        //: self.popCurrentViewController()
        self.eventSize()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appMaxValue, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func willSharedData(msg: String, data: [String: Any]) {
        //: guard SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(app_verticalText.replacingOccurrences(of: "user", with: "ty"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(k_actionData) + String(noti_bottomPathValue.suffix(5)))] as? String else { return }
            //: let httpsUrl = SceneAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = SceneAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = MakeConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: AccumulationThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            AccumulationThen.share.gestureName(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ShowThen.atomicTotal85Awake(title: nil,
                                                //: message: msg,
                                                message: msg,
                                                //: leftBtnTitle: "Cancel".localized,
                                                leftBtnTitle: (String(show_topValue)).localized,
                                                //: rightBtnTitle: "Get gold coins".localized) {
                                                rightBtnTitle: (String(mainAppText) + String(mainEqualJumpName.suffix(9))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ShowThen.imageLine()
                //: } rightBlock: {
            } rightBlock: {
                //: AccumulationThen.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                AccumulationThen.share.requestIn(clickEvent: app_noValue, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func showPlayer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(k_bagName))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.attention(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.withPush(point: addBean)
        }
    }
}

// MARK: - HeadPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension FrontViewController: HeadPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func liveButtonSize(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(mainItemValue) + appSoundValue.replacingOccurrences(of: "the", with: "p")).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(kFrontTingTitle) + noti_tempData.replacingOccurrences(of: "view", with: "x"))].stringValue == WithSchemaConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(kMakeMainName.suffix(6)) + " hung " + String(showShareName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptRakeMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.contentEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }
        //: popCurrentViewController()
        eventSize()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func titleSave(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.fromRemove()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.viewEqual()
        //: self.createTalkRoom()
        self.down()
    }
}

// MARK: - TurnObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension FrontViewController: TurnObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func changeIn(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(kTempValue) + main_actionContent.lowercased()) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.exceptRakeMsg(showMsg: (String(dataHeadName.prefix(5)) + dataRequestContent.replacingOccurrences(of: "social", with: "s")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.contentEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                StopThen.shared.currTalkingVC = nil
            }
            //: self.popCurrentViewController()
            self.eventSize()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(userToTitle.suffix(4)) + "ceptC" + kOfValue.replacingOccurrences(of: "invite", with: "ll")) && errorNo == 101 { // 接听通话时，余额不足
            //: guard SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue else { return }
            //: AccumulationThen.share.func__jumpToWebRecharge(sufficient: false)
            AccumulationThen.share.requestIn(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(userToTitle.suffix(4)) + "ceptC" + kOfValue.replacingOccurrences(of: "invite", with: "ll")) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.exceptRakeMsg(showMsg: msg)
            //: self.popCurrentViewController()
            self.eventSize()
        }
    }
}

// MARK: - BeanObjectProtocol【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension FrontViewController: BeanObjectProtocol {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func giveAndTake() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (String(main_handleSizeInData)), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: noti_suspendTitle.map{$0^23}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func showSize() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exceptRakeMsg(showMsg: (String(data_nameValue) + mainChinData.replacingOccurrences(of: "image", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (showSizeData.replacingOccurrences(of: "info", with: "el") + String(showErrorData.prefix(4))), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exceptRakeMsg(showMsg: (String(data_nameValue) + mainChinData.replacingOccurrences(of: "image", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (String(kTempValue) + main_actionContent.lowercased()), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: noti_suspendTitle.map{$0^23}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.exceptRakeMsg(showMsg: (String(userClickName.suffix(5)) + "all has" + String(user_keepData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            StopThen.shared.modelTo(info: [String(bytes: noti_transactionText.reversed(), encoding: .utf8)!: (String(showPicEffectName)), String(bytes: appStatusTopData.map{belowValue(kind: $0)}, encoding: .utf8)!: [String(bytes: showModelContent.map{$0^51}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.titleDismiss()
        //: self.popCurrentViewController()
        self.eventSize()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func brushModifyMute(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.tableRemove(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func reciprocationKeyHandsfree(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.gameLicenseHandsfree(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func remoteRecord() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        StopThen.shared.currTalkingVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = CasementWindowView.behindSize()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.occupier()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.style()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.clipVisual(talkingTime)
        //: popCurrentViewController()
        eventSize()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func popUp(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.clipVisual(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func barTitle(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.untilColor(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension FrontViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func keyDown() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func flickConstraint() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
