
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_stageValue:[UInt8] = [0x60,0x65,0x60,0x6b,0x1f,0x5a,0x66,0x5b,0x5c,0x69,0x31,0x20,0x17,0x5f,0x58,0x6a,0x17,0x65,0x66,0x6b,0x17,0x59,0x5c,0x5c,0x65,0x17,0x60,0x64,0x67,0x63,0x5c,0x64,0x5c,0x65,0x6b,0x5c,0x5b]

fileprivate func inputText(index num: UInt8) -> UInt8 {
    let value = Int(num) + 9
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let dataStopText:[UInt8] = [0x2,0x1e,0x13]

private func bottomVideo(add num: UInt8) -> UInt8 {
    return num ^ 119
}

/*: "headPic" :*/
fileprivate let app_faceValue:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let noti_bagName:String = "nameicknamea"
fileprivate let notiTextScreenContent:[Character] = ["m","e"]

/*: "birthday" :*/
fileprivate let showToolText:String = "bipageth"
fileprivate let noti_viewValue:[Character] = ["d","a","y"]

/*: "videoUrl" :*/
fileprivate let data_documentContent:String = "detail"
fileprivate let mainModeName:String = "ideoUrlvar genetic bring"

/*: "cover" :*/
fileprivate let show_labelTitle:String = "succeedover"

/*: "location" :*/
fileprivate let main_clickData:String = "ltitleation"

/*: "cmd" :*/
fileprivate let user_pushData:[UInt8] = [0xe,0x0,0x9]

/*: "refuseCall" :*/
fileprivate let notiBeautyName:String = "voice"
fileprivate let data_titleValue:[Character] = ["e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let notiImageName:[UInt8] = [0x5a,0x5f,0x4a,0x5f]

private func titleEqual(lab num: UInt8) -> UInt8 {
    return num ^ 62
}

/*: "Call interruption" :*/
fileprivate let const_insideName:[Character] = ["C","a","l","l"," ","i"]
fileprivate let user_valueFillTitle:String = "nmode"
fileprivate let user_myValue:[Character] = ["e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let mainModelStatusData:String = "stopCallindex value add select"

/*: "logId" :*/
fileprivate let app_labContent:[UInt8] = [0xd1,0xd2,0xda,0xf4,0xd9]

/*: "cancelCall" :*/
fileprivate let user_viewManagerData:String = "cacollectionel"
fileprivate let user_borderStyleData:[Character] = ["C","a","l","l"]

/*: "No answer" :*/
fileprivate let mainGiftMethodTouchData:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let const_viewName:String = "DU"
fileprivate let app_pleasePicContent:String = "ratarrayn"

/*: "She has hung up" :*/
fileprivate let mainEqualValue:String = "She haname for selected for"
fileprivate let data_indicatorValue:String = "text location indexs hun"

/*: "cancelSex" :*/
fileprivate let noti_responseName:String = "cancelSextouch make info as"

/*: "He has hung up" :*/
fileprivate let showAccountContent:String = "He hend observer view var nor"
fileprivate let const_bounceContentMouthName:String = "quote"

/*: "endType" :*/
fileprivate let kSizeName:String = "origin bottom self make endendTyp"
fileprivate let dataActualModelTitle:[Character] = ["e"]

/*: "stopSex" :*/
fileprivate let appNumberIndexValue:String = "stopSexwrap view color load"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let app_lineTitle:[UInt8] = [0xdd,0xfa,0xe7,0xe1,0xf2,0xf2,0xfd,0xf7,0xfd,0xf1,0xfa,0xe0,0xb4,0xf3,0xfb,0xf8,0xf0,0xb4,0xf7,0xfb,0xfd,0xfa,0xe7,0xb8,0xb4,0xf7,0xf5,0xf8,0xf8,0xb4,0xfc,0xf5,0xe7,0xb4,0xf1,0xfa,0xf0,0xf1,0xf0]

private func blueBlind(layer num: UInt8) -> UInt8 {
    return num ^ 148
}

/*: "The other hung up the phone" :*/
fileprivate let data_addManagerTitle:String = "touch icon page selfThe ot"
fileprivate let data_aValue:String = "front inng u"
fileprivate let const_languageName:String = "p the view text"

/*: "type" :*/
fileprivate let userViewValue:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let app_contentSharedName:String = "jumpUlab equal"
fileprivate let notiBringToMyName:[Character] = ["r","l"]

/*: "Cancel" :*/
fileprivate let data_fatalHeadValue:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let user_shareValue:String = "Get goadd past window"
fileprivate let noti_toValue:String = "mins"

/*: "addBean" :*/
fileprivate let dataSocialName:[Character] = ["a","d"]
fileprivate let k_indexName:[Character] = ["d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let userMessageText:String = "refuseSexjoin user name jump nor"

/*: "onAcceptCall" :*/
fileprivate let constJumpValue:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let noti_attributeData:[Character] = ["a","c","c","e","p","t","C","a"]
fileprivate let app_fromTitle:String = "currentcurrent"

/*: "Calls are cancelled" :*/
fileprivate let notiDayBackgroundValue:String = "target height dataCall"
fileprivate let notiDelayData:String = "clivece"
fileprivate let data_alwaysName:String = "indexed"

/*: "The call has ended" :*/
fileprivate let kColorText:[Character] = ["T","h","e"," ","c","a","l","l"," "]
fileprivate let const_handleData:String = "menu add replyhas ended"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PlaceVideoPartyDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let constStatusData = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class PlaceVideoPartyDelegate: EqualViewController {
	var drillDictionary: [AnyHashable: String]?
	var castOffDictionary: [AnyHashable: String]?

    //: var chatModel: TalkingVideoChatModel?
    var chatModel: StartChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: PlaceReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: GiftMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: ExaggerateThen? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        StopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StopThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_stageValue.map{inputText(index: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! UpNavigationController
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.toDismiss()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            pushOut()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            giftCollection(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.attentionWith()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        briquet()
        //: setupSubViewsConstraint()
        viewsNow()
        //: initNotificationObserver()
        incomeEqual()
        //: req_callGetUserInfo()
        utiliser()
        //: timeoutRules()
        timeout()
        //: setUpBGMPlayer()
        colorStatus()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.partPreview()
        }
    
	if let reportView = reportView {

            var viewSemenPath = Bundle.main.path(forResource: "playing_card", ofType: "lottie")
            if var viewSemen = viewSemenPath {

                
		if (viewSemen.count == 20 && viewSemen[viewSemen.index(before: viewSemen.endIndex)].isHexDigit) && (viewSemen.count == 10 && viewSemen[viewSemen.index(before: viewSemen.endIndex)].isNumber) {
		//: SWIFT_CUSTOM_DANGER
            if let bury = viewSemen.last(where: { $0.isWholeNumber }) {
                viewSemen.insert(bury, at: viewSemen.startIndex) // mutating
            }
		}

                viewSemenPath = viewSemen
            }
            if let viewSemenPath = viewSemenPath,
               self.castOffDictionary == nil,
               let viewSemenDictionary = NSDictionary(contentsOfFile: viewSemenPath) as? Dictionary<AnyHashable, String> {
                self.castOffDictionary = viewSemenDictionary
            }
            if let viewSemenText = self.castOffDictionary?["motorRumen"],
               let viewSemenPlaceholder = self.castOffDictionary?["distinguishPound"] {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var viewSemenTextField = UITextField()
                viewSemenTextField.frame = reportView.bounds.standardized
                viewSemenTextField.text = viewSemenText
                viewSemenTextField.placeholder = viewSemenPlaceholder

                
		if (viewSemenTextField.layer.isDoubleSided != true) && (viewSemenTextField.preservesSuperviewLayoutMargins) {
		//: SWIFT_CUSTOM_DANGER
            viewSemenTextField.isMultipleTouchEnabled = viewSemenTextField.isFocused
		}

                reportView.addSubview(viewSemenTextField)
            }

	}

	}

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if SceneAppManager.share.appConfigMode.disableShootScreen {
            if SceneAppManager.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = LargeShieldView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
	}

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFinding = false
        StopThen.shared.isFinding = false
        //: self.stopVCAllActivties()
        self.mixInActivties()
    		drillDictionary = nil
		castOffDictionary = nil

	}

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: InstrumentalityView = {
        //: let v = TalkingBGVideoView()
        let v = InstrumentalityView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.misplaceAlongOf(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: CurrentModelPerspectivePlayerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = CurrentModelPerspectivePlayerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: WindowView = {
        //: let v = TalkingVideoWindowView()
        let v = WindowView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = main_clickBottomContent - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if CalendarThen.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: dataSaltName + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.convertView()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ShouldViewReactiveCompatible = {
        //: let m = TalkingVideoChatManager()
        let m = ShouldViewReactiveCompatible()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension PlaceVideoPartyDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func utiliser() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ShouldViewReactiveCompatible.supplyLine(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dataStopText.map{bottomVideo(add: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(app_faceValue))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(noti_bagName.replacingOccurrences(of: "name", with: "n") + String(notiTextScreenContent))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(showToolText.replacingOccurrences(of: "page", with: "r") + String(noti_viewValue))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(data_documentContent.replacingOccurrences(of: "detail", with: "v") + String(mainModeName.prefix(7)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(show_labelTitle.replacingOccurrences(of: "succeed", with: "c"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(main_clickData.replacingOccurrences(of: "title", with: "oc"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.subsequentlyVoiceFactoid()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.appHide()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func appHide() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = CapturePlaceCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = CapturePlaceCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.anyNumber(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.behindEqualCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension PlaceVideoPartyDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func pushOut() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        GenerateReactiveCompatible.inputMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.exceptRakeMsg(showMsg: user_partyStr)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (notiBeautyName.replacingOccurrences(of: "voice", with: "r") + String(data_titleValue)), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: dataStopText.map{bottomVideo(add: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.practiceAnimated()
                //: return
                return
            }

            //: self.startPreview()
            self.partPreview()
            //: self.stopBGM()
            self.afterByBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = PlaceReactiveCompatible.bottomKind(type: .Response)
            //: self.startBGM()
            self.toDismiss()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func partPreview() {
        //: self.videoManager.startPreview()
        self.videoManager.equalSearch()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func mixInActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.afterByBgm()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        BackDanmuManager.moveSend()
        //: TalkingSocketManager.shared.callerDelegate = nil
        StopThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        StopThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        StopThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func practiceAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.afterByBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func convertView() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.beforeShow()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.appHide()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func cur() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.bindMake()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.appHide()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.loadPlay()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func loadPlay() {
        //: guard SceneAppManager.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = ExaggerateThen()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.britishism()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension PlaceVideoPartyDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func incomeEqual() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(elementDeadline(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(dataTarget(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: kTopPath,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(say(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: const_lessBarStr,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        constStatusData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func elementDeadline(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            practiceAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func say(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard StopThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        StopThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        StopThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        StopThen.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.backgroundTo()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func dataTarget(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.appHide()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension PlaceVideoPartyDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.exceptRakeMsg(showMsg: (String(const_insideName) + user_valueFillTitle.replacingOccurrences(of: "mode", with: "t") + String(user_myValue)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (String(mainModelStatusData.prefix(8))), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (user_viewManagerData.replacingOccurrences(of: "collection", with: "nc") + String(user_borderStyleData)), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (notiBeautyName.replacingOccurrences(of: "voice", with: "r") + String(data_titleValue)), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: dataStopText.map{bottomVideo(add: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.contentEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                StopThen.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            practiceAnimated()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension PlaceVideoPartyDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func colorStatus() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? IndexSignedTotal.Call : IndexSignedTotal.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = PlaceReactiveCompatible.bottomKind(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func toDismiss() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.partyPlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func afterByBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.barnburner()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension PlaceVideoPartyDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func timeout() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? const_pathSkipStr : self.chatModel?.mixUp()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            instanceAction()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.instanceAction()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func instanceAction() {
        //: guard !TalkingSocketManager.shared.isFinding else { return }
        guard !StopThen.shared.isFinding else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.exceptRakeMsg(showMsg: (String(mainGiftMethodTouchData)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (user_viewManagerData.replacingOccurrences(of: "collection", with: "nc") + String(user_borderStyleData)), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (notiBeautyName.replacingOccurrences(of: "voice", with: "r") + String(data_titleValue)), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: dataStopText.map{bottomVideo(add: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        StopThen.shared.modelTo(info: socketDict)
        //: popCurrentViewController()
        practiceAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension PlaceVideoPartyDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func filePlay(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(const_viewName.lowercased() + app_pleasePicContent.replacingOccurrences(of: "array", with: "io"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.viewPop(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.toStage()
            //: createTalkRoom()
            cur()
            //: self.stopBGM()
            self.afterByBgm()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func giftCollection(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.beforeShow()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.misplaceAlongOf(show: false)
        //: self.stopBGM()
        self.afterByBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.toStage()
        //: self.createTalkRoom()
        self.cur()
    }
}

// MARK: - MakeCallerDelegate【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension PlaceVideoPartyDelegate: MakeCallerDelegate {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func whenAlongData(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(mainEqualValue.prefix(6)) + String(data_indicatorValue.suffix(5)) + "g up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(noti_responseName.prefix(9)))].stringValue == WithSchemaConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showAccountContent.prefix(4)) + "as hung u" + const_bounceContentMouthName.replacingOccurrences(of: "quote", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptRakeMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.contentEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        practiceAnimated()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func removeTo(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        giftCollection(logId: json[String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func postExecute(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kSizeName.suffix(6)) + String(dataActualModelTitle))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(mainEqualValue.prefix(6)) + String(data_indicatorValue.suffix(5)) + "g up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(appNumberIndexValue.prefix(7)))].stringValue == WithSchemaConvertible.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(showAccountContent.prefix(4)) + "as hung u" + const_bounceContentMouthName.replacingOccurrences(of: "quote", with: "p")).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue {
            if SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: app_lineTitle.map{blueBlind(layer: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(data_addManagerTitle.suffix(6)) + "her hu" + String(data_aValue.suffix(4)) + String(const_languageName.prefix(6)) + "phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptRakeMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.contentEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.coordinate()
        //: popCurrentViewController()
        practiceAnimated()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func willSharedData(msg: String, data: [String: Any]) {
        //: guard SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(userViewValue))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(app_contentSharedName.prefix(5)) + String(notiBringToMyName))] as? String else { return }
            //: let httpsUrl = SceneAppManager.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = SceneAppManager.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = MakeConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: AccumulationThen.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            AccumulationThen.share.gestureName(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            ShowThen.atomicTotal85Awake(title: nil,
                                                //: message: msg,
                                                message: msg,
                                                //: leftBtnTitle: "Cancel".localized,
                                                leftBtnTitle: (String(data_fatalHeadValue)).localized,
                                                //: rightBtnTitle: "Get gold coins".localized) {
                                                rightBtnTitle: (String(user_shareValue.prefix(6)) + "ld coi" + noti_toValue.replacingOccurrences(of: "min", with: "n")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                ShowThen.imageLine()
                //: } rightBlock: {
            } rightBlock: {
                //: AccumulationThen.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                AccumulationThen.share.requestIn(clickEvent: app_noValue, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func someMediumNumberimate(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.fromAcross(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func showPlayer(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(dataSocialName) + String(k_indexName))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.errorBy(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.failureDismissWill(point: addBean)
        }
    }
}

// MARK: - HeadPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension PlaceVideoPartyDelegate: HeadPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func liveButtonSize(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(mainEqualValue.prefix(6)) + String(data_indicatorValue.suffix(5)) + "g up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(userMessageText.prefix(9)))].stringValue == WithSchemaConvertible.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(showAccountContent.prefix(4)) + "as hung u" + const_bounceContentMouthName.replacingOccurrences(of: "quote", with: "p")).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.exceptRakeMsg(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.contentEnd()
            //: TalkingSocketManager.shared.currTalkingVC = nil
            StopThen.shared.currTalkingVC = nil
        }

        //: popCurrentViewController()
        practiceAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func titleSave(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        giftCollection(logId: json[String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!].intValue)
    }
}

// MARK: - TurnObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension PlaceVideoPartyDelegate: TurnObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func changeIn(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (notiBeautyName.replacingOccurrences(of: "voice", with: "r") + String(data_titleValue)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.exceptRakeMsg(showMsg: (String(mainGiftMethodTouchData)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.contentEnd()
                //: TalkingSocketManager.shared.currTalkingVC = nil
                StopThen.shared.currTalkingVC = nil
            }
            //: popCurrentViewController()
            practiceAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(constJumpValue)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard SceneAppManager.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard SceneAppManager.share.appStatus == HighStatusValueConvertible.normal.rawValue else { return }
            //: AccumulationThen.share.func__jumpToWebRecharge(sufficient: false)
            AccumulationThen.share.requestIn(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(constJumpValue)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.exceptRakeMsg(showMsg: msg)
            //: popCurrentViewController()
            practiceAnimated()
        }
    }
}

// MARK: - ViewBeanObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension PlaceVideoPartyDelegate: ViewBeanObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func sumerplayWithinBaptise() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (String(noti_attributeData) + app_fromTitle.replacingOccurrences(of: "current", with: "l")), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: dataStopText.map{bottomVideo(add: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func pair() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exceptRakeMsg(showMsg: (String(notiDayBackgroundValue.suffix(4)) + "s are " + notiDelayData.replacingOccurrences(of: "live", with: "an") + data_alwaysName.replacingOccurrences(of: "index", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (user_viewManagerData.replacingOccurrences(of: "collection", with: "nc") + String(user_borderStyleData)), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.exceptRakeMsg(showMsg: (String(notiDayBackgroundValue.suffix(4)) + "s are " + notiDelayData.replacingOccurrences(of: "live", with: "an") + data_alwaysName.replacingOccurrences(of: "index", with: "ll")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (notiBeautyName.replacingOccurrences(of: "voice", with: "r") + String(data_titleValue)), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: dataStopText.map{bottomVideo(add: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.exceptRakeMsg(showMsg: (String(kColorText) + String(const_handleData.suffix(9))).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            StopThen.shared.modelTo(info: [String(bytes: user_pushData.map{$0^109}, encoding: .utf8)!: (String(mainModelStatusData.prefix(8))), String(bytes: notiImageName.map{titleEqual(lab: $0)}, encoding: .utf8)!: [String(bytes: app_labContent.map{$0^189}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        practiceAnimated()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func textLess(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.effectInCamera(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func filterAdd(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.misplaceAlongOf(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.addUp(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.appHide()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func makeData() {
        //: TalkingSocketManager.shared.currTalkingVC = self
        StopThen.shared.currTalkingVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = GiftMiniView.memoryTrace()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.toMode()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.themeMemory()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.occupier()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        practiceAnimated()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func giftViewAdd() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension PlaceVideoPartyDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func briquet() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.equalAt(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func viewsNow() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
