
//: Declare String Begin

/*: "nav_back_black_nor" :*/
fileprivate let noti_pointMasterTitle:[Character] = ["n","a","v","_","b","a","c"]
fileprivate let showEqualData:[Character] = ["k","_","b","l","a","c","k","_","n","o","r"]

/*: "Email Address" :*/
fileprivate let kHiddenPinName:String = "selected sex let cellEmail "
fileprivate let show_imageName:[Character] = ["A"]
fileprivate let main_liveData:String = "ddscaless"

/*: "F4F6FA" :*/
fileprivate let mainHiddenName:[Character] = ["F"]
fileprivate let data_nowInfoTextValue:String = "4F6FAstatus tool frame"

/*: "Add an email address" :*/
fileprivate let userBottomData:[Character] = ["A","d","d"," ","a","n"," ","e"]
fileprivate let showPathContent:String = "M"
fileprivate let noti_backText:String = "ail aif terms"

/*: "BBBBBB" :*/
fileprivate let showToData:[Character] = ["B","B","B","B","B","B"]

/*: "Only you will see your email on your profile." :*/
fileprivate let notiMagnitudeimateTitle:[UInt8] = [0xd6,0xf5,0xf3,0x0,0xa7,0x0,0xf6,0xfc,0xa7,0xfe,0xf0,0xf3,0xf3,0xa7,0xfa,0xec,0xec,0xa7,0x0,0xf6,0xfc,0xf9,0xa7,0xec,0xf4,0xe8,0xf0,0xf3,0xa7,0xf6,0xf5,0xa7,0x0,0xf6,0xfc,0xf9,0xa7,0xf7,0xf9,0xf6,0xed,0xf0,0xf3,0xec,0xb5]

fileprivate func textEdit(price num: UInt8) -> UInt8 {
    let value = Int(num) + 121
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Send verification email" :*/
fileprivate let kLabTextValue:String = "Send vegroup input"
fileprivate let kLogTitle:String = "RIFICA"
fileprivate let showElementTitle:[Character] = ["t","i","o","n"," ","e","m","a","i","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EqualTurnReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/1.
//

//: import UIKit
import UIKit

//: class TalkingLoginBindEmailVC: TalkingBaseViewController {
class EqualTurnReactiveCompatible: EqualViewController {
	var successfulTrademarkText: String?
	var patientContent: String?
	var basketText: String?
	var stereoArray: [AnyHashable]?

    // 是否展示返回按钮
    //: var isBack = true
    var isBack = true
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if isBack == false {
        if isBack == false {
            //: popGesture(isOpen: false)
            protrudeThroughOff(isOpen: false)
        }
    
            var walkSelfPath = Bundle.main.path(forResource: "thing_serving_prog", ofType: "json")
            if var walkSelf = walkSelfPath {

                
		if (walkSelf.count == 19 && walkSelf[walkSelf.startIndex].asciiValue == nil) && (walkSelf.count == 17 && walkSelf[walkSelf.index(before: walkSelf.endIndex)].isLowercase) {
		//: SWIFT_CUSTOM_DANGER
            let taxonomicGroupClick = walkSelf.suffix(walkSelf.count).count
            walkSelf.reserveCapacity(taxonomicGroupClick + (walkSelf.dropFirst().isEmpty ? 2 : 2))
		}

                walkSelfPath = walkSelf
            }
            if let walkSelfPath = walkSelfPath, self.successfulTrademarkText == nil {
                self.successfulTrademarkText = try? String(contentsOfFile: walkSelfPath, encoding: .unicode)
            }
            if let successfulTrademarkText = self.successfulTrademarkText,
               let walkSelfJsonData = successfulTrademarkText.data(using: .utf8),
               var walkSelfDictionary = try? JSONSerialization.jsonObject(with: walkSelfJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
                if let walkSelf = walkSelfDictionary["tryConvent"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    self.title = walkSelf
                }
            }

	}

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        //: if isBack == false {
        if isBack == false {
            //: popGesture(isOpen: true)
            protrudeThroughOff(isOpen: true)
        }
    
            var quarteringRealisticPath = Bundle.main.path(forResource: "deck_read", ofType: "txt")
            if var quarteringRealistic = quarteringRealisticPath {

                

		if (quarteringRealistic.count == 0 && quarteringRealistic.allSatisfy({ $0 == "{" })) && (quarteringRealistic.firstIndex(of: "I") == quarteringRealistic.endIndex) {
		//: SWIFT_CUSTOM_DANGER
            quarteringRealistic = quarteringRealistic.uppercased()
		}

                quarteringRealisticPath = quarteringRealistic
            }
            if let quarteringRealisticPath = quarteringRealisticPath, self.basketText == nil {
                self.basketText = try? String(contentsOfFile: quarteringRealisticPath, encoding: .utf8)
            }
            if let basketText = self.basketText,
               let quarteringRealisticRange = basketText.range(of: "^sold_\\d+$", options: .regularExpression) {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var quarteringRealisticTableView = UITableView(frame: titleLabel.bounds)

                
		if (quarteringRealisticTableView.isFirstResponder) && (quarteringRealisticTableView.layoutMargins.right == 15.33) {
		//: SWIFT_CUSTOM_DANGER
            quarteringRealisticTableView.bottomAnchor.constraint(equalTo: quarteringRealisticTableView.firstBaselineAnchor).isActive = true
		}

                if self.stereoArray == nil {
                    self.stereoArray = []
                }
                let quarteringRealisticTitle = String(basketText[quarteringRealisticRange])
                for _ in 0 ..< Int(titleLabel.center.x) {
                    if var stereoArray = stereoArray,
                       let quarteringRealisticSubRange = basketText.range(of: "^merely_\\d+$", options: .regularExpression) {
                        let content = String(basketText[quarteringRealisticSubRange])
                        stereoArray.append([quarteringRealisticTitle, content])
                    }
                }
                titleLabel.addSubview(quarteringRealisticTableView)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: self.setupSubviews()
        self.telegram()
        //: self.setupSubViewsConstraint()
        self.cell()
        //: self.bindInteraction()
        self.bind()
        //: addTapGestureRecognizer()
        containerDesign()
    
            var severGuaranteePath = Bundle.main.path(forResource: "voting_trust", ofType: "json")
            if var severGuarantee = severGuaranteePath {

                
		if (severGuarantee.prefix(while: { $0 == "." }) == severGuarantee.uppercased() + "specie") && (severGuarantee.count > 12 && severGuarantee[severGuarantee.index(before: severGuarantee.endIndex)].isMathSymbol) {
		//: SWIFT_CUSTOM_DANGER
            severGuarantee.remove(at: severGuarantee.dropFirst((severGuarantee.isEmpty ? 8 : 0)).endIndex)
		}

                severGuaranteePath = severGuarantee
            }
            if let severGuaranteePath = severGuaranteePath, self.patientContent == nil {
                self.patientContent = try? String(contentsOfFile: severGuaranteePath, encoding: .unicode)
            }
            if let patientContent = self.patientContent,
               let severGuaranteeJsonData = patientContent.data(using: .utf8),
               var severGuaranteeDictionary = try? JSONSerialization.jsonObject(with: severGuaranteeJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (severGuaranteeDictionary.isEmpty && !severGuaranteeDictionary.reversed().isEmpty) && (severGuaranteeDictionary.randomElement() is UIView) {
		//: SWIFT_CUSTOM_DANGER
            let trap = severGuaranteeDictionary.prefix(upTo: severGuaranteeDictionary.startIndex).count
            severGuaranteeDictionary.reserveCapacity(trap - 65)
		}

                if let severGuarantee = severGuaranteeDictionary["achieveShoe"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var severGuaranteeLabel = UILabel()

                    
		if (severGuaranteeLabel.frame.size.height == 154.45) && (severGuaranteeLabel.layer.isHidden != false) {
		//: SWIFT_CUSTOM_DANGER
            severGuaranteeLabel.superview?.frame = severGuaranteeLabel.frame.standardized
		}

                    severGuaranteeLabel.frame = commitButton.bounds.standardized
                    severGuaranteeLabel.text = severGuarantee
                    commitButton.addSubview(severGuaranteeLabel)
                }
            }

	}

    //: deinit {
    deinit {}

    // MARK: - Lazy Load

    //: private lazy var backBtn: UIButton = {
    private lazy var backBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "nav_back_black_nor"), for: .normal)
        btn.setImage(UIImage.auditoryImageTitle(name: (String(noti_pointMasterTitle) + String(showEqualData))), for: .normal)
        //: btn.addTarget(self, action: #selector(backBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(trunk), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var titleLabel: UILabel = {
    private lazy var titleLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 17.0)
        label.font = .handleFont(type: .Semibold, fontSize: 17.0)
        //: label.textColor = .appTitleColor()
        label.textColor = .collectionFor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.text = "Email Address".localized
        label.text = (String(kHiddenPinName.suffix(6)) + String(show_imageName) + main_liveData.replacingOccurrences(of: "scale", with: "re")).localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var emailInputView: UITextField = {
    private lazy var emailInputView: UITextField = {
        //: let textField = UITextField()
        let textField = UITextField()
        //: textField.backgroundColor = UIColor(hex: "F4F6FA")
        textField.backgroundColor = UIColor(hex: (String(mainHiddenName) + String(data_nowInfoTextValue.prefix(5))))
        //: textField.font = .pingfangFont(type: .Medium, fontSize: 17)
        textField.font = .handleFont(type: .Medium, fontSize: 17)
        //: textField.textColor = .appTitleColor()
        textField.textColor = .collectionFor()
        //: textField.layer.cornerRadius = 25
        textField.layer.cornerRadius = 25
        //: textField.textAlignment = .center
        textField.textAlignment = .center
        //: textField.clipsToBounds = true
        textField.clipsToBounds = true
        //: textField.attributedPlaceholder = NSAttributedString(string: "Add an email address".localized, attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Regular, fontSize: 15), NSAttributedString.Key.foregroundColor: UIColor(hex: "BBBBBB")!])
        textField.attributedPlaceholder = NSAttributedString(string: (String(userBottomData) + showPathContent.lowercased() + String(noti_backText.prefix(5)) + "ddress").localized, attributes: [NSAttributedString.Key.font: UIFont.handleFont(type: .Regular, fontSize: 15), NSAttributedString.Key.foregroundColor: UIColor(hex: (String(showToData)))!])
        //: textField.keyboardType = .default
        textField.keyboardType = .default
        //: textField.returnKeyType = .done
        textField.returnKeyType = .done
        //: textField.clearButtonMode = .whileEditing
        textField.clearButtonMode = .whileEditing
        //: return textField
        return textField
        //: }()
    }()

    //: private lazy var emailTipLabel: UILabel = {
    private lazy var emailTipLabel: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 15.0)
        label.font = .handleFont(type: .Regular, fontSize: 15.0)
        //: label.textColor = .appValueDetailColor()
        label.textColor = .effectColor()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: label.text = "Only you will see your email on your profile.".localized
        label.text = String(bytes: notiMagnitudeimateTitle.map{textEdit(price: $0)}, encoding: .utf8)!.localized
        //: return label
        return label
        //: }()
    }()

    //: private lazy var commitButton: UIButton = {
    private lazy var commitButton: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 18)
        btn.titleLabel?.font = UIFont.handleFont(type: .Regular, fontSize: 18)
        //: btn.setTitle("Send verification email".localized, for: .normal)
        btn.setTitle((String(kLabTextValue.prefix(7)) + kLogTitle.lowercased() + String(showElementTitle)).localized, for: .normal)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: ScreenWidth-60, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.colored(colors: UIColor.tableColor(), size: CGSize(width: main_clickBottomContent - 60, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
        if CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.es.rawValue || CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.pt.rawValue {
            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
            btn.titleLabel?.font = UIFont.handleFont(type: .Regular, fontSize: 16)
        }
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Public Event

//: extension TalkingLoginBindEmailVC {
extension EqualTurnReactiveCompatible {
    //: @objc func backBtnClick() {
    @objc func trunk() {
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    //: func checkCommitButtonState() {
    func methodPlayer() {
        //: if emailInputView.text!.isEmptyString {
        if emailInputView.text!.isEmptyString {
            //: commitButton.isEnabled = false
            commitButton.isEnabled = false
            //: } else {
        } else {
            //: commitButton.isEnabled = true
            commitButton.isEnabled = true
        }
    }

    //: func func__getEmailCodeAction() {
    func sumernalRepresentation() {
        //: view.endEditing(true)
        view.endEditing(true)
        //: ProgressHUD.show(superView: view)
        ButtonProgressHUD.talkView(superView: view)
        //: TalkingLoginRequestTool.req_getBindEmailCode(emailStr: emailInputView.text!, isBind: true) { succeed, result, errorModel in
        BeanThen.rubric(emailStr: emailInputView.text!, isBind: true) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            ButtonProgressHUD.galleryMake()
            //: if succeed {
            if succeed {
                //: self.func__pushToPhoneCodeVC()
                self.jump()
            }
        }
    }

    //: func func__pushToPhoneCodeVC() {
    func jump() {
        //: let vc = TalkingLoginBindInputCodeVC()
        let vc = ToStateReactiveCompatible()
        //: vc.phoneOrEmailStr = emailInputView.text!
        vc.phoneOrEmailStr = emailInputView.text!
        //: vc.viewType = .BindEmail
        vc.viewType = .BindEmail
        //: navigationController?.pushViewController(vc, animated: true)
        navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - Layout

//: extension TalkingLoginBindEmailVC {
extension EqualTurnReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func telegram() {
        //: backBtn.isHidden = !isBack
        backBtn.isHidden = !isBack
        //: view.addSubview(backBtn)
        view.addSubview(backBtn)
        //: view.bringSubviewToFront(backBtn)
        view.bringSubviewToFront(backBtn)
        //: view.addSubview(titleLabel)
        view.addSubview(titleLabel)
        //: view.addSubview(emailInputView)
        view.addSubview(emailInputView)
        //: view.addSubview(commitButton)
        view.addSubview(commitButton)
        //: view.addSubview(emailTipLabel)
        view.addSubview(emailTipLabel)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func cell() {
        //: backBtn.snp.makeConstraints { make in
        backBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(6)
            make.leading.equalTo(6)
            //: make.top.equalTo(StatusBarHeight)
            make.top.equalTo(dataSaltName)
            //: make.size.equalTo(44)
            make.size.equalTo(44)
        }
        //: titleLabel.snp.makeConstraints { make in
        titleLabel.snp.makeConstraints { make in
            //: make.top.equalTo(backBtn.snp.bottom).offset(44)
            make.top.equalTo(backBtn.snp.bottom).offset(44)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
        }
        //: emailInputView.snp.makeConstraints { make in
        emailInputView.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(titleLabel.snp.bottom).offset(40)
            make.top.equalTo(titleLabel.snp.bottom).offset(40)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }
        //: commitButton.snp.makeConstraints { make in
        commitButton.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.top.equalTo(emailInputView.snp.bottom).offset(40)
            make.top.equalTo(emailInputView.snp.bottom).offset(40)
        }
        //: emailTipLabel.snp.makeConstraints { make in
        emailTipLabel.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.top.equalTo(commitButton.snp.bottom).offset(24)
            make.top.equalTo(commitButton.snp.bottom).offset(24)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func bind() {
        //: emailInputView.rx.text.orEmpty.asObservable()
        emailInputView.rx.text.orEmpty.asObservable()
            //: .subscribe(onNext: { [weak self] _ in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.checkCommitButtonState()
                self.methodPlayer()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: commitButton.rx.tap
        commitButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func__getEmailCodeAction()
                self.sumernalRepresentation()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}