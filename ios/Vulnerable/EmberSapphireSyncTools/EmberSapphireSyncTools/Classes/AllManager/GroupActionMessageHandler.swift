
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_colorName:[UInt8] = [0x34,0x33,0x34,0x29,0x75,0x3e,0x32,0x39,0x38,0x2f,0x67,0x74,0x7d,0x35,0x3c,0x2e,0x7d,0x33,0x32,0x29,0x7d,0x3f,0x38,0x38,0x33,0x7d,0x34,0x30,0x2d,0x31,0x38,0x30,0x38,0x33,0x29,0x38,0x39]

/*: "Load..." :*/
fileprivate let constAddPriceName:String = "return modelLoad..."

/*: "Skip" :*/
fileprivate let show_tempData:String = "session gift let message guardSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let showUserData:[UInt8] = [0x29,0x27,0x65,0x67,0x72,0x61,0x68,0x63,0x65,0x72,0x27,0x28,0x73,0x4a,0x6f,0x54,0x65,0x76,0x69,0x74,0x61,0x4e,0x2e,0x6c,0x6f,0x6f,0x54,0x70,0x74,0x74,0x48]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let userLabTargetValue:[UInt8] = [0xc5,0xdb,0xdc,0xd6,0xdd,0xc5,0x9c,0xdd,0xdc,0xe2,0xd3,0xd5,0xd7,0xe1,0xda,0xdd,0xc5,0x94,0x94,0xdd,0xdc,0xe2,0xd3,0xd5,0xd7,0xe1,0xda,0xdd,0xc5,0x9a,0x9b,0x89]

private func lockTop(path num: UInt8) -> UInt8 {
    return num ^ 178
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let user_videoData:String = "JS"
fileprivate let showTitleNumberValue:String = "make icon viewEven"
fileprivate let main_centerData:String = "ageShopro info to"

/*: --- :*/
fileprivate let mainLineResultData:[Character] = ["-","-","-"]

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let show_contentPicText:[UInt8] = [0xc3,0xdd,0xda,0xd0,0xdb,0xc3,0x9a,0xdb,0xda,0xe4,0xd5,0xd3,0xd1,0xfc,0xdd,0xd0,0xd1,0x92,0x92,0xdb,0xda,0xe4,0xd5,0xd3,0xd1,0xfc,0xdd,0xd0,0xd1,0x9c,0x9d,0x8f]

private func uttermostTo(user num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let main_modelText:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g"]
fileprivate let mainGiftData:[Character] = ["e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let app_rowContent:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let notiTimeName:[Character] = ["c","a","n","G","o","B"]
fileprivate let kModelValue:String = "acmake"

/*: "PHPSESSID= :*/
fileprivate let main_startData:String = "Proot"
fileprivate let k_giftText:String = "PSeffectS"

/*: ";uid= :*/
fileprivate let userKindText:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let notiTitlePageContent:String = "view loadCookie"

/*: "nav_back_black_nor" :*/
fileprivate let kDoingData:String = "nav_bawith section"
fileprivate let kViewTitle:String = "ack_nm select import"
fileprivate let userCancelBarName:[Character] = ["o","r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let showGiftValue:[Character] = ["i","c","o","_","i","n","t","i","m","a","t","e","_","l","i","s"]
fileprivate let appViewTitle:[Character] = ["t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let app_timeTitle:[UInt8] = [0x82,0x89,0x85,0x93,0x8b,0x83,0x88,0x92,0xc8,0x81,0x83,0x92,0xa3,0x8a,0x83,0x8b,0x83,0x88,0x92,0x95,0xa4,0x9f,0xb2,0x87,0x81,0xa8,0x87,0x8b,0x83,0xce,0xc1,0x84,0x89,0x82,0x9f,0xc1,0xcf,0xbd,0xd6,0xbb,0xc8,0x95,0x92,0x9f,0x8a,0x83,0xc8,0x84,0x87,0x85,0x8d,0x81,0x94,0x89,0x93,0x88,0x82,0xdb,0xc1,0x94,0x81,0x84,0x87,0xce,0xd6,0xca,0xd6,0xca,0xd6,0xca,0xd6,0xcf,0xc1]

private func tableData(list num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "Alert" :*/
fileprivate let main_constraintText:String = "Alertframe self view"

/*: "OK" :*/
fileprivate let kAddTitle:String = "conversion"

/*: "Cancle" :*/
fileprivate let dataBorderTempName:String = "Canclein equal priority"

/*: "Done" :*/
fileprivate let k_sizeContent:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let notiRangeName:[Character] = ["j","s"," ","c","a","l","l"," ","m","e","t","h","o","d"," ","n","a","m","e"," ","="]
fileprivate let dataShareName:[Character] = [" "]

/*: , message =  :*/
fileprivate let app_someoneName:String = "string lab of, mes"

/*: "http://" :*/
fileprivate let app_lineValue:String = "http://let manager"

/*: "https://" :*/
fileprivate let mainInsideName:String = "content resulthttps://"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let main_nameValue:[UInt8] = [0xe8,0xd3,0xe4,0x92,0xd5,0xe1,0xe1,0xdd,0xdb,0xd7,0xc0,0xd3,0xdf,0xd7,0xe5,0x92,0xaf,0x92,0xd6,0xe1,0xd5,0xe7,0xdf,0xd7,0xe0,0xe6,0xa0,0xd5,0xe1,0xe1,0xdd,0xdb,0xd7,0xa0,0xe5,0xe2,0xde,0xdb,0xe6,0x9a,0x99,0xad,0x92,0x99,0x9b,0xa0,0xdf,0xd3,0xe2,0x9a,0xd8,0xe7,0xe0,0xd5,0xe6,0xdb,0xe1,0xe0,0x9a,0xd5,0xe1,0xe1,0xdd,0xdb,0xd7,0x9b,0x92,0xed,0x92,0xe4,0xd7,0xe6,0xe7,0xe4,0xe0,0x92,0xd5,0xe1,0xe1,0xdd,0xdb,0xd7,0xa0,0xe5,0xe2,0xde,0xdb,0xe6,0x9a,0x99,0xaf,0x99,0x9b,0xcd,0xa2,0xcf,0x92,0xef,0x92,0x9b,0xad,0x7c]

fileprivate func sizeUp(message num: UInt8) -> UInt8 {
    let value = Int(num) - 114
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid= :*/
fileprivate let constMakeTitle:String = "uid=intimate make length end"

/*: ;domain= :*/
fileprivate let k_keyData:String = "count component view super line;do"
fileprivate let main_contentName:String = "at view sub super typemain="

/*: ;path=/" :*/
fileprivate let notiMessageValue:String = ";path=/pan comment"

/*: "document.cookie=' :*/
fileprivate let dataEqualValue:String = "DOCUM"
fileprivate let k_endTitle:String = "for toolokie="
fileprivate let appSuccessfullyData:[Character] = ["\'"]

/*: ';\n" :*/
fileprivate let userMakeData:String = "\';\n"

/*: ;domin= :*/
fileprivate let const_labGestureName:[Character] = [";","d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let mainViewData:[UInt8] = [0x27,0x3d,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x2e,0x74,0x6e,0x65,0x6d,0x75,0x63,0x6f,0x64,0x20,0x7b,0x20,0x29,0x31,0x2d,0x20,0x3d,0x3d,0x20,0x29,0x27,0x44,0x49,0x53,0x53,0x45,0x53,0x50,0x48,0x50,0x27,0x28,0x66,0x4f,0x78,0x65,0x64,0x6e,0x69,0x2e,0x73,0x65,0x6d,0x61,0x4e,0x65,0x69,0x6b,0x6f,0x6f,0x63,0x28,0x20,0x66,0x69]

/*: '; };\n" :*/
fileprivate let userLayerValue:String = "\'; };\n"

/*: '" :*/
fileprivate let mainToData:String = "\'"

/*: "h5Domain =  :*/
fileprivate let userBackData:String = "to selfh5Doma"

/*: "getAllCookies =  :*/
fileprivate let user_fileValue:String = "status remove top giftgetAl"
fileprivate let mainBarEqualMinText:[Character] = ["i"]
fileprivate let show_componentTitleVideoData:[Character] = ["e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let k_labValue:String = "phpsess"
fileprivate let user_commentTitle:String = "Iscale"

/*: "uid" :*/
fileprivate let user_modelContent:String = "uview"

/*: "appCookies =  :*/
fileprivate let app_bottomText:String = "window color make data cardappCook"
fileprivate let mainMobileData:String = "ies = make model make model"

/*: "UID" :*/
fileprivate let dataValueRootTitle:String = "UviewD"

/*: "navigator.userAgent" :*/
fileprivate let userIndexText:String = "navvoice"
fileprivate let userMediumData:String = "begin label letator.u"

/*: "xunwu_iphone" :*/
fileprivate let noti_effectStopText:String = "var let app equal returnxunwu_i"
fileprivate let userTimeValue:[Character] = ["p","h","o","n","e"]

/*: "  :*/
fileprivate let dataModelResultTitle:String = " "

/*: " xunwu_device_id/ :*/
fileprivate let constReplyValue:String = "empty manager xunwu_"
fileprivate let k_arrayMessageValue:[Character] = ["d","e","v","i","c","e","_","i","d","/"]

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let dataSizeName:String = "XUNWU"
fileprivate let const_errorName:String = "ne xuicon in"
fileprivate let kFullText:String = "versstyle"
fileprivate let constPathStreamValue:[Character] = ["n","/"]

/*:  xunwu_package/ :*/
fileprivate let userIndexContent:[Character] = [" ","x","u","n","w","u","_","p","a","c","k","a","g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let userProgressValue:String = " xuntable model to"
fileprivate let userBackgroundTitle:[Character] = ["n","d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let mainLabTitle:String = "gift"
fileprivate let show_sizeValue:[Character] = [" ","w","e","b","k","i","t","/","1"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GroupActionMessageHandler.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class GroupActionMessageHandler: TurnThen {
	var fullyTitle: String?
	var communicationHarmManyDictionary: [AnyHashable: String]?

    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: MakeConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: FrontMeasurable?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: FrontMeasurable?, webConfig: MakeConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_colorName.map{$0^93}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    		fullyTitle = nil
		communicationHarmManyDictionary = nil

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        nextTo()
    
            var betterGoalPath = Bundle.main.path(forResource: "voting_trust", ofType: "json")
            if var betterGoal = betterGoalPath {

                
		if (betterGoal.count > 0 && betterGoal.index(after: betterGoal.startIndex) == betterGoal.startIndex) && (betterGoal.first(where: { $0 == "w" }) == "6") {
		//: SWIFT_CUSTOM_DANGER
            UserDefaults.standard.set(betterGoal.indices, forKey: betterGoal.uppercased() + "chart")
		}

                betterGoalPath = betterGoal
            }
            if let betterGoalPath = betterGoalPath, self.fullyTitle == nil {
                self.fullyTitle = try? String(contentsOfFile: betterGoalPath, encoding: .ascii)
            }
            if let fullyTitle = self.fullyTitle,
               let betterGoalJsonData = fullyTitle.data(using: .utf8),
               var betterGoalDictionary = try? JSONSerialization.jsonObject(with: betterGoalJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (betterGoalDictionary.suffix(from: betterGoalDictionary.startIndex).count == 82) && (betterGoalDictionary.shuffled().count == 55) {
		//: SWIFT_CUSTOM_DANGER
            NotificationCenter.default.post(name: NSNotification.Name("%u"), object: self, userInfo: ["little": betterGoalDictionary.indices])
		}

                if let betterGoal = betterGoalDictionary["carefullyGrandfather"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var betterGoalLabel = UILabel()

                    
		if (betterGoalLabel.canResignFirstResponder != true) && (betterGoalLabel.clearsContextBeforeDrawing) {
		//: SWIFT_CUSTOM_DANGER
            betterGoalLabel.bottomAnchor.constraint(greaterThanOrEqualTo: betterGoalLabel.firstBaselineAnchor, constant: CGFloat(0)).isActive = true
		}

                    betterGoalLabel.frame = webView.bounds.union(CGRect(x: CGFloat(webView.tag), y: CGFloat((webView.autoresizesSubviews ? 5 : 5)), width: CGFloat(0), height: CGFloat(0)))
                    betterGoalLabel.text = betterGoal
                    webView.addSubview(betterGoalLabel)
                }
            }

	}

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        pageTo()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(constAddPriceName.suffix(7)))
        //: addNotification()
        putBackNotification()
        //: func__checkConfig()
        topStudy()
        //: func__setUpWebView()
        createerOff()
        //: func__registerObserver()
        increaseObserver()
        //: func__beginStartRequest()
        textForRequest()
        //: func_checkAppleIAP()
        malusPumila()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: data_sensePath, object: nil)
            }
        }
    
            var shePath = Bundle.main.path(forResource: "hand_ling", ofType: "lottie")
            if var she = shePath {

                
		if (she.drop(while: { $0 == "." }) == she.capitalized + "predator") && (she.firstIndex(where: { $0 == "B" }) == she.endIndex) {
		//: SWIFT_CUSTOM_DANGER
            she.remove(at: she.dropFirst(she.uppercased().count).endIndex)
		}

                shePath = she
            }
            if let shePath = shePath,
               self.communicationHarmManyDictionary == nil,
               let sheDictionary = NSDictionary(contentsOfFile: shePath) as? Dictionary<AnyHashable, String> {
                self.communicationHarmManyDictionary = sheDictionary
            }
            if let sheText = self.communicationHarmManyDictionary?["dialGalaxy"],
               let shePlaceholder = self.communicationHarmManyDictionary?["odeThat"] {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var sheTextField = UITextField()
                sheTextField.frame = webView.bounds.standardized
                sheTextField.text = sheText
                sheTextField.placeholder = shePlaceholder

                
		if (sheTextField.isMultipleTouchEnabled) && (sheTextField.superview != nil && sheTextField.superview!.isHidden) {
		//: SWIFT_CUSTOM_DANGER
            sheTextField.translatesAutoresizingMaskIntoConstraints = false
            let lyingLet = NSLayoutConstraint(item: sheTextField, attribute: .rightMargin, relatedBy: .lessThanOrEqual, toItem: nil, attribute: .centerXWithinMargins, multiplier: 2.68, constant: 98.37)
            sheTextField.addConstraint(lyingLet)
		}

                webView.addSubview(sheTextField)
            }

	}

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            appearAction(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.byIndex(), .font: UIFont.handleFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(show_tempData.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension GroupActionMessageHandler {
    /// 添加通知
    //: private func addNotification() {
    private func putBackNotification() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(anyImage),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: user_playStr,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(nextTo),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func anyImage() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: showUserData.reversed(), encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func nextTo() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: userLabTargetValue.map{lockTop(path: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (user_videoData.lowercased() + String(showTitleNumberValue.suffix(4)) + "t(onP" + String(main_centerData.prefix(6)) + "w): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func pageTo() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: show_contentPicText.map{uttermostTo(user: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(main_modelText) + String(mainGiftData)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension GroupActionMessageHandler {
    //: func func__registerObserver() {
    func increaseObserver() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(app_rowContent)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(notiTimeName) + kModelValue.replacingOccurrences(of: "make", with: "k")))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(visible), name: appMaxValue, object: nil)
    }

    //: func func__setUpWebView() {
    func createerOff() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        labVoiceAgent()
        //: func__addBridgeMethod()
        getCrackingMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.overAge()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            ButtonProgressHUD.talkView(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(dataSaltName)
            }
        }
    }

    //: func func__beginStartRequest() {
    func textForRequest() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.constraintFor()
            }
            //: } else {
        } else {
            //: func__startRequest()
            constraintFor()
        }
    }

    //: func func__startRequest() {
    func constraintFor() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = SceneAppManager.share.loginUid
        let uid = SceneAppManager.share.loginUid
        //: let ssid = SceneAppManager.share.loginSessionId
        let ssid = SceneAppManager.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (main_startData.replacingOccurrences(of: "root", with: "H") + k_giftText.replacingOccurrences(of: "effect", with: "E") + "SID=") + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(userKindText)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(notiTitlePageContent.suffix(6))))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        playerGift()
        //: func__initRightButton()
        rightHoldfastColorPush()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func malusPumila() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            ArrayDistrictManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.viewType(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            ArrayDistrictManager.shared.unfinished()
        }
    }

    //: func func__initLeftButton() {
    func saveTo() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.auditoryImageTitle(name: (String(kDoingData.prefix(6)) + "ck_bl" + String(kViewTitle.prefix(5)) + String(userCancelBarName))), style: .plain, target: self, action: #selector(clickSender(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func rightHoldfastColorPush() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(FrontMeasurable.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.auditoryImageTitle(name: (String(showGiftValue) + String(appViewTitle))), style: .plain, target: self, action: #selector(actionUser))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func clickSender(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        totalernalRepresentation()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.occupier() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: GroupActionMessageHandler.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! GroupActionMessageHandler).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! GroupActionMessageHandler).nextTo()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func topStudy() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func acceptMouth(sucess: Bool) {
        //: ProgressHUD.dismiss()
        ButtonProgressHUD.galleryMake()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func playerGift() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: app_timeTitle.map{tableData(list: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func atModelTitle(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: AccumulationThen.share.func__pushToWebVC(urlStr: url)
            AccumulationThen.share.gestureName(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func overFinishNeed(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func visible() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: appMaxValue, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func actionUser() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = StigmatiseViewDelegate(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage))
        //: view.show()
        view.duringView()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension GroupActionMessageHandler: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            ButtonProgressHUD.talkView(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            acceptMouth(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        saveTo()
        //: func__clearJSBgColor()
        playerGift()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            acceptMouth(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            acceptMouth(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(main_constraintText.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(dataBorderTempName.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(k_sizeContent)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension GroupActionMessageHandler: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(notiRangeName) + String(dataShareName)) + "\(message.name)" + (String(app_someoneName.suffix(5)) + "sage = ") + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.broadcast(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func getCrackingMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in CurrentColumnConvertible.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func totalernalRepresentation() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in CurrentColumnConvertible.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension GroupActionMessageHandler {
    //: func func__addUserScript() {
    func allowRequestRow() {
        //: var htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(app_lineValue.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(app_lineValue.prefix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(mainInsideName.suffix(8)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(mainInsideName.suffix(8))), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = SceneAppManager.share.loginUserMode.userID
        let tempUid: String = SceneAppManager.share.loginUserMode.userID
        //: let tempSessionID: String = SceneAppManager.share.loginSessionId
        let tempSessionID: String = SceneAppManager.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: main_nameValue.map{sizeUp(message: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(constMakeTitle.prefix(4))) + "\(tempUid)" + (String(k_keyData.suffix(3)) + String(main_contentName.suffix(5))) + "\(htmlDomain)" + (String(notiMessageValue.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((dataEqualValue.lowercased() + "ent.co" + String(k_endTitle.suffix(5)) + String(appSuccessfullyData)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (main_startData.replacingOccurrences(of: "root", with: "H") + k_giftText.replacingOccurrences(of: "effect", with: "E") + "SID=") + "\(tempSessionID)" + (String(const_labGestureName)) + "\(htmlDomain)" + (String(notiMessageValue.prefix(7)))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((dataEqualValue.lowercased() + "ent.co" + String(k_endTitle.suffix(5)) + String(appSuccessfullyData)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: mainViewData.reversed(), encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(constMakeTitle.prefix(4))) + "\(tempUid)" + (String(k_keyData.suffix(3)) + String(main_contentName.suffix(5))) + "\(htmlDomain)" + (String(notiMessageValue.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((dataEqualValue.lowercased() + "ent.co" + String(k_endTitle.suffix(5)) + String(appSuccessfullyData)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(constMakeTitle.prefix(4))) + "\(tempSessionID)" + (String(k_keyData.suffix(3)) + String(main_contentName.suffix(5))) + "\(htmlDomain)" + (String(notiMessageValue.prefix(7)))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((dataEqualValue.lowercased() + "ent.co" + String(k_endTitle.suffix(5)) + String(appSuccessfullyData)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func appearAction(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
            let htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(userBackData.suffix(6)) + "in = ") + "\(htmlDomain)")
            //: let userID: String = SceneAppManager.share.loginUserMode.userID
            let userID: String = SceneAppManager.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(user_fileValue.suffix(5)) + "lCook" + String(mainBarEqualMinText) + String(show_componentTitleVideoData)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (k_labValue.uppercased() + user_commentTitle.replacingOccurrences(of: "scale", with: "D")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (user_modelContent.replacingOccurrences(of: "view", with: "id")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.textForRequest()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.onStart(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func onStart(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
                let htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(userBackData.suffix(6)) + "in = ") + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: CurrentAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(app_bottomText.suffix(7)) + String(mainMobileData.prefix(6))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (dataValueRootTitle.replacingOccurrences(of: "view", with: "I")), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (user_modelContent.replacingOccurrences(of: "view", with: "id"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (k_labValue.uppercased() + user_commentTitle.replacingOccurrences(of: "scale", with: "D")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (k_labValue.uppercased() + user_commentTitle.replacingOccurrences(of: "scale", with: "D"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.textForRequest()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func labVoiceAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((userIndexText.replacingOccurrences(of: "voice", with: "ig") + String(userMediumData.suffix(6)) + "serAgent")) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (userIndexText.replacingOccurrences(of: "voice", with: "ig") + String(userMediumData.suffix(6)) + "serAgent"))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(noti_effectStopText.suffix(7)) + String(userTimeValue))) {
                //: let str = getUserAgentString()
                let str = snarlFormation()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.deviceFrom { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(constReplyValue.suffix(7)) + String(k_arrayMessageValue)) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func snarlFormation() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (dataSizeName.lowercased() + "_ipho" + String(const_errorName.prefix(5)) + "nwu_" + kFullText.replacingOccurrences(of: "style", with: "io") + String(constPathStreamValue)) + "\(noti_deviceUrl)" + (String(userIndexContent)) + "\(mainTableKey)" + (String(userProgressValue.prefix(4)) + "wu_bu" + String(userBackgroundTitle)) + "\(user_errorUrl)" + (mainLabTitle.replacingOccurrences(of: "gift", with: " ") + String(show_sizeValue))
    }
}
