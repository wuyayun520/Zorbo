
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let main_lengthData:[UInt8] = [0xb2,0xa3,0xa3,0xbf,0xb6,0xac,0xba,0xb2,0xa3,0xac,0xa3,0xb2,0xaa,0xac,0xa0,0xa6,0xb0,0xb0,0xb6,0xb6,0xb7,0xac,0xbd,0xbc,0xa7,0xba,0xb5,0xba,0xb0,0xb2,0xa7,0xba,0xbc,0xbd]

private func playerRequestKey(make num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let mainCellData:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x44,0x45,0x45,0x43,0x43,0x55,0x53,0x5f,0x45,0x42,0x49,0x52,0x43,0x53,0x42,0x55,0x53,0x5f,0x50,0x41,0x49,0x5f,0x45,0x4c,0x50,0x50,0x41]

/*: "mf/recharge/createApplePay" :*/
fileprivate let const_viewValue:String = "path cookie white for sendmf/rec"
fileprivate let const_errorName:String = "bottom image var contente/crea"
fileprivate let showContentName:String = "tplayer"
fileprivate let noti_pickValue:String = "Applif pension"

/*: "productId" :*/
fileprivate let user_countThroughValue:[Character] = ["p","r","o","d","u","c","t","I"]
fileprivate let show_fatalData:String = "app"

/*: "source" :*/
fileprivate let k_equalMarginValue:String = "info"
fileprivate let userOldData:[Character] = ["o","u","r","c","e"]

/*: "IAP充值创建订单失败: :*/
fileprivate let noti_managerText:[Character] = ["I","A","P","\u{5145}","\u{503c}","创","建","订","单","\u{5931}","\u{8d25}",":"]

/*: , 参数: :*/
fileprivate let userSharedValue:[Character] = [","," ","参","数",":"]

/*: ." :*/
fileprivate let app_playerName:String = "end"

/*: "orderNum" :*/
fileprivate let k_modelValue:String = "var launch color wraporderNum"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let k_letterText:[Character] = ["m","f","/","r","e","c","h","a","r","g","e","/"]
fileprivate let noti_sizeContentData:String = "aatatle"

/*: "IAP充值校验失败: :*/
fileprivate let noti_cornerValue:[Character] = ["I","A","P","充","值","校","验","失","败",":"]

/*: "transactionId" :*/
fileprivate let showContentData:String = "TRANSAC"
fileprivate let userEqualData:String = "reason"
fileprivate let k_makeValue:String = "ionIddata self make"

/*: "reportMoney" :*/
fileprivate let const_indexCellText:[Character] = ["r","e","p","o","r","t","M","o","n","e"]
fileprivate let const_builderData:String = "Y"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let appInputValue:[Character] = ["m","f","/","A","u","t","o","S","u","b","/"]
fileprivate let dataBottomText:[Character] = ["A","p","p","l"]
fileprivate let noti_playerData:String = "eCreactual make"

/*: "IAP订阅创建订单失败: :*/
fileprivate let k_makeCommentText:[Character] = ["I","A","P","\u{8ba2}","阅","\u{521b}","建","订","单","失","败",":"]

/*: "orderId" :*/
fileprivate let const_rowKeyTitle:[UInt8] = [0xca,0xcd,0xbf,0xc0,0xcd,0xa4,0xbf]

fileprivate func addLoad(dismiss num: UInt8) -> UInt8 {
    let value = Int(num) + 165
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let data_userColorText:[Character] = ["m","f","/","A","u","t","o","S","u","b","/","A","p","p","l","e","P","a","y"]
fileprivate let dataNumberEventTitle:String = "handle"
fileprivate let dataTimeName:String = "ulabeless"

/*: "IAP订阅校验失败: :*/
fileprivate let data_viewText:String = "title self live heightIAP订"
fileprivate let noti_statusTitle:String = ":"

/*: "App" :*/
fileprivate let dataBurnValue:String = "Appuser bright make from"

/*: "OrderTransactionInfo_ :*/
fileprivate let const_infoValue:String = "the regular make view containerOrder"
fileprivate let showHeadData:[Character] = ["T","r","a","n","s","a","c","t","i","o","n","I","n","f","o","_"]

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let userOpenValue:[UInt8] = [0xfc,0x1f,0x11,0x12,0x1f,0x1,0x1f,0xe,0x1b,0x20,0xe,0x10,0x21,0x16,0x1c,0x1b,0xf6,0x1b,0x13,0x1c,0xc,0x0,0x22,0xf,0x20,0x10,0x1f,0x16,0xf,0x12,0xc]

fileprivate func thanCollection(age num: UInt8) -> UInt8 {
    let value = Int(num) - 173
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "verifyData" :*/
fileprivate let appByData:[UInt8] = [0xd8,0xcb,0xdc,0xc7,0xc8,0xd7,0xea,0xcf,0xda,0xcf]

private func makeSucceed(photo num: UInt8) -> UInt8 {
    return num ^ 174
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let noti_userText:String = "IAP查询\u{5185}购"
fileprivate let show_socialData:String = "selected失败, "
fileprivate let show_withContent:String = "user color view format telltId:"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let k_infoData:String = "IAP查询内extension rank return"
fileprivate let k_partyName:String = "购\u{5546}品失败, p"
fileprivate let mainBlackName:[Character] = ["r","o","d","u","c","t","s",".","c","o","u","n","t","为","0","."]

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let kLiveBubbleEqualName:String = "IAP\u{67e5}询"
fileprivate let noti_cellName:[Character] = ["内","购"]
fileprivate let notiPhoneColorTitle:String = "商\u{54c1}失败,"

/*: "IAP-purchased: :*/
fileprivate let constTotalFileName:String = "source empty var trueIAP-pu"
fileprivate let dataTitleValue:[Character] = ["r","c"]
fileprivate let userVoiceValue:[Character] = ["h","a","s","e","d",":"]

/*: , 订单号: :*/
fileprivate let dataAcceptGiftValue:String = ", 订单号:"

/*: , 商品Id: :*/
fileprivate let app_sizeValue:String = "stop guard new var index, 商品I"
fileprivate let appModelValue:String = "d:height view gift"

/*: , 苹果transId: :*/
fileprivate let k_editText:[Character] = [","," ","\u{82f9}","果","t","r","a"]
fileprivate let mainTopData:String = "nsId:normal label self"

/*: "IAP-failed: :*/
fileprivate let noti_sizeDomainName:[Character] = ["I","A","P","-","f","a","i","l","e","d",":"]

/*: "IAP-deferred: :*/
fileprivate let data_infoStyleTitle:String = "IAP-decount false left let"
fileprivate let userFirstName:String = "ferred:if to leading icon check"

/*: "IAP未知交易类型: :*/
fileprivate let noti_reTitleCookieValue:String = "IAP\u{672a}知"
fileprivate let user_makeData:String = "\u{4ea4}易类型:"

/*: " 未知的交易类型" :*/
fileprivate let noti_equalCellValue:String = " 未"
fileprivate let const_enableText:[Character] = ["知","\u{7684}","\u{4ea4}","易","类","\u{578b}"]

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let noti_clickCardData:[Character] = ["I","A","P","获","取","本","地","\u{6536}","据","数","据","失","败",","]
fileprivate let show_arrayData:[Character] = [" ","t","r","a","n","s","a","c","t","i","o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let mainObserverData:String = ", ordermake item height"
fileprivate let dataPhotoValue:[Character] = ["I","d",":"]

/*: , payType: :*/
fileprivate let showKeyFrameSumData:String = "add"
fileprivate let kColorData:String = " payTreturn size collection make false"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ArrayDistrictManager.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let k_videoMsg = NSNotification.Name(rawValue: String(bytes: main_lengthData.map{playerRequestKey(make: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let show_errorMessage = NSNotification.Name(rawValue: String(bytes: mainCellData.reversed(), encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let appUserUrl = 9

/// 支付类型
//: enum ApplePayType {
enum SceneType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum CaptureContentLiteral: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (CaptureContentLiteral, Double, SceneType) -> Void

//: class AppleIAPManager: NSObject {
class ArrayDistrictManager: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: SceneType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = ArrayDistrictManager()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(upTerminate),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func upTerminate() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension ArrayDistrictManager {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func goColor(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = UpRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(const_viewValue.suffix(6)) + "harg" + String(const_errorName.suffix(6)) + showContentName.replacingOccurrences(of: "player", with: "e") + String(noti_pickValue.prefix(4)) + "ePay")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(user_countThroughValue) + show_fatalData.replacingOccurrences(of: "app", with: "d"))] = productId
        //: dict["source"] = source
        dict[(k_equalMarginValue.replacingOccurrences(of: "info", with: "s") + String(userOldData))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        main_listData.currentCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                NurseLogReactiveCompatible.citeOrLineup(msg: (String(noti_managerText)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(k_modelValue.suffix(8)))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func actionLast(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = UpRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(k_letterText) + noti_sizeContentData.replacingOccurrences(of: "at", with: "p") + "PayNotify")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        main_listData.currentCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    NurseLogReactiveCompatible.citeOrLineup(msg: (String(noti_cornerValue)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.withGame(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.tabTo()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_videoMsg, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(String(const_indexCellText) + const_builderData.lowercased())].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension ArrayDistrictManager {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func makeSecureHandle(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = UpRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(appInputValue) + String(dataBottomText) + String(noti_playerData.prefix(4)) + "ateOrder")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(user_countThroughValue) + show_fatalData.replacingOccurrences(of: "app", with: "d"))] = productId
        //: dict["source"] = source
        dict[(k_equalMarginValue.replacingOccurrences(of: "info", with: "s") + String(userOldData))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        main_listData.currentCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                NurseLogReactiveCompatible.citeOrLineup(msg: (String(k_makeCommentText)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: const_rowKeyTitle.map{addLoad(dismiss: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func waysAndMeans(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = UpRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(data_userColorText) + dataNumberEventTitle.replacingOccurrences(of: "handle", with: "S") + dataTimeName.replacingOccurrences(of: "label", with: "cc"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        main_listData.currentCompletion(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    NurseLogReactiveCompatible.citeOrLineup(msg: (String(data_viewText.suffix(4)) + "阅校验失败" + noti_statusTitle.capitalized) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.withGame(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.subscribePath()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_errorMessage, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(String(const_indexCellText) + const_builderData.lowercased())].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension ArrayDistrictManager {
    /// 初始化数据
    //: private func iap_initData() {
    private func infoEvent() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = scoff(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = scoff(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func scoff(payType: SceneType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = tabTo()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = subscribePath()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func tabTo() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(dataBurnValue.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = app_seatPath.string(forKey: mainInformationData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(const_infoValue.suffix(5)) + String(showHeadData)) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func subscribePath() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(dataBurnValue.prefix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = app_seatPath.string(forKey: mainInformationData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: userOpenValue.map{thanCollection(age: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func showData(_ transactionId: String, _ payType: SceneType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: appByData.map{makeSucceed(photo: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: appByData.map{makeSucceed(photo: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension ArrayDistrictManager {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func unfinished() {
        //: iap_initData()
        infoEvent()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            priorityExecute(dict[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            priorityExecute(dict[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func priorityExecute(_ transactionId: String?, _ payType: SceneType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        withGame(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension ArrayDistrictManager {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func subText(productId: String, payType: SceneType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        infoEvent()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            goColor(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.blackOut(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            makeSecureHandle(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.blackOut(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func blackOut(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            NurseLogReactiveCompatible.citeOrLineup(msg: (noti_userText + show_socialData.replacingOccurrences(of: "selected", with: "商品") + "produc" + String(show_withContent.suffix(4))) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.playAddCover()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func playAddCover() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension ArrayDistrictManager: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            NurseLogReactiveCompatible.citeOrLineup(msg: (String(k_infoData.prefix(6)) + k_partyName + String(mainBlackName)))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        NurseLogReactiveCompatible.citeOrLineup(msg: (kLiveBubbleEqualName + String(noti_cellName) + notiPhoneColorTitle + " Error:") + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension ArrayDistrictManager: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    NurseLogReactiveCompatible.citeOrLineup(msg: (String(constTotalFileName.suffix(6)) + String(dataTitleValue) + String(userVoiceValue)) + "\(currentPayType)" + (dataAcceptGiftValue.capitalized) + "\(String(describing: createOrderId))" + (String(app_sizeValue.suffix(5)) + String(appModelValue.prefix(2))) + "\(transaction.payment.productIdentifier)" + (String(k_editText) + String(mainTopData.prefix(5))) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    withGame(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                NurseLogReactiveCompatible.citeOrLineup(msg: (String(noti_sizeDomainName)) + "\(currentPayType)" + (dataAcceptGiftValue.capitalized) + "\(String(describing: createOrderId))" + (String(app_sizeValue.suffix(5)) + String(appModelValue.prefix(2))) + "\(transaction.payment.productIdentifier)" + (String(k_editText) + String(mainTopData.prefix(5))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                NurseLogReactiveCompatible.citeOrLineup(msg: (String(data_infoStyleTitle.prefix(6)) + String(userFirstName.prefix(7))) + "\(currentPayType)" + (dataAcceptGiftValue.capitalized) + "\(String(describing: createOrderId))" + (String(app_sizeValue.suffix(5)) + String(appModelValue.prefix(2))) + "\(transaction.payment.productIdentifier)" + (String(k_editText) + String(mainTopData.prefix(5))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                NurseLogReactiveCompatible.citeOrLineup(msg: (noti_reTitleCookieValue + user_makeData) + "\(currentPayType)" + (dataAcceptGiftValue.capitalized) + "\(String(describing: createOrderId))" + (String(app_sizeValue.suffix(5)) + String(appModelValue.prefix(2))) + "\(transaction.payment.productIdentifier)" + (String(k_editText) + String(mainTopData.prefix(5))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((noti_equalCellValue.capitalized + String(const_enableText)))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func withGame(_ transactionId: String, _ payType: SceneType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = showData(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            NurseLogReactiveCompatible.citeOrLineup(msg: (String(noti_clickCardData) + String(show_arrayData)) + "\(transactionId)" + (String(mainObserverData.prefix(7)) + String(dataPhotoValue)) + "\(String(describing: createOrderId))" + (showKeyFrameSumData.replacingOccurrences(of: "add", with: ",") + String(kColorData.prefix(5)) + "ype:") + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据

                if self.payCacheList.filter({ $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] == transactionId || $0[String(bytes: const_rowKeyTitle.map{addLoad(dismiss: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: const_rowKeyTitle.map{addLoad(dismiss: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: appByData.map{makeSucceed(photo: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.tabTo()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据

                if self.subscribeCacheList.filter({ $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] == transactionId || $0[String(bytes: const_rowKeyTitle.map{addLoad(dismiss: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: const_rowKeyTitle.map{addLoad(dismiss: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: appByData.map{makeSucceed(photo: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.subscribePath()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > appUserUrl {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            actionLast(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(showContentData.lowercased() + userEqualData.replacingOccurrences(of: "reason", with: "t") + String(k_makeValue.prefix(5)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            waysAndMeans(transactionId, params: paramsArr.first!)
        }
    }
}