
//: Declare String Begin

/*: / :*/
fileprivate let app_cellData:String = "/"

/*:  ( :*/
fileprivate let showPlayerName:String = " temp"

/*: ; build: :*/
fileprivate let userFromValue:String = "; buiimage true file"
fileprivate let showRawData:String = "ld:target push add"

/*: ; iOS  :*/
fileprivate let const_feeDayContent:String = "; iOS image first not"

/*: ;  :*/
fileprivate let const_valueName:String = "; "

/*: )" :*/
fileprivate let show_pathName:String = "number"

/*: "audio" :*/
fileprivate let kTimeData:[Character] = ["a","u","d","i","o"]

/*: + :*/
fileprivate let app_eyeTitle:[Character] = ["+"]

/*: .wav" :*/
fileprivate let userRefText:String = "model manager view.wav"

/*: "audio/wav" :*/
fileprivate let showMainData:[Character] = ["a"]
fileprivate let app_shadowName:[Character] = ["u","d","i","o","/","w","a","v"]

/*: .jpg" :*/
fileprivate let show_fileData:String = ".jpgremove in result value"

/*: "image/jpeg" :*/
fileprivate let showCellText:String = "IMAG"
fileprivate let const_firstModelContent:String = "block"

/*: "://app." :*/
fileprivate let notiBottomServerName:String = ":"
fileprivate let app_playerToName:String = "//app.app height accept self value"

/*: "data" :*/
fileprivate let dataItemModelTitle:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let user_styleName:[Character] = [":","n","u","l","l"]

/*: ":{}" :*/
fileprivate let notiLayerData:String = ":{constraint"

/*: "Request failed, Try again later" :*/
fileprivate let appLabelValue:[UInt8] = [0xe,0x39,0x2d,0x29,0x39,0x2f,0x28,0x7c,0x3a,0x3d,0x35,0x30,0x39,0x38,0x70,0x7c,0x8,0x2e,0x25,0x7c,0x3d,0x3b,0x3d,0x35,0x32,0x7c,0x30,0x3d,0x28,0x39,0x2e]

private func infoData(image num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "json error" :*/
fileprivate let mainTableValue:[Character] = ["j","s","o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let dataModelName:String = "sharelat"
fileprivate let const_atText:String = "with self log self color=iphon"
fileprivate let showIndexName:String = "action data fore&ve"
fileprivate let appGalleryData:[Character] = ["r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let const_greetListText:[Character] = ["&","p","a","c","k"]
fileprivate let kLetterName:String = "ageId=push selected effect"

/*: &bundleId= :*/
fileprivate let data_equalMakeContent:[Character] = ["&","b","u","n","d","l","e","I"]
fileprivate let mainActionColorName:[Character] = ["d","="]

/*: &lang= :*/
fileprivate let user_nextName:String = "view view&lang="

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let k_transactionValue:[Character] = ["?","!","@","#","$","^","&"]
fileprivate let data_windowName:String = "%*+,:;=\'"

/*: "`<>()[]{}/\\| " :*/
fileprivate let user_infoContent:String = "`<>(model[]{}"
fileprivate let showMakeName:[Character] = ["/","\\","|"," "]

/*: "/route/slowindex" :*/
fileprivate let main_giftValue:String = "leading time at user/rou"
fileprivate let const_centerViewData:String = "owindlayerx"

/*: "/route/index" :*/
fileprivate let appAllowName:String = "/routlet male white let"
fileprivate let noti_styleValue:String = "false user guard self viewe/index"

/*: "?path= :*/
fileprivate let notiMainGiftName:String = "?pathview read line show"
fileprivate let user_bottomValue:String = "time"

/*: "&GJNonceStr= :*/
fileprivate let constResultTitle:[Character] = ["&"]
fileprivate let mainTableButtonData:String = "GJNoawake self party let"

/*: "& :*/
fileprivate let main_managerOutsideValue:[Character] = ["&"]

/*: = :*/
fileprivate let userPackValue:[Character] = ["="]

/*: "&params= :*/
fileprivate let showShareGiftContent:String = "&"
fileprivate let dataShowText:String = "else view sex return extensionparams="

/*: "/ :*/
fileprivate let data_centerName:String = "style"

/*: "? :*/
fileprivate let constLabContent:[Character] = ["?"]

/*: "token" :*/
fileprivate let show_tableData:String = "TOKEN"

/*: "请求成功 :*/
fileprivate let notiImageName:[Character] = ["请","求","成","功"]

/*: "AAAAAAAA" :*/
fileprivate let constEqualData:String = "aaaaaaaa"

/*: "AA" :*/
fileprivate let const_likeData:[Character] = ["A","A"]

/*: "BB" :*/
fileprivate let notiAppValue:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let appToNameTitle:[Character] = ["\u{4e0b}","载","完","成"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let dataSectionValue:String = "equal actual custom/Docu"

/*: ".mp4" :*/
fileprivate let user_frameData:[Character] = [".","m","p","4"]

/*: "移动地址: :*/
fileprivate let const_showName:String = "\u{79fb}动地址:"

/*: "后台任务下载回来" :*/
fileprivate let appBoxName:[Character] = ["\u{540e}","台"]
fileprivate let const_cancelContent:String = "任务下载\u{56de}来"

/*: "下载成功 : :*/
fileprivate let constHiddenTitle:[Character] = ["下","载","成","功"," ",":"]

/*: "Net Error" :*/
fileprivate let constMakeName:String = "key type letNet "
fileprivate let app_withData:String = "star frame the lab leftError"

/*: "下载失败 : :*/
fileprivate let noti_priceName:[Character] = ["\u{4e0b}","载","失","败"," ",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BeanRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let appFailBottomValue = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let constButtonName = "\(constVideoPath)/\(notiAppFormat) (\(user_errorUrl)" + (String(userFromValue.prefix(5)) + String(showRawData.prefix(3))) + "\(main_deviceStr)" + (String(const_feeDayContent.prefix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: ToErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let main_listData = BeanRequestTool()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class BeanRequestTool: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func currentCompletion(model: UpRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.nameBinary() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.upload(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.viewFinder(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func upload(model: UpRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.objectWithTo(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.fillErrorUp(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: IncidentScalar.NetError.rawValue, errorMsg: k_liveFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptRakeMsg(showMsg: k_liveFormat)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(kTimeData)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.clipStamp())+\(mainTableKey)" + (String(userRefText.suffix(4))), mimeType: (String(showMainData) + String(app_shadowName)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(show_fileData.prefix(4))), mimeType: (showCellText.lowercased() + "e/jpe" + const_firstModelContent.replacingOccurrences(of: "block", with: "g")))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: IncidentScalar.NetError.rawValue, errorMsg: k_liveFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptRakeMsg(showMsg: k_liveFormat)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func viewFinder(model: UpRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.objectWithTo(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.fillErrorUp(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: user_barId ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = appFailBottomValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: IncidentScalar.NetError.rawValue, errorMsg: k_liveFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptRakeMsg(showMsg: k_liveFormat)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = appFailBottomValue }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    giftPlayer(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: IncidentScalar.NetError.rawValue, errorMsg: k_liveFormat))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        exceptRakeMsg(showMsg: k_liveFormat)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func giftPlayer(model: UpRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            CurrentAddrTool.share.packageCookies(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if user_barId, model.requestServer.contains((notiBottomServerName.capitalized + String(app_playerToName.prefix(6)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.videoKey(key: CurrentAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(dataItemModelTitle)) + "\"" + (String(user_styleName)), with: "" + "\"" + (String(dataItemModelTitle)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !user_barId {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(user_styleName)), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<ShouldMeasurable>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == IncidentScalar.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, ToErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    exceptRakeMsg(showMsg: responseModel.msg ?? String(bytes: appLabelValue.map{infoData(image: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case IncidentScalar.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: k_topUrl, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, ToErrorResponse(errorCode: IncidentScalar.NetError.rawValue, errorMsg: (String(mainTableValue)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func objectWithTo(model: UpRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (dataModelName.replacingOccurrences(of: "share", with: "p") + "form" + String(const_atText.suffix(6)) + String(showIndexName.suffix(4)) + String(appGalleryData)) + "\(noti_deviceUrl)" + (String(const_greetListText) + String(kLetterName.prefix(6))) + "\(mainTableKey)" + (String(data_equalMakeContent) + String(mainActionColorName)) + "\(user_errorUrl)" + (String(user_nextName.suffix(6))) + "\(CurrentAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if user_barId, serverUrl.contains((notiBottomServerName.capitalized + String(app_playerToName.prefix(6)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if CurrentAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(main_giftValue.suffix(4)) + "te/sl" + const_centerViewData.replacingOccurrences(of: "layer", with: "e")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(appAllowName.prefix(5)) + String(noti_styleValue.suffix(7)))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(notiMainGiftName.prefix(5)) + user_bottomValue.replacingOccurrences(of: "time", with: "=")) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(constResultTitle) + String(mainTableButtonData.prefix(4)) + "nceStr=") + "\(CurrentAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.withKey(key: CurrentAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((showShareGiftContent.capitalized + String(dataShowText.suffix(7))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func fillErrorUp(model: UpRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(constButtonName)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (show_tableData.lowercased()), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func appPhone(model: UpRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.objectWithTo(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (String(notiImageName)) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (constEqualData.uppercased())) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (String(appToNameTitle)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(dataSectionValue.suffix(5)) + "ments/") + NSDate.clipStamp() + (String(user_frameData))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (const_showName) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (String(appBoxName) + const_cancelContent))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = IndexReactiveCompatible.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func centerCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: userCreateName.caput(), with: constStatusStr)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(constHiddenTitle)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ToErrorResponse(errorCode: IncidentScalar.Normal.rawValue, errorMsg: (String(constMakeName.suffix(4)) + String(app_withData.suffix(5))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ToErrorResponse(errorCode: IncidentScalar.NetError.rawValue, errorMsg: (String(constMakeName.suffix(4)) + String(app_withData.suffix(5))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(noti_priceName)) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func messageSmall(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: userCreateName.caput(), with: constStatusStr)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(constHiddenTitle)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ToErrorResponse(errorCode: IncidentScalar.Normal.rawValue, errorMsg: (String(constMakeName.suffix(4)) + String(app_withData.suffix(5))).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ToErrorResponse(errorCode: IncidentScalar.NetError.rawValue, errorMsg: (String(constMakeName.suffix(4)) + String(app_withData.suffix(5))).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(noti_priceName)) + "\(errMsg)")
            }
        }
    }
}
