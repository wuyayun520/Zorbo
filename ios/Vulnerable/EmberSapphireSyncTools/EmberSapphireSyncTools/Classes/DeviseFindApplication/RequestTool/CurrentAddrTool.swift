
//: Declare String Begin

/*: "http://" :*/
fileprivate let notiEqualContent:String = "huser"
fileprivate let constMagnitudeeractionName:[Character] = ["t","p",":","/","/"]

/*: "https://" :*/
fileprivate let main_listValue:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let user_clickModelContent:String = "table color title by requesthttp://"

/*: "serverSelctIndex =  :*/
fileprivate let app_managerText:String = "equal white selfserverS"
fileprivate let mainTopData:String = "hidden at content super centerelctInd"
fileprivate let user_trackDropData:String = "ex = view right make hidden lab"

/*: "serverUrlStr =  :*/
fileprivate let appToData:String = "listrver"
fileprivate let const_beautyTitle:String = "Urself else equal hidden"
fileprivate let kEqualViewValue:String = "let if iflStr = "

/*: "app. :*/
fileprivate let noti_keyContent:String = "interval raw app gender labapp."

/*: .com" :*/
fileprivate let showFrameContent:[Character] = [".","c","o","m"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let showNameValue:String = "data start self rankmf/ch"
fileprivate let showSectionRateContent:String = "back if textom/s"

/*: "recharge/applePayNotify" :*/
fileprivate let app_leaveValue:String = "reprogressha"
fileprivate let kSoundValue:String = "moment source modify var iconpleP"
fileprivate let kViewValue:String = "inputy"

/*: "user/appleLogin" :*/
fileprivate let notiReportTitle:String = "party data model action clickuser/a"
fileprivate let mainSizeUserTitle:String = "gistroke"

/*: "IM/getMyBlackList" :*/
fileprivate let dataStoreViewGuardValue:String = "for firstIM/g"
fileprivate let main_iconText:String = "Blacnormal top model"

/*: "mf/business/sendMsg" :*/
fileprivate let k_emptyName:[Character] = ["m","f","/","b","u","s","i","n","e","s","s"]
fileprivate let mainViewCleanInData:String = "user equal manager image title/sendMsg"

/*: "mf/business/sendGift" :*/
fileprivate let data_pushTitle:[Character] = ["m","f","/","b","u","s"]
fileprivate let show_infoValue:String = "icelles"
fileprivate let user_pureValue:[Character] = ["s","/","s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let dataGiftText:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","M","s","g"]

/*: "en" :*/
fileprivate let appBorderFatalData:String = "itemn"

/*: "ar" :*/
fileprivate let appLayerData:[Character] = ["a","r"]

/*: "es" :*/
fileprivate let appRequestCurrentName:String = "ES"

/*: "pt" :*/
fileprivate let app_backName:String = "pvalue"

/*: "GJ21FB" :*/
fileprivate let notiClearContent:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let notiMomentName:[Character] = ["完","成","\u{4e86}","检","测",",","\u{8bf7}","求","状","态"]
fileprivate let userToTitle:[Character] = ["\u{6b63}","常"]

/*: "接口请求失败" :*/
fileprivate let appSucceedText:String = "接口请求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let app_insideName:[Character] = ["完","成","\u{4e86}","\u{68c0}","测",",","需","要","\u{66f4}","换","s","e","r","v","e"]
fileprivate let main_toPushValue:String = "r urlicon list success"

/*: "HTTPProxy" :*/
fileprivate let showLastViewValue:String = "HmmP"

/*: "__SCOPED__" :*/
fileprivate let constHiddenValue:String = "__SCOsize model equal guard"

/*: "tap" :*/
fileprivate let constInfoTitle:[Character] = ["t","a","p"]

/*: "tun" :*/
fileprivate let dataAtName:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let constShowContent:String = "managersec"

/*: "ppp" :*/
fileprivate let mainThenValue:String = "labellabellabel"

/*: "func__updateTalkingCookies =  :*/
fileprivate let noti_priceValue:[Character] = ["f","u","n","c","_"]
fileprivate let app_replyValue:String = "_updateempty like"
fileprivate let data_intimateValue:[Character] = ["T","a","l","k","i","n","g","C","o","o","k","i","e","s"," ","="," "]

/*: "https:// :*/
fileprivate let notiButtonText:[Character] = ["h","t","t","p","s",":","/"]
fileprivate let dataTargetValue:String = "/"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let constDataTableValue:[UInt8] = [0x20,0x3d,0x20,0x6c,0x72,0x55,0x68,0x74,0x69,0x57,0x73,0x65,0x69,0x6b,0x6f,0x6f,0x43,0x65,0x6d,0x61,0x53,0x65,0x74,0x65,0x6c,0x65,0x64,0x5f,0x5f,0x63,0x6e,0x75,0x66]

/*: "UID" :*/
fileprivate let notiSendValue:String = "uid"

/*: "PHPSESSID" :*/
fileprivate let main_shareName:String = "appHappSE"

/*: "func__saveAllCookies =  :*/
fileprivate let k_resultKeyData:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o"]
fileprivate let mainBottomValue:[Character] = ["o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let main_modelData:String = "fulocation"
fileprivate let main_buttonData:String = "return for make to inAllCookie"
fileprivate let user_imageViewRawName:[Character] = ["s"," "," ","e","r","r","o","r"]

/*: "🍪 删除 :*/
fileprivate let user_lengthValue:String = "🍪 删除"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CurrentAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum AttentionNameLiteral: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class CurrentAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = CurrentAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        postBackground()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((notiEqualContent.replacingOccurrences(of: "user", with: "t") + String(constMagnitudeeractionName))) || newValue.hasPrefix((String(main_listValue))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if userViewId && !user_barId {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(user_clickModelContent.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(user_clickModelContent.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    packageCookies(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func postBackground() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = app_seatPath.integer(forKey: constViewKey)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(app_managerText.suffix(7)) + String(mainTopData.suffix(7)) + String(user_trackDropData.prefix(5))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = constraintCorner()
        //: var serverPrefix = "http://"
        var serverPrefix = (notiEqualContent.replacingOccurrences(of: "user", with: "t") + String(constMagnitudeeractionName))
        //: if Environment_Formal && !Encryption_Request {
        if userViewId && !user_barId {
            //: serverPrefix = "http://"
            serverPrefix = (notiEqualContent.replacingOccurrences(of: "user", with: "t") + String(constMagnitudeeractionName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (appToData.replacingOccurrences(of: "list", with: "se") + String(const_beautyTitle.prefix(2)) + String(kEqualViewValue.suffix(7))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func constraintCorner() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func clickStr(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(noti_keyContent.suffix(4))) + "\(constStatusStr)" + (String(showFrameContent)), (String(noti_keyContent.suffix(4))) + "\(constStatusStr)" + (String(showFrameContent))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(showNameValue.suffix(5)) + "atRo" + String(showSectionRateContent.suffix(4)) + "endGift"),
         //: "recharge/applePayNotify",
         (app_leaveValue.replacingOccurrences(of: "progress", with: "c") + "rge/ap" + String(kSoundValue.suffix(4)) + "ayNoti" + kViewValue.replacingOccurrences(of: "input", with: "f")),
         //: "user/appleLogin"] + slowPathArr1
         (String(notiReportTitle.suffix(6)) + "ppleLo" + mainSizeUserTitle.replacingOccurrences(of: "stroke", with: "n"))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(dataStoreViewGuardValue.suffix(4)) + "etMy" + String(main_iconText.prefix(4)) + "kList"),
         //: "mf/business/sendMsg",
         (String(k_emptyName) + String(mainViewCleanInData.suffix(8))),
         //: "mf/business/sendGift",
         (String(data_pushTitle) + show_infoValue.replacingOccurrences(of: "cell", with: "n") + String(user_pureValue)),
         //: "mf/chatRoom/sendMsg"]
         (String(dataGiftText))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = TopReactiveCompatible.getSystemLangCode()
        let lang = TopReactiveCompatible.furtherance()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .dataLength(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(notiClearContent)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension CurrentAddrTool {
    //: func checkServerUrl() {
    func wineStewardWith() {
        //: if SceneAppManager.share.networkStatus != .Unavailable {
        if SceneAppManager.share.networkStatus != .Unavailable {
            //: func_requestHost()
            barHost()
            //: } else {
        } else {
            //: func_starCodeTime()
            mediumColorValue()
        }
    }

    //: func func_starCodeTime() {
    func mediumColorValue() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.barHost()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func enableEmpty() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func barHost() {
        //: if SceneAppManager.share.networkStatus == .Unavailable {
        if SceneAppManager.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        enableEmpty()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        DirectorManagerRequest.serviceName { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (String(notiMomentName) + String(userToTitle)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (appSucceedText.capitalized))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.mediumColorValue()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(app_insideName) + String(main_toPushValue.prefix(5))))
                    //: self.func__changeHostUrl()
                    self.adult()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func adult() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        enableEmpty()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        app_seatPath.set(serverSelctIndex, forKey: constViewKey)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (notiEqualContent.replacingOccurrences(of: "user", with: "t") + String(constMagnitudeeractionName))
        //: if Environment_Formal && !Encryption_Request {
        if userViewId, !user_barId {
            //: serverPrefix = "http://"
            serverPrefix = (notiEqualContent.replacingOccurrences(of: "user", with: "t") + String(constMagnitudeeractionName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension CurrentAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func utilised() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(showLastViewValue.replacingOccurrences(of: "m", with: "T") + "Proxy")] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func examine() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(constHiddenValue.prefix(5)) + "PED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((String(constInfoTitle))) || keyStr.contains((String(dataAtName))) || keyStr.contains((constShowContent.replacingOccurrences(of: "manager", with: "ip"))) || keyStr.contains((mainThenValue.replacingOccurrences(of: "label", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension CurrentAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func packageCookies(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(noti_priceValue) + String(app_replyValue.prefix(7)) + String(data_intimateValue)) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = safetyMarginUrl()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
        var urlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (notiEqualContent.replacingOccurrences(of: "user", with: "t") + String(constMagnitudeeractionName)), with: (String(main_listValue)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(main_listValue))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(notiButtonText) + dataTargetValue.capitalized) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            alongForStreetSmart(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        eachDeliverCurrentAction()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func alongForStreetSmart(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: constDataTableValue.reversed(), encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func safetyMarginUrl() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (notiEqualContent.replacingOccurrences(of: "user", with: "t") + String(constMagnitudeeractionName))
        //: if Environment_Formal && !Encryption_Request {
        if userViewId, !user_barId {
            //: serverPrefix = "https://"
            serverPrefix = (String(main_listValue))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(noti_keyContent.suffix(4))) + "\(constStatusStr)" + (String(showFrameContent)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func astatine(note _: Notification) {
        //: if SceneAppManager.share.loginUid.isEmptyString {
        if SceneAppManager.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            viewName()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            ifAddRequest()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func viewName() {
        //: var htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
        var htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (notiSendValue.uppercased()) {
                        //: appCookie["UID"] = cookie
                        appCookie[(notiSendValue.uppercased())] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (notiSendValue.uppercased()) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(notiSendValue.uppercased())] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((notiSendValue.uppercased())), h5Cookie.keys.contains((notiSendValue.uppercased())) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(notiSendValue.uppercased())]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(notiSendValue.uppercased())]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")), h5Cookie.keys.contains((main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func ifAddRequest() {
        //: let htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
        let htmlDomain: String = SceneAppManager.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == SceneAppManager.share.loginUserMode.userID {
            if cookie.name == (notiSendValue.uppercased()), cookie.value == SceneAppManager.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (notiSendValue.uppercased()) {
                        //: appCookie["UID"] = cookie
                        appCookie[(notiSendValue.uppercased())] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((notiSendValue.uppercased())) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(notiSendValue.uppercased())] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(main_shareName.replacingOccurrences(of: "app", with: "P") + "SSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func eachDeliverCurrentAction() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(k_resultKeyData) + String(mainBottomValue)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            app_seatPath.set(cookieData, forKey: noti_halfMessage)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (main_modelData.replacingOccurrences(of: "location", with: "nc") + "__save" + String(main_buttonData.suffix(9)) + String(user_imageViewRawName)))
        }
    }

    //: func func__setAppCookies() {
    func dataEqual() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = app_seatPath.object(forKey: noti_halfMessage)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func cookies() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        app_seatPath.removeObject(forKey: noti_halfMessage)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
