
//: Declare String Begin

/*: "fee" :*/
fileprivate let main_likeValue:String = "findexindex"

/*: "VIPFee" :*/
fileprivate let notiWhiteData:String = "sense self button trueVIPFee"

/*: "chattingNum" :*/
fileprivate let kToData:[Character] = ["c","h","a","t","t"]
fileprivate let showGiftTitle:String = "ingNumbutton let view"

/*: "You're already in her live room" :*/
fileprivate let notiAssetTitle:[UInt8] = [0xbd,0x8b,0x91,0xc3,0x96,0x81,0xc4,0x85,0x88,0x96,0x81,0x85,0x80,0x9d,0xc4,0x8d,0x8a,0xc4,0x8c,0x81,0x96,0xc4,0x88,0x8d,0x92,0x81,0xc4,0x96,0x8b,0x8b,0x89]

private func timeBack(status num: UInt8) -> UInt8 {
    return num ^ 228
}

/*: "toUid" :*/
fileprivate let showMakeName:[UInt8] = [0x65,0x60,0x46,0x5a,0x55]

fileprivate func titleFrame(on num: UInt8) -> UInt8 {
    let value = Int(num) + 15
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "momentId" :*/
fileprivate let constManagerName:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "extra" :*/
fileprivate let showItemValue:[UInt8] = [0x1c,0x1,0xd,0xb,0x18]

/*: "uid" :*/
fileprivate let dataCloseName:[UInt8] = [0x50,0x44,0x3f]

fileprivate func ifDo(corner num: UInt8) -> UInt8 {
    let value = Int(num) - 219
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let user_playerLocationContent:[UInt8] = [0x5c,0x5b,0x59,0x56,0x5b,0x52,0x40,0x61,0x4e,0x61,0x62,0x60]

fileprivate func managerSize(current num: UInt8) -> UInt8 {
    let value = Int(num) + 19
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let data_formatTitle:[UInt8] = [0xb2,0xa8,0x95,0xbe,0xac,0x8e,0xa8,0xbe,0xa9]

/*: "userStatus" :*/
fileprivate let userStyleName:[UInt8] = [0xd1,0xd7,0xc1,0xd6,0xf7,0xd0,0xc5,0xd0,0xd1,0xd7]

/*: "Account is restricted！" :*/
fileprivate let user_directionTitle:String = "Accounput any gift"
fileprivate let noti_titleText:String = "estradd"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let noti_pageData:[UInt8] = [0xc5,0xe2,0xea,0xef,0xe6,0xe7,0xa3,0xf7,0xec,0xa3,0xe6,0xed,0xf7,0xe6,0xf1,0xa3,0xf7,0xeb,0xe6,0xa3,0xe0,0xeb,0xe2,0xf7,0xa3,0xf1,0xec,0xec,0xee,0xad,0xa3,0xd3,0xef,0xe6,0xe2,0xf0,0xe6,0xa3,0xf7,0xf1,0xfa,0xa3,0xe2,0xe4,0xe2,0xea,0xed,0xa3,0xef,0xe2,0xf7,0xe6,0xf1]

private func timeFaceSkin(color num: UInt8) -> UInt8 {
    return num ^ 131
}

/*: "url" :*/
fileprivate let k_playContent:String = "URL"

/*: "userDetail" :*/
fileprivate let main_eyeData:String = "userDphoto make make view else"

/*: "&type=7" :*/
fileprivate let const_moveTitle:String = "equal list&type="
fileprivate let user_informationSourceName:String = "7"

/*: "amount" :*/
fileprivate let main_whiteAspectTitle:[UInt8] = [0x74,0x6e,0x75,0x6f,0x6d,0x61]

/*: "transparency" :*/
fileprivate let showWhoCollectionName:String = "traninfo"

/*: "`%^{}" :*/
fileprivate let userContentName:String = "`%^{}"

/*: "[]|\\<>" :*/
fileprivate let constVideoText:String = "[]|\\<"
fileprivate let user_succeedName:[Character] = [">"]

/*: "version= :*/
fileprivate let k_atName:String = "than"
fileprivate let show_contentData:String = "view height add changersion="

/*: &packageId= :*/
fileprivate let notiGiftContent:[Character] = ["&","p","a","c","k","a","g","e","I","d"]
fileprivate let main_withName:[Character] = ["="]

/*: &bundleId= :*/
fileprivate let data_clearValue:[Character] = ["&","b","u","n","d"]
fileprivate let userDoingValue:[Character] = ["l","e","I","d","="]

/*: & :*/
fileprivate let k_pathValue:String = "&"

/*: ? :*/
fileprivate let show_momentValue:[Character] = ["?"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AccumulationThen.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class AccumulationThen: NSObject {
public class AccumulationThen: NSObject {
    //: @objc static public let share = AccumulationThen()
    @objc public static let share = AccumulationThen()
    //: private override init() {}
    override private init() {}
}

//: extension AccumulationThen {
extension AccumulationThen {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func pension(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        noti_statusInviteScreenMessage.makeUp(eventID: app_senseDomainFormat)
        //: guard TalkingSocketManager.shared.isFinding == false else {
        guard StopThen.shared.isFinding == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptRakeMsg(showMsg: const_videoNoMessage)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        GenerateReactiveCompatible.inputMicrophone { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            StartReactiveCompatible.rowByCompletion { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = ObjectRecognizerDelegate()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(main_likeValue.replacingOccurrences(of: "index", with: "e"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(notiWhiteData.suffix(6)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(String(kToData) + String(showGiftTitle.prefix(6)))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.occupier()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func equalQuantityerval() {
        //: SceneAppManager.share.userFillInfoMode = UserFillInfoModel.init()
        SceneAppManager.share.userFillInfoMode = EventSizeInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = ViewGenderVc()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = saveer()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func modelSharedBack(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = saveer()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: EqualTurnReactiveCompatible.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: ProductViewController.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = EqualTurnReactiveCompatible()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func character(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.occupier() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? ToWithManagerDelegate, DataPushListener.handleCount().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.toolPostClick()

            //: } else if currentVC.isKind(of: TalkingVoiceRoomViewController.self), TalkingVoiceRoomManager.shared().isParty {
        } else if currentVC.isKind(of: TurnViewController.self), MediaThen.exhibitShared().isParty {
            //: TalkingVoiceRoomManager.shared().voiceRoom_mini()
            MediaThen.exhibitShared().outSendMini()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? EffectViewDelegate {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.pop()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = EffectViewDelegate(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        withAttribute()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func mailingList(uid: String, enterType: IndexEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard GenerateReactiveCompatible.numericalQuantityBlock() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(DataPushListener.handleCount().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.exceptRakeMsg(showMsg: String(bytes: notiAssetTitle.map{timeBack(status: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        GiftMotionPictureFanAudienceManager().pickUp(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = ExceptHandyJSON.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.saveer()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: ControllerWindowViewDelegate.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! ControllerWindowViewDelegate).totalAnimated()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            DataPushListener.handleCount().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = ControllerWindowViewDelegate()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                noti_statusInviteScreenMessage.makeUp(eventID: dataHalfTitle, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                noti_statusInviteScreenMessage.makeUp(eventID: data_fileKey, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    /// 进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func func_pushToVoiceRoomVC(roomId: String? = nil) {
    func sprechgesang(roomId: String? = nil) {
        //: if TalkingLiveManager.shared().isLive == true {
        if DataPushListener.handleCount().isLive == true { // 直播中
            //: func__showStatusBarErrorMsg(showMsg: kMessage_live_limit)
            exceptRakeMsg(showMsg: mainPlayMsg)
            //: return
            return
        }
        //: if roomId == TalkingVoiceRoomManager.shared().partyModel.roomId {
        if roomId == MediaThen.exhibitShared().partyModel.roomId { // 返回语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_goback()
            MediaThen.exhibitShared().vocalism()
            //: return
            return
        }
        //: if TalkingVoiceRoomManager.shared().isParty == true {
        if MediaThen.exhibitShared().isParty == true { // 切换语聊房
            //: if roomId != nil {
            if roomId != nil {
                //: TalkingVoiceRoomManager.shared().voiceRoom_switch(roomId: roomId!, beforeRoomId: TalkingVoiceRoomManager.shared().partyModel.roomId)
                MediaThen.exhibitShared().blockTo(roomId: roomId!, beforeRoomId: MediaThen.exhibitShared().partyModel.roomId)
            }
            //: return
            return
        }
        //: TalkingVoiceRoomManager.shared().voiceRoom_checkAndTurnOn(roomId: roomId)
        MediaThen.exhibitShared().modeColor(roomId: roomId)
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func giveAndTakeCard(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = LightViewController(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = saveer()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func inputFor(chatID: String, isFrom: RecordAlbumFromEnum = .Normal, completion: ((_ vc: BackTipsProtocol) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != TopReactiveCompatible.getXiaoMiID() {
        if chatID != TopReactiveCompatible.withOf() { // 系统消息
            //: ProgressHUD.show()
            ButtonProgressHUD.endDismiss()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: showMakeName.map{titleFrame(on: $0)}, encoding: .utf8)!: chatID, String(bytes: constManagerName.reversed(), encoding: .utf8)!: "0", String(bytes: showItemValue.map{$0^121}, encoding: .utf8)!: "1"]
            //: LabelUpReactiveCompatible.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            LabelUpReactiveCompatible.borderWrite(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = ProductTransformable.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = ProductTransformable.nextCover(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                PlayerClickThen.blockFinishTo(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: dataCloseName.map{ifDo(corner: $0)}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: user_playerLocationContent.map{managerSize(current: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: data_formatTitle.map{$0^219}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: userStyleName.map{$0^164}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: showObjectFormat,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.exceptRakeMsg(showMsg: (String(user_directionTitle.prefix(6)) + "t is r" + noti_titleText.replacingOccurrences(of: "add", with: "i") + "cted！").localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.sizeWrap(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: ProductTransformable())
                let chatVC = self.sizeWrap(chatID: chatID, isFrom: isFrom, userWrap: ProductTransformable())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ProductTransformable ) -> TalkingPrivateChatController {
    func sizeWrap(chatID: String, isFrom: RecordAlbumFromEnum = .Normal, userWrap: ProductTransformable) -> BackTipsProtocol {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = saveer()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: BackTipsProtocol.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! BackTipsProtocol
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = BackTipsProtocol(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func keyId(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()
        //: LabelUpReactiveCompatible.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        LabelUpReactiveCompatible.recordView(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            ButtonProgressHUD.galleryMake()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.exceptRakeMsg(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if TopReactiveCompatible.isGroupChat(groupId) {
            if TopReactiveCompatible.mankindApp(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.behindPresent(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = ShouldControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.saveer()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.behindPresent(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = ShouldControllerDelegate(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.saveer()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.exceptRakeMsg(showMsg: String(bytes: noti_pageData.map{timeFaceSkin(color: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func behindPresent(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.saveer()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: ShouldControllerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! ShouldControllerDelegate
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func instanceStatus(toast: String?) {
        //: let tpAuth = TPUserAuth(rawValue: SceneAppManager.share.loginUserMode.isTPAuth)
        let tpAuth = ShouldDefaultsSerializable(rawValue: SceneAppManager.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingFaceVerificationVC()
            let vc = ObjectStartViewController()
            //: vc.toastStr = toast ?? ""
            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = saveer()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingFinalVerificationVC()
            let vc = ReleaseVerificationVc()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = saveer()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func postVideo(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func keyVc() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = saveer()
        //: let vc = TalkingFeedbackVC.init()
        let vc = ContributionReactiveCompatible()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func toppleAction(jumpModel: ArrayMeasurable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (k_playContent.lowercased()) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            gestureName(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(main_eyeData.prefix(5)) + "etail") {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func underVc() {
    @objc public func underVc() {
        //: AccumulationThen.share.func__pushToWebVC(webViewType: .SubscribePage)
        AccumulationThen.share.cropViewArc(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func columnShared(type: SizeCollection, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = CopernicanSystemThen()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        saveer()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension AccumulationThen {
extension AccumulationThen {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func cropViewArc(webViewType: FrontMeasurable) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        everyDrop(webViewType: webViewType, webConfig: MakeConfig())
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func requestIn(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(const_moveTitle.suffix(6)) + user_informationSourceName.capitalized)) {
        //: let payWinType = SceneAppManager.share.appUserConfigMode.payWinType
        let payWinType = SceneAppManager.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)
        //: if payWinType == 1 || (SceneAppManager.share.loginUserMode.loungePlus && sufficient == false) {
        if payWinType == 1 || (SceneAppManager.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
            //: func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
            fee(webViewType: .RechargeHalfPage, clickEvent: clickEvent)

            //: } else if payWinType == 2 {
        } else if payWinType == 2 { // 会员订阅弹窗
            //: func__pushToSubscribeAlert(appendParams: appendParams)
            prod(appendParams: appendParams)
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func prod(appendParams: String = (String(const_moveTitle.suffix(6)) + user_informationSourceName.capitalized)) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = AllManager.contextShow(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: AccumulationThen.share.func__pushToWebVC(urlStr: urlStr)
        AccumulationThen.share.gestureName(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        noti_statusInviteScreenMessage.giveawayLoad(eventID: app_screenBackUrl, parameterStr: [String(bytes: main_whiteAspectTitle.reversed(), encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func fee(webViewType: FrontMeasurable, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = MakeConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        everyDrop(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            appIconFormat = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                noti_statusInviteScreenMessage.giveawayLoad(eventID: clickEvent, parameterStr: [String(bytes: main_whiteAspectTitle.reversed(), encoding: .utf8)!: 0])
            }
        }
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func everyDrop(webViewType: FrontMeasurable, webConfig: MakeConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = AllManager.contextShow(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        mentalImage(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func gestureName(urlStr: String?, webConfig: MakeConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? MakeConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        mentalImage(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func mentalImage(urlStr: String?, webViewType: FrontMeasurable?, webConfig: MakeConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(showWhoCollectionName.replacingOccurrences(of: "info", with: "s") + "parency")].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (k_atName.replacingOccurrences(of: "than", with: "ve") + String(show_contentData.suffix(6))) + "\(noti_deviceUrl)" + (String(notiGiftContent) + String(main_withName)) + "\(mainTableKey)" + (String(data_clearValue) + String(userDoingValue)) + "\(user_errorUrl)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = GroupActionMessageHandler(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = saveer() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: GroupActionMessageHandler.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! GroupActionMessageHandler).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.elementComponentRadius(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_clickBottomContent, height: main_clickBottomContent / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension AccumulationThen {
public extension AccumulationThen {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func saveer() -> UIViewController? {
        //: return currentViewController()
        return occupier()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct ArrayMeasurable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
