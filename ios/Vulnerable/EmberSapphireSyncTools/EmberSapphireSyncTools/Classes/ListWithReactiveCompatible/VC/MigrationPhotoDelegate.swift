
//: Declare String Begin

/*: "nil" :*/
fileprivate let user_spaceTitle:String = "momentil"

/*: "Edit profiles" :*/
fileprivate let user_pageHiddenName:String = "Edit to view error image old"
fileprivate let app_youTitle:String = "S"

/*: "Done" :*/
fileprivate let showNameValue:String = "title detail falseDone"

/*: "#999999" :*/
fileprivate let mainFrontName:String = "#resultresultresultresultresultresult"

/*: "uid" :*/
fileprivate let noti_titleValue:String = "numberid"

/*: "TaproomThen" :*/
fileprivate let dataSystemContent:String = "handle makeTalk"
fileprivate let notiTitleToData:String = "ditNamelse item"
fileprivate let const_cellData:String = "eCellextension category view the total"

/*: "AttentionSignCell" :*/
fileprivate let dataIndexViewName:[Character] = ["T","a","l","k","i","n","g","E","d","i"]
fileprivate let k_arrayManagerPlayName:[Character] = ["t","S","i","g","n","C","e","l","l"]

/*: "Save the change?" :*/
fileprivate let showToValue:String = "Saveequal main if"
fileprivate let notiPhoneTitle:String = " change?index return"

/*: "Cancel" :*/
fileprivate let mainViewTargetName:String = "any color post imageCancel"

/*: "Save" :*/
fileprivate let user_defineName:String = "Savevoice model for format"

/*: "nickname" :*/
fileprivate let const_firstData:String = "nicmodeam"
fileprivate let noti_talkDataName:String = "with"

/*: "birthday" :*/
fileprivate let dataRequestToTitle:[Character] = ["b","i","r","t","h","d","a","y"]

/*: "desc" :*/
fileprivate let userManagerData:[Character] = ["d","e","s","c"]

/*: "Modify the success" :*/
fileprivate let kGiftTotalInputData:String = "Modifygift false for app i"
fileprivate let showMakeData:String = "show make extension effect icon succes"
fileprivate let kVoiceData:String = "hide"

/*: "pic" :*/
fileprivate let app_intimateValue:[Character] = ["p","i","c"]

/*: "[ :*/
fileprivate let showQuantityData:String = "["

/*: ]" :*/
fileprivate let kButtonOfTitle:[Character] = ["]"]

/*: "pids" :*/
fileprivate let main_bottomValue:String = "pdatas"

/*: "aboutMe" :*/
fileprivate let user_edgeData:[Character] = ["a","b","o","u","t","M","e"]

/*: "interest" :*/
fileprivate let k_rangeContent:[Character] = ["i","n","t","e","r","e","s"]
fileprivate let dataMaxName:String = "T"

/*: "tagIds" :*/
fileprivate let noti_playerListName:String = "tagIdrange string"
fileprivate let kSexLocalName:[Character] = ["s"]

/*: "category" :*/
fileprivate let show_userName:String = "caengineego"
fileprivate let app_extentManagerData:[Character] = ["r","y"]

/*: "An error occurred, please try again" :*/
fileprivate let dataImageName:[UInt8] = [0x44,0x6b,0x25,0x60,0x77,0x77,0x6a,0x77,0x25,0x6a,0x66,0x66,0x70,0x77,0x77,0x60,0x61,0x29,0x25,0x75,0x69,0x60,0x64,0x76,0x60,0x25,0x71,0x77,0x7c,0x25,0x64,0x62,0x64,0x6c,0x6b]

/*: "imgUrl" :*/
fileprivate let kRankName:String = "share seat inimgUr"
fileprivate let appSucceedContent:[Character] = ["l"]

/*: "videoUrl" :*/
fileprivate let userHiddenValue:[Character] = ["v","i","d","e","o","U","r"]
fileprivate let dataToManagerText:[Character] = ["l"]

/*: "Video cover submitted successfully" :*/
fileprivate let mainSuiteTextContent:[UInt8] = [0x24,0x1b,0x16,0x17,0x1d,0x52,0x11,0x1d,0x4,0x17,0x0,0x52,0x1,0x7,0x10,0x1f,0x1b,0x6,0x6,0x17,0x16,0x52,0x1,0x7,0x11,0x11,0x17,0x1,0x1,0x14,0x7,0x1e,0x1e,0xb]

/*: "EventViewCell" :*/
fileprivate let dataColorName:[Character] = ["T","a","l","k","i","n","g","E","d","i"]
fileprivate let notiGroupName:[Character] = ["t","V","i","d","e","o","C","e","l","l"]

/*: "video_url" :*/
fileprivate let data_styleFormatTitle:[Character] = ["v","i","d","e","o","_","u","r","l"]

/*: "img_url" :*/
fileprivate let const_equalValue:String = "img_urlbird go"

/*: "status" :*/
fileprivate let k_buttonData:[Character] = ["s","t","a","t","u","s"]

/*: "TurnMakeReactiveCompatible" :*/
fileprivate let main_hiddenName:String = "model as mode menu returnTalkin"
fileprivate let notiToolData:String = "gEditPelse selected"
fileprivate let appRowValue:String = "hotoCellproduct quantity return shared"

/*: "AtBirthdayCell" :*/
fileprivate let noti_sizeAdValue:String = "view succeedTalkin"
fileprivate let showVideoText:String = "return fortBirth"

/*: "TurnEditMeCell" :*/
fileprivate let user_topName:[Character] = ["T","a","l","k","i","n","g","E","d","i","t","A","b","o","u","t","M"]
fileprivate let appColorName:String = "eCelleffect view click view type"

/*: "UITableViewCell" :*/
fileprivate let app_dataName:[Character] = ["U","I","T","a","b","l","e"]
fileprivate let app_partData:[Character] = ["V","i","e","w","C","e","l","l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MigrationPhotoDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/8/30.
//

//: import UIKit
import UIKit

//: class TalkingEditProfilesVC: TalkingBaseViewController {
class MigrationPhotoDelegate: EqualViewController {
	var requestPackageDictionary: [AnyHashable: String]?
	var swayerName: String?
	var slipUpName: String?

    //: var seleteAboutme: [UserSeleteTagModel] = []
    var seleteAboutme: [LabelModelType] = []
    //: var seleteInters: [UserSeleteTagModel] = []
    var seleteInters: [LabelModelType] = []

    //: var isdeleteAboutme = false
    var isdeleteAboutme = false
    //: var isdeleteInters = false
    var isdeleteInters = false

    //: var isTips = false
    var isTips = false

    //: var PhotoArray: [TalkingUserInfoGalleryModel] = []
    var PhotoArray: [ChemicalElementMeasurable] = []
    //: var seletePhotoArray: [UIImage] = []
    var seletePhotoArray: [UIImage] = []
    //: var DeletePhotoArray: [String] = []
    var DeletePhotoArray: [String] = []

    //: var nameStr: String = ""
    var nameStr: String = ""
    //: var birthday: String = ""
    var birthday: String = ""
    //: var signStr: String = "nil"
    var signStr: String = (user_spaceTitle.replacingOccurrences(of: "moment", with: "n"))

    //: private var VideoIconPath = ""
    private var VideoIconPath = ""
    //: private var videoPath = ""
    private var videoPath = ""
    //: private var videoStatus = -3
    private var videoStatus = -3 // 视频审核状态：0待审核 1审核通过 -1审核拒绝 -3未上传
    //: private var isChangeVideo = false
    private var isChangeVideo = false

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = false
        self.hideNavi = false
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.overAge()
        //: self.title = "Edit profiles".localized
        self.title = (String(user_pageHiddenName.prefix(5)) + "profile" + app_youTitle.lowercased()).localized

        //: let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        let btn = UIButton(frame: CGRect(x: 0, y: 0, width: 40, height: 18))
        //: btn.setTitle("Done".localized, for: .normal)
        btn.setTitle((String(showNameValue.suffix(4))).localized, for: .normal)
        //: btn.setTitleColor(UIColor.init(hex: "#999999"), for: .normal)
        btn.setTitleColor(UIColor(hex: (mainFrontName.replacingOccurrences(of: "result", with: "9"))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.handleFont(type: .Medium, fontSize: 15)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(channel), for: .touchUpInside)
        //: let item = UIBarButtonItem(customView: btn)
        let item = UIBarButtonItem(customView: btn)
        //: self.navigationItem.rightBarButtonItem=item
        self.navigationItem.rightBarButtonItem = item

        //: seleteAboutme = SceneAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = SceneAppManager.share.loginUserMode.aboutMe!
        //: seleteInters = SceneAppManager.share.loginUserMode.interest!
        seleteInters = SceneAppManager.share.loginUserMode.interest!
        //: designView()
        nearModeView()
        //: GetGallery()
        self.share()
        //: getVideoInfo()
        instance()

        //: NotificationCenter.default.addObserver(self, selector: #selector(keyboardShowBeHidden(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(replace(notification:)), name: UIResponder.keyboardWillShowNotification, object: nil)
    
            var previousPath = Bundle.main.path(forResource: "voting_trust", ofType: "json")
            if var previous = previousPath {

                
		if (previous.description == previous.uppercased() + "eat") && (previous.count > 20 && previous[previous.startIndex].isMathSymbol) {
		//: SWIFT_CUSTOM_DANGER
            if previous.hasPrefix(previous.uppercased() + "rift") {
                previous = ""
            }
		}

                previousPath = previous
            }
            if let previousPath = previousPath, self.slipUpName == nil {
                self.slipUpName = try? String(contentsOfFile: previousPath, encoding: .unicode)
            }
            if let slipUpName = self.slipUpName,
               let previousJsonData = slipUpName.data(using: .utf8),
               var previousDictionary = try? JSONSerialization.jsonObject(with: previousJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (previousDictionary.prefix(51).count == 95) && (previousDictionary.randomElement() is UIView) {
		//: SWIFT_CUSTOM_DANGER
            let varThirty = previousDictionary.suffix(from: previousDictionary.index(previousDictionary.startIndex, offsetBy: 97)).isEmpty
            previousDictionary.removeAll(keepingCapacity: varThirty)
		}

                if let previous = previousDictionary["lineCruel"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var previousLabel = UILabel()

                    
		if (previousLabel.motionEffects.count == 20) && (previousLabel.tag == 7268) {
		//: SWIFT_CUSTOM_DANGER
            if UIView.requiresConstraintBasedLayout {
                previousLabel.setNeedsLayout()
            }
		}

                    previousLabel.frame = btn.bounds.integral
                    previousLabel.text = previous
                    btn.addSubview(previousLabel)
                }
            }

	}

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    
            var henceVarPath = Bundle.main.path(forResource: "confidence_tie", ofType: "lottie")
            if var henceVar = henceVarPath {

                
		if (henceVar.last(where: { $0 == "x" }) == "#") && (henceVar.suffix(from: henceVar.startIndex) == henceVar.uppercased() + "mind") {
		//: SWIFT_CUSTOM_DANGER
            if henceVar >= henceVar.uppercased() + "mother" {
                print(henceVar)
            }
		}

                henceVarPath = henceVar
            }
            if let henceVarPath = henceVarPath,
               self.requestPackageDictionary == nil,
               let henceVarDictionary = NSDictionary(contentsOfFile: henceVarPath) as? Dictionary<AnyHashable, String> {
                self.requestPackageDictionary = henceVarDictionary
            }
            if let henceVarText = self.requestPackageDictionary?["outerSpiritual"],
               let henceVarPlaceholder = self.requestPackageDictionary?["parAgricultural"] {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var henceVarTextField = UITextField()
                henceVarTextField.frame = CGRect(x: 0, y: 0, width: 0, height: 0)
                henceVarTextField.text = henceVarText
                henceVarTextField.placeholder = henceVarPlaceholder

                
		if (henceVarTextField.clipsToBounds) && (henceVarTextField.subviews.count == 33) {
		//: SWIFT_CUSTOM_DANGER
            if henceVarTextField.constraintsAffectingLayout(for: .vertical).isEmpty {
                
            if #available(iOS 13.0, *) {
                if (henceVarTextField.editingInteractionConfiguration == .none) {
                    henceVarTextField.resignFirstResponder()
                }
            }

            }
		}

                self.view.addSubview(henceVarTextField)
            }

	}

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            var abortionOfImportPath = Bundle.main.path(forResource: "voting_trust", ofType: "json")
            if var abortionOfImport = abortionOfImportPath {

                
		if (!abortionOfImport.isEmpty && abortionOfImport.prefix(through: abortionOfImport.startIndex) == abortionOfImport.lowercased() + "mount") && (!abortionOfImport.isEmpty && abortionOfImport == abortionOfImport.capitalized + "screen") {
		//: SWIFT_CUSTOM_DANGER
            if abortionOfImport[abortionOfImport.endIndex].isHexDigit {
                abortionOfImport = ""
            }
		}

                abortionOfImportPath = abortionOfImport
            }
            if let abortionOfImportPath = abortionOfImportPath, self.swayerName == nil {
                self.swayerName = try? String(contentsOfFile: abortionOfImportPath, encoding: .unicode)
            }
            if let swayerName = self.swayerName,
               let abortionOfImportJsonData = swayerName.data(using: .utf8),
               var abortionOfImportDictionary = try? JSONSerialization.jsonObject(with: abortionOfImportJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (abortionOfImportDictionary.dropFirst().count == 54) && (abortionOfImportDictionary.randomElement() is UIView) {
		//: SWIFT_CUSTOM_DANGER
            UserDefaults.standard.set(abortionOfImportDictionary.underestimatedCount, forKey: "%u")
		}

                if let abortionOfImport = abortionOfImportDictionary["pleaDaily"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var abortionOfImportLabel = UILabel()

                    
		if (abortionOfImportLabel.superview != nil && !abortionOfImportLabel.isDescendant(of: abortionOfImportLabel.superview!)) && (abortionOfImportLabel.forLastBaselineLayout.center.x == 80.11) {
		//: SWIFT_CUSTOM_DANGER
            abortionOfImportLabel.invalidateIntrinsicContentSize()
		}

                    abortionOfImportLabel.frame = MainTable.bounds.offsetBy(dx: CGFloat(Double(MainTable.frame.size.height)), dy: CGFloat(MainTable.semanticContentAttribute.rawValue))
                    abortionOfImportLabel.text = abortionOfImport
                    MainTable.addSubview(abortionOfImportLabel)
                }
            }

	}

    //: deinit {
    deinit {
        //: ProgressHUD.dismiss()
        ButtonProgressHUD.galleryMake()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    		requestPackageDictionary = nil
		swayerName = nil
		slipUpName = nil

	}

    // MARK: - Lazy load

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()
}

// MARK: - Laod data

//: extension TalkingEditProfilesVC {
extension MigrationPhotoDelegate {
    //: func GetGallery() {
    func share() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = SceneAppManager.share.loginUserMode.userID
        dict[(noti_titleValue.replacingOccurrences(of: "number", with: "u"))] = SceneAppManager.share.loginUserMode.userID
        //: TalkingMeRequestTool.req_GalleryByUid(params: dict) { succeed, result, errorModel in
        PhysiognomyRequestTool.collection(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let array: Array =  result as! Array<Any>
                let array: Array = result as! [Any]
                //: if array.count>0 {
                if array.count > 0 {
                    //: self.PhotoArray.removeAll()
                    self.PhotoArray.removeAll()
                }
                //: if let datas = Array<TalkingUserInfoGalleryModel>.deserialize(from: array as? Array) {
                if let datas = Array<ChemicalElementMeasurable>.deserialize(from: array as? Array) {
                    //: self.PhotoArray.append(contentsOf: (datas as? [TalkingUserInfoGalleryModel])!)
                    self.PhotoArray.append(contentsOf: (datas as? [ChemicalElementMeasurable])!)
                }
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: override func naviPopback() {
    override func conversationResign() {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(dataSystemContent.suffix(4)) + "ingE" + String(notiTitleToData.prefix(6)) + String(const_cellData.prefix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: TaproomThen = i as! TaproomThen
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(dataIndexViewName) + String(k_arrayManagerPlayName)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: AttentionSignCell = i as! AttentionSignCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }
        //: if (nameStr.count > 0&&SceneAppManager.share.loginUserMode.nickname != nameStr)||( birthday.count > 0&&SceneAppManager.share.loginUserMode.birthday != self.birthday)||(signStr != "nil" && SceneAppManager.share.loginUserMode.signature != signStr)||self.seletePhotoArray.count>0||self.DeletePhotoArray.count>0||isdeleteAboutme||isdeleteInters || isChangeVideo {
        if (nameStr.count > 0 && SceneAppManager.share.loginUserMode.nickname != nameStr) || (birthday.count > 0 && SceneAppManager.share.loginUserMode.birthday != self.birthday) || (signStr != (user_spaceTitle.replacingOccurrences(of: "moment", with: "n")) && SceneAppManager.share.loginUserMode.signature != signStr) || self.seletePhotoArray.count > 0 || self.DeletePhotoArray.count > 0 || isdeleteAboutme || isdeleteInters || isChangeVideo {
            //: let config = ShowAlertConfig()
            let config = NumerateAlertConfig()
            //: config.alignment = .center
            config.alignment = .center
            //: TalkingAlertShow.customAlert(message: "Save the change?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Save".localized, leftBlock: { [weak self] in
            ShowThen.executeFor(message: (String(showToValue.prefix(4)) + " the" + String(notiPhoneTitle.prefix(8))).localized, leftBtnTitle: (String(mainViewTargetName.suffix(6))).localized, rightBtnTitle: (String(user_defineName.prefix(4))).localized, leftBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                ShowThen.imageLine()
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)

                //: }, rightBlock: { [weak self] in
            }, rightBlock: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: TalkingAlertShow.hideAlert()
                ShowThen.imageLine()
                //: self.uploadTool(isBack: true)
                self.detail(isBack: true)
                //: }, config: config)
            }, config: config)
            //: } else {
        } else {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    //: @objc func doneBtnClick() {
    @objc func channel() {
        //: uploadTool(isBack: false)
        detail(isBack: false)
    }

    //: func uploadTool(isBack: Bool) {
    func detail(isBack: Bool) {
        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(dataSystemContent.suffix(4)) + "ingE" + String(notiTitleToData.prefix(6)) + String(const_cellData.prefix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: TaproomThen = i as! TaproomThen
                //: namecell.nameTF.resignFirstResponder()
                namecell.nameTF.resignFirstResponder()
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(dataIndexViewName) + String(k_arrayManagerPlayName)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: AttentionSignCell = i as! AttentionSignCell
                //: namecell.textView.resignFirstResponder()
                namecell.textView.resignFirstResponder()
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()

        //: if nameStr.count > 0 && SceneAppManager.share.loginUserMode.nickname != nameStr {
        if nameStr.count > 0, SceneAppManager.share.loginUserMode.nickname != nameStr {
            //: dict["nickname"] = nameStr
            dict[(const_firstData.replacingOccurrences(of: "mode", with: "kn") + noti_talkDataName.replacingOccurrences(of: "with", with: "e"))] = nameStr
        }
        //: if birthday.count > 0 && SceneAppManager.share.loginUserMode.birthday != birthday {
        if birthday.count > 0, SceneAppManager.share.loginUserMode.birthday != birthday {
            //: dict["birthday"] = birthday
            dict[(String(dataRequestToTitle))] = birthday
        }
        //: if signStr != "nil" && SceneAppManager.share.loginUserMode.signature != signStr {
        if signStr != (user_spaceTitle.replacingOccurrences(of: "moment", with: "n")), SceneAppManager.share.loginUserMode.signature != signStr {
            //: dict["desc"] = signStr
            dict[(String(userManagerData))] = signStr
        }

        //: photoAndTagTool(BackT: isBack, isTips: dict.count == 0)
        output(BackT: isBack, isTips: dict.count == 0)

        //: if VideoIconPath.count>1 && videoPath.count>1 && isChangeVideo == true && SceneAppManager.share.loginUserMode.sex == Gender.female.rawValue {
        if VideoIconPath.count > 1, videoPath.count > 1, isChangeVideo == true, SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.female.rawValue {
            //: uploadVideo()
            television()
        }

        //: if dict.count == 0 {
        if dict.count == 0 {
            //: return
            return
        }

        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()

        //: TalkingMeRequestTool.req_UpdateUserInfo(params: dict) { [weak self] succeed, result, errorModel in
        PhysiognomyRequestTool.thrust(params: dict) { [weak self] succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            ButtonProgressHUD.galleryMake()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                self.constraintPremium(showMsg: (String(kGiftTotalInputData.prefix(6)) + " the" + String(showMakeData.suffix(7)) + kVoiceData.replacingOccurrences(of: "hide", with: "s")).localized)

                //: if self.nameStr.count > 0 {
                if self.nameStr.count > 0 {
                    //: SceneAppManager.share.loginUserMode.nickname = self.nameStr
                    SceneAppManager.share.loginUserMode.nickname = self.nameStr
                }
                //: if self.birthday.count > 0 {
                if self.birthday.count > 0 {
                    //: SceneAppManager.share.loginUserMode.birthday = self.birthday
                    SceneAppManager.share.loginUserMode.birthday = self.birthday
                }
                //: SceneAppManager.share.loginUserMode.signature = self.signStr
                SceneAppManager.share.loginUserMode.signature = self.signStr

                //: if isBack {
                if isBack {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }

                //: } else {
            } else {
                //: if errorModel?.errorCode != 100 {
                if errorModel?.errorCode != 100 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                    self.exceptRakeMsg(showMsg: errorModel!.errorMsg)
                }
            }
        }
    }

    //: func photoAndTagTool(BackT: Bool, isTips: Bool) {
    func output(BackT: Bool, isTips: Bool) {
        //: self.isTips = isTips
        self.isTips = isTips
        //: if self.DeletePhotoArray.count>0 {
        if self.DeletePhotoArray.count > 0 {
            // 先删再加
            //: deletePhoto()
            sizePhoto()
            //: } else {
        } else {
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: uploadPhoto()
                launch()
            }
        }

        //: if isdeleteAboutme {
        if isdeleteAboutme {
            //: deleteTag(type: 1)
            viewTo(type: 1)
        }
        //: if isdeleteInters {
        if isdeleteInters {
            //: deleteTag(type: 2)
            viewTo(type: 2)
        }
        //: if BackT {
        if BackT {
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
    }

    /// 上传相册
    //: func uploadPhoto() {
    func launch() {
        //: for i in 0..<seletePhotoArray.count {
        for i in 0 ..< seletePhotoArray.count {
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            let resultData: NSData = seletePhotoArray[i].compressedImageData()! as NSData
            //: dict["pic"] = resultData
            dict[(String(app_intimateValue))] = resultData
            //: ProgressHUD.show()
            ButtonProgressHUD.endDismiss()
            //: TalkingMeRequestTool.req_UploadPic(params: dict) { [weak self] succeed, result, errorModel in
            PhysiognomyRequestTool.largess(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if succeed {
                if succeed {
                    //: if  self.isTips {
                    if self.isTips {
                        //: self.isTips = false
                        self.isTips = false
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                        self.constraintPremium(showMsg: (String(kGiftTotalInputData.prefix(6)) + " the" + String(showMakeData.suffix(7)) + kVoiceData.replacingOccurrences(of: "hide", with: "s")).localized)
                    }
                    //: self.seletePhotoArray.removeAll()
                    self.seletePhotoArray.removeAll()
                    //: self.GetGallery()
                    self.share()
                }
            }
        }
    }

    /// 删除相册
    //: func deletePhoto() {
    func sizePhoto() {
        //: var str = DeletePhotoArray.joined(separator: ",")
        var str = DeletePhotoArray.joined(separator: ",")
        //: str = "[\(str)]"
        str = "[\(str)]"
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["pids"] = str
        dict[(main_bottomValue.replacingOccurrences(of: "data", with: "id"))] = str
        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()
        //: TalkingMeRequestTool.req_DeletePic(params: dict) { [weak self] succeed, result, errorModel in
        PhysiognomyRequestTool.atSource(params: dict) { [weak self] succeed, _, _ in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if self.seletePhotoArray.count>0 {
            if self.seletePhotoArray.count > 0 {
                //: self.uploadPhoto()
                self.launch()
                //: } else {
            } else {
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
            }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.constraintPremium(showMsg: (String(kGiftTotalInputData.prefix(6)) + " the" + String(showMakeData.suffix(7)) + kVoiceData.replacingOccurrences(of: "hide", with: "s")).localized)
                }

                //: self.DeletePhotoArray.removeAll()
                self.DeletePhotoArray.removeAll()
                //: self.GetGallery()
                self.share()
            }
        }
    }

    /// 修改tag
    //: func deleteTag(type: Int) {
    func viewTo(type: Int) {
        //: var array: [UserSeleteTagModel] = []
        var array: [LabelModelType] = []
        //: var category = ""
        var category = ""
        //: if type==1 {
        if type == 1 {
            //: array=seleteAboutme
            array = seleteAboutme
            //: category="aboutMe"
            category = (String(user_edgeData))
            //: } else {
        } else {
            //: array=seleteInters
            array = seleteInters
            //: category="interest"
            category = (String(k_rangeContent) + dataMaxName.lowercased())
        }
        //: var str = ""
        var str = ""
        //: for i in 0..<array.count {
        for i in 0 ..< array.count {
            //: let model = array[i]
            let model = array[i]
            //: str += model.tag_id!
            str += model.tag_id!
            //: if i<array.count-1 {
            if i < array.count - 1 {
                //: str += ","
                str += ","
            }
        }
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["tagIds"] = str
        dict[(String(noti_playerListName.prefix(5)) + String(kSexLocalName))] = str
        //: dict["category"] = category
        dict[(show_userName.replacingOccurrences(of: "engine", with: "t") + String(app_extentManagerData))] = category
        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()
        //: TalkingMeRequestTool.req_EditTag(params: dict) { [weak self] succeed, result, errorModel in
        PhysiognomyRequestTool.params(params: dict) { [weak self] succeed, _, _ in
            //: ProgressHUD.dismiss()
            ButtonProgressHUD.galleryMake()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: if  self.isTips {
                if self.isTips {
                    //: self.isTips = false
                    self.isTips = false
                    //: self.func__showStatusBarSuccessMsg(showMsg: "Modify the success".localized)
                    self.constraintPremium(showMsg: (String(kGiftTotalInputData.prefix(6)) + " the" + String(showMakeData.suffix(7)) + kVoiceData.replacingOccurrences(of: "hide", with: "s")).localized)
                }
                //: if type==1 {
                if type == 1 {
                    //: self.isdeleteAboutme = false
                    self.isdeleteAboutme = false
                    //: SceneAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    SceneAppManager.share.loginUserMode.aboutMe = self.seleteAboutme
                    //: } else {
                } else {
                    //: self.isdeleteInters = false
                    self.isdeleteInters = false
                    //: SceneAppManager.share.loginUserMode.interest = self.seleteInters
                    SceneAppManager.share.loginUserMode.interest = self.seleteInters
                }
            }
        }
    }

    /// 上传视频
    //: func uploadVideo() {
    func television() {
        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
        DirectionFaceVideoManager.shared.towardMake(type: 2, coverPath: self.VideoIconPath, videoPath: self.videoPath) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                self.exceptRakeMsg(showMsg: String(bytes: dataImageName.map{$0^5}, encoding: .utf8)!.localized)
                //: return
                return
            }

            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["imgUrl"] = coverUrl
            dict[(String(kRankName.suffix(5)) + String(appSucceedContent))] = coverUrl
            //: dict["videoUrl"] = videoUrl
            dict[(String(userHiddenValue) + String(dataToManagerText))] = videoUrl

            //: TalkingMeRequestTool.uploadVideoUserEdit(params: dict) { [weak self] succeed, result, errorModel in
            PhysiognomyRequestTool.playerCompletion(params: dict) { [weak self] succeed, _, _ in
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: guard let self = self else { return }
                guard let self = self else { return }

                //: guard succeed else {
                guard succeed else {
                    //: self.func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
                    self.exceptRakeMsg(showMsg: String(bytes: dataImageName.map{$0^5}, encoding: .utf8)!.localized)
                    //: return
                    return
                }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
                //: self.videoStatus = 0
                self.videoStatus = 0
                //: self.func__showStatusBarSuccessMsg(showMsg: "Video cover submitted successfully".localized)
                self.constraintPremium(showMsg: String(bytes: mainSuiteTextContent.map{$0^114}, encoding: .utf8)!.localized)
                //: for i in self.MainTable.visibleCells {
                for i in self.MainTable.visibleCells {
                    //: if i.reuseIdentifier == "TalkingEditVideoCell" {
                    if i.reuseIdentifier == (String(dataColorName) + String(notiGroupName)) {
                        //: let videocell: TalkingEditVideoCell = i as! TalkingEditVideoCell
                        let videocell: EventViewCell = i as! EventViewCell
                        //: videocell.upDateCellView(videoPlayPath: dict["videoUrl"] as! String)
                        videocell.pushStartPath(videoPlayPath: dict[(String(userHiddenValue) + String(dataToManagerText))] as! String)
                        //: videocell.setVideoStatusLB(status: self.videoStatus)
                        videocell.set(status: self.videoStatus)
                    }
                }
            }
        }
    }

    //: func getVideoInfo() {
    func instance() {
        //: TalkingMeRequestTool.getUploadVideoInfo { [weak self] succeed, result, errorModel in
        PhysiognomyRequestTool.getViewProgramVideoUploadWith { [weak self] succeed, result, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {return}
                guard let dict = result as? [String: Any] else { return }
                //: self.videoPath = dict["video_url"] as! String
                self.videoPath = dict[(String(data_styleFormatTitle))] as! String
                //: self.VideoIconPath = dict["img_url"] as! String
                self.VideoIconPath = dict[(String(const_equalValue.prefix(7)))] as! String
                //: self.videoStatus = dict["status"] as? Int ?? -3
                self.videoStatus = dict[(String(k_buttonData))] as? Int ?? -3
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }
}

// MARK: - Event

//: extension TalkingEditProfilesVC {
extension MigrationPhotoDelegate {
    //: @objc func keyboardShowBeHidden(notification: NSNotification) {
    @objc func replace(notification: NSNotification) {
        //: let info = notification.userInfo
        let info = notification.userInfo
        //: let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height
        let keyBoardHeight = (info![UIResponder.keyboardFrameEndUserInfoKey] as! NSValue).cgRectValue.size.height

        //: for i in MainTable.visibleCells {
        for i in MainTable.visibleCells {
            //: if i.reuseIdentifier == "TalkingEditNameCell" {
            if i.reuseIdentifier == (String(dataSystemContent.suffix(4)) + "ingE" + String(notiTitleToData.prefix(6)) + String(const_cellData.prefix(5))) {
                //: let namecell: TalkingEditNameCell = i as! TalkingEditNameCell
                let namecell: TaproomThen = i as! TaproomThen
                //: if namecell.nameTF.isFirstResponder {
                if namecell.nameTF.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
            //: if i.reuseIdentifier == "TalkingEditSignCell" {
            if i.reuseIdentifier == (String(dataIndexViewName) + String(k_arrayManagerPlayName)) {
                //: let namecell: TalkingEditSignCell = i as! TalkingEditSignCell
                let namecell: AttentionSignCell = i as! AttentionSignCell
                //: if namecell.textView.isFirstResponder {
                if namecell.textView.isFirstResponder {
                    //: MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY-keyBoardHeight), animated: true)
                    MainTable.setContentOffset(CGPoint(x: 0, y: namecell.frame.maxY - keyBoardHeight), animated: true)
                }
            }
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingEditProfilesVC: UITableViewDelegate, UITableViewDataSource {
extension MigrationPhotoDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 7
        return 7
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: if SceneAppManager.share.loginUserMode.sex == Gender.female.rawValue {
            if SceneAppManager.share.loginUserMode.sex == WithSchemaConvertible.female.rawValue {
                //: return 226
                return 226
            }
            //: return 0
            return 0

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let sizewh = Int((ScreenWidth-30-12*2-9*2)/3)
            let sizewh = Int((main_clickBottomContent - 30 - 12 * 2 - 9 * 2) / 3)
            //: let Y = 46+sizewh*3+18
            let Y = 46 + sizewh * 3 + 18
            //: let cellheiht = Y+12+26+15
            let cellheiht = Y + 12 + 26 + 15
            //: return CGFloat(cellheiht)
            return CGFloat(cellheiht)
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: return 107+15
            return 107 + 15
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: return 64
            return 64
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: return 176
            return 176
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: if indexPath.section == 5 && seleteAboutme.count>0 {
            if indexPath.section == 5 && seleteAboutme.count > 0 {
                //: return self.computeCellheight(type: 1)+64
                return self.sendAcross(type: 1) + 64
                //: } else if indexPath.section == 6 && seleteInters.count>0 {
            } else if indexPath.section == 6 && seleteInters.count > 0 {
                //: return self.computeCellheight(type: 2)+64
                return self.sendAcross(type: 2) + 64
            }
            //: return 64
            return 64
        }
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, estimatedHeightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, estimatedHeightForRowAt _: IndexPath) -> CGFloat {
        //: return 56
        return 56
    }

    //: func tableView(_ tableView: UITableView, heightForHeaderInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForHeaderInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.1
        return 0.1
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: if indexPath.section == 0 {
        if indexPath.section == 0 {
            //: let identifier = "TalkingEditVideoCell"
            let identifier = (String(dataColorName) + String(notiGroupName))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditVideoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? EventViewCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditVideoCell(style: .default, reuseIdentifier: identifier)
                cell = EventViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.setVideoStatusLB(status: self.videoStatus)
            cell?.set(status: self.videoStatus)
            //: if VideoIconPath.count>1 && videoPath.count>1 {
            if VideoIconPath.count > 1 && videoPath.count > 1 {
                //: cell?.setCellView(iconPath: VideoIconPath)
                cell?.search(iconPath: VideoIconPath)
                //: cell?.upDateCellView(videoPlayPath: videoPath)
                cell?.pushStartPath(videoPlayPath: videoPath)
            }
            //: cell?.seleteBlock = { [weak self] imagPath, VideoPath in
            cell?.seleteBlock = { [weak self] imagPath, VideoPath in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = imagPath
                self.VideoIconPath = imagPath
                //: self.videoPath = VideoPath
                self.videoPath = VideoPath
                //: self.isChangeVideo = true
                self.isChangeVideo = true
            }
            //: cell?.deleteBlock = { [weak self] in
            cell?.deleteBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.VideoIconPath = ""
                self.VideoIconPath = ""
                //: self.videoPath = ""
                self.videoPath = ""
                //: self.isChangeVideo = false
                self.isChangeVideo = false
            }
            //: return cell!
            return cell!

            //: } else if indexPath.section == 1 {
        } else if indexPath.section == 1 {
            //: let identifier = "TalkingEditPhotoCell"
            let identifier = (String(main_hiddenName.suffix(6)) + String(notiToolData.prefix(6)) + String(appRowValue.prefix(8)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditPhotoCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TurnMakeReactiveCompatible
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditPhotoCell(style: .default, reuseIdentifier: identifier)
                cell = TurnMakeReactiveCompatible(style: .default, reuseIdentifier: identifier)
            }
            //: if PhotoArray.count>0 {
            if PhotoArray.count > 0 {
                //: cell?.setPhoto(selete: PhotoArray)
                cell?.buttonBySelete(selete: PhotoArray)
                //: } else {
            } else {
                //: cell?.resetPhoto()
                cell?.current()
            }
            //: cell?.delegate = self
            cell?.delegate = self
            //: return cell!
            return cell!
            //: } else if indexPath.section == 2 {
        } else if indexPath.section == 2 {
            //: let identifier = "TalkingEditNameCell"
            let identifier = (String(dataSystemContent.suffix(4)) + "ingE" + String(notiTitleToData.prefix(6)) + String(const_cellData.prefix(5)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditNameCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TaproomThen
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditNameCell(style: .default, reuseIdentifier: identifier)
                cell = TaproomThen(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.nameBlock = { [weak self] name in
            cell?.nameBlock = { [weak self] name in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.nameStr = name
                self.nameStr = name
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 3 {
        } else if indexPath.section == 3 {
            //: let identifier = "TalkingEditBirthdayCell"
            let identifier = (String(noti_sizeAdValue.suffix(6)) + "gEdi" + String(showVideoText.suffix(6)) + "dayCell")
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditBirthdayCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AtBirthdayCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditBirthdayCell(style: .default, reuseIdentifier: identifier)
                cell = AtBirthdayCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.birthdayBlock = { [weak self] day in
            cell?.birthdayBlock = { [weak self] day in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.birthday = day
                self.birthday = day
            }
            //: return cell!
            return cell!
            //: } else if indexPath.section == 4 {
        } else if indexPath.section == 4 {
            //: let identifier = "TalkingEditSignCell"
            let identifier = (String(dataIndexViewName) + String(k_arrayManagerPlayName))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditSignCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? AttentionSignCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditSignCell(style: .default, reuseIdentifier: identifier)
                cell = AttentionSignCell(style: .default, reuseIdentifier: identifier)
            }
            //: cell?.signBlock = { [weak self] sign in
            cell?.signBlock = { [weak self] sign in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.signStr = sign
                self.signStr = sign
            }

            //: return cell!
            return cell!
            //: } else if indexPath.section == 5 || indexPath.section == 6 {
        } else if indexPath.section == 5 || indexPath.section == 6 {
            //: let identifier = "TalkingEditAboutMeCell"
            let identifier = (String(user_topName) + String(appColorName.prefix(5)))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TalkingEditAboutMeCell
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier) as? TurnEditMeCell
            //: if cell == nil {
            if cell == nil {
                //: cell = TalkingEditAboutMeCell(style: .default, reuseIdentifier: identifier)
                cell = TurnEditMeCell(style: .default, reuseIdentifier: identifier)
            }
            //: if indexPath.section == 5 {
            if indexPath.section == 5 {
                //: cell!.tagtype = .AboutMe
                cell!.tagtype = .AboutMe
                //: } else {
            } else {
                //: cell!.tagtype = .Interests
                cell!.tagtype = .Interests
            }
            //: cell?.setTitle()
            cell?.pathFor()
            //: if seleteAboutme.count>0 && indexPath.section == 5 {
            if seleteAboutme.count > 0 && indexPath.section == 5 {
                //: cell?.setMessage(seleteAboutme)
                cell?.notCheck(seleteAboutme)
            }
            //: if seleteInters.count>0 && indexPath.section == 6 {
            if seleteInters.count > 0 && indexPath.section == 6 {
                //: cell?.setMessage(seleteInters)
                cell?.notCheck(seleteInters)
            }
            //: cell?.editBtnBlock = { [weak self] in
            cell?.editBtnBlock = { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: let vc = TalkingEditTagVC.init()
                let vc = TurnTagVc()
                //: vc.delegate = self
                vc.delegate = self
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: vc.tagtype = .AboutMe
                    vc.tagtype = .AboutMe
                    //: } else {
                } else {
                    //: vc.tagtype = .Interests
                    vc.tagtype = .Interests
                }
                //: self.navigationController!.pushViewController(vc, animated: true)
                self.navigationController!.pushViewController(vc, animated: true)
            }
            //: cell?.deleteBlock = { [weak self] tag in
            cell?.deleteBlock = { [weak self] tag in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: if indexPath.section == 5 {
                if indexPath.section == 5 {
                    //: if  self.seleteAboutme.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteAboutme.contains(where: { $0.actionStop(compareTo: tag) }) {
                        //: self.seleteAboutme.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteAboutme.removeAll(where: { $0.actionStop(compareTo: tag) })
                        //: self.isdeleteAboutme = true
                        self.isdeleteAboutme = true
                    }
                    //: } else {
                } else {
                    //: if  self.seleteInters.contains(where: { $0.equals(compareTo: tag)}) {
                    if self.seleteInters.contains(where: { $0.actionStop(compareTo: tag) }) {
                        //: self.seleteInters.removeAll(where: {$0.equals(compareTo: tag)})
                        self.seleteInters.removeAll(where: { $0.actionStop(compareTo: tag) })
                        //: self.isdeleteInters = true
                        self.isdeleteInters = true
                    }
                }
            }
            //: return cell!
            return cell!
            //: } else {
        } else {
            //: let identifier = "UITableViewCell"
            let identifier = (String(app_dataName) + String(app_partData))
            //: var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            var cell = tableView.dequeueReusableCell(withIdentifier: identifier)
            //: if cell == nil {
            if cell == nil {
                //: cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
                cell = UITableViewCell(style: .default, reuseIdentifier: identifier)
            }
            //: return cell!
            return cell!
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    //: func computeCellheight(type: Int) -> CGFloat {
    func sendAcross(type: Int) -> CGFloat {
        //: let itemSpacing = CGFloat(8)
        let itemSpacing = CGFloat(8)
        //: var contentWidthInRow = CGFloat(0)
        var contentWidthInRow = CGFloat(0)
        //: var array = Array<UserSeleteTagModel>()
        var array = [LabelModelType]()
        //: if type == 1 {
        if type == 1 {
            //: array = self.seleteAboutme
            array = self.seleteAboutme
            //: } else {
        } else {
            //: array = self.seleteInters
            array = self.seleteInters
        }
        //: var contentHeightInRow = CGFloat(30)
        var contentHeightInRow = CGFloat(30)

        //: for item in array {
        for item in array {
            //: var tag_name = ""
            var tag_name = ""
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue {
            if CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.ar.rawValue {
                //: tag_name = item.tag_name_ar
                tag_name = item.tag_name_ar
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue {
            } else if CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.es.rawValue {
                //: tag_name = item.tag_name_es
                tag_name = item.tag_name_es
                //: } else if TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            } else if CurrentAddrTool.share.interfaceLang == AttentionNameLiteral.pt.rawValue {
                //: tag_name = item.tag_name_pt
                tag_name = item.tag_name_pt
            }
            //: if tag_name.count <= 0 {
            if tag_name.count <= 0 {
                //: tag_name = item.tag_name
                tag_name = item.tag_name
            }
            //: let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.pingfangFont(type: .Regular, fontSize: 15)], context: nil)
            let rect = (tag_name as NSString).boundingRect(with: .zero, options: .usesLineFragmentOrigin, attributes: [.font: UIFont.handleFont(type: .Regular, fontSize: 15)], context: nil)
            //: let tagWidth = rect.width + 30
            let tagWidth = rect.width + 30
            //: if contentWidthInRow + tagWidth + itemSpacing > ScreenWidth-54 {
            if contentWidthInRow + tagWidth + itemSpacing > main_clickBottomContent - 54 {
                // 需要换行
                //: contentHeightInRow += (10 + 30)
                contentHeightInRow += (10 + 30)
                //: contentWidthInRow = itemSpacing
                contentWidthInRow = itemSpacing
                //: } else {
            } else {
                //: contentWidthInRow += (tagWidth+itemSpacing)
                contentWidthInRow += (tagWidth + itemSpacing)
            }
        }
        //: if array.count == 10 {
        if array.count == 10 {
            //: contentHeightInRow += 40
            contentHeightInRow += 40
        }
        //: return contentHeightInRow + 12
        return contentHeightInRow + 12
    }
}

// MARK: - ExceptThen

//: extension TalkingEditProfilesVC: EditTagDelegate {
extension MigrationPhotoDelegate: ExceptThen {
    //: func freshSeleteTag() {
    func border() {
        //: seleteAboutme =  SceneAppManager.share.loginUserMode.aboutMe!
        seleteAboutme = SceneAppManager.share.loginUserMode.aboutMe!
        //: seleteInters =  SceneAppManager.share.loginUserMode.interest!
        seleteInters = SceneAppManager.share.loginUserMode.interest!
        //: self.MainTable .reloadData()
        self.MainTable.reloadData()
    }
}

// MARK: - PlayerCurrentThen

//: extension TalkingEditProfilesVC: EditPhotoDelegate {
extension MigrationPhotoDelegate: PlayerCurrentThen {
    /// 添加本地图片
    //: func addPhoto(_ icon: [UIImage]) {
    func showEnable(_ icon: [UIImage]) {
        //: for i in icon {
        for i in icon {
            //: self.seletePhotoArray.append(i)
            self.seletePhotoArray.append(i)
            //: var model = TalkingUserInfoGalleryModel.init()
            var model = ChemicalElementMeasurable()
            //: model.imagePic = i
            model.imagePic = i
            //: model.isLocal = true
            model.isLocal = true
            //: PhotoArray.append(model)
            PhotoArray.append(model)
        }
    }

    /// 删除本地相册选择的图片
    //: func deletePhoto(_ icon: UIImage) {
    func map(_ icon: UIImage) {
        //: if  seletePhotoArray.contains(where: {$0 == icon}) {
        if seletePhotoArray.contains(where: { $0 == icon }) {
            //: seletePhotoArray.removeAll(where: {$0 == icon})
            seletePhotoArray.removeAll(where: { $0 == icon })
        }
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if icon == PhotoArray[i].imagePic {
            if icon == PhotoArray[i].imagePic {
                //: seleteIndex =  i
                seleteIndex = i
                //: break
                break
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }

    /// 删除后台相册的图片
    //: func deletePhotoWithUid(_ iconUid: String) {
    func save(_ iconUid: String) {
        //: var seleteIndex = -1
        var seleteIndex = -1
        //: for i in 0..<PhotoArray.count {
        for i in 0 ..< PhotoArray.count {
            //: if iconUid == PhotoArray[i].uid {
            if iconUid == PhotoArray[i].uid {
                //: seleteIndex =  i
                seleteIndex = i
                //: self.DeletePhotoArray.append(iconUid)
                self.DeletePhotoArray.append(iconUid)
            }
        }
        //: if seleteIndex>=0 && seleteIndex<PhotoArray.count {
        if seleteIndex >= 0 && seleteIndex < PhotoArray.count {
            //: PhotoArray.remove(at: seleteIndex)
            PhotoArray.remove(at: seleteIndex)
            //: self.MainTable.reloadData()
            self.MainTable.reloadData()
        }
    }
}

// MARK: - UI

//: extension TalkingEditProfilesVC {
extension MigrationPhotoDelegate {
    //: private func designView() {
    private func nearModeView() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingEditVideoCell.self, forCellReuseIdentifier: "TalkingEditVideoCell")
        MainTable.register(EventViewCell.self, forCellReuseIdentifier: (String(dataColorName) + String(notiGroupName)))
        //: MainTable.register(TalkingEditPhotoCell.self, forCellReuseIdentifier: "TalkingEditPhotoCell")
        MainTable.register(TurnMakeReactiveCompatible.self, forCellReuseIdentifier: (String(main_hiddenName.suffix(6)) + String(notiToolData.prefix(6)) + String(appRowValue.prefix(8))))
        //: MainTable.register(TalkingEditNameCell.self, forCellReuseIdentifier: "TalkingEditNameCell")
        MainTable.register(TaproomThen.self, forCellReuseIdentifier: (String(dataSystemContent.suffix(4)) + "ingE" + String(notiTitleToData.prefix(6)) + String(const_cellData.prefix(5))))
        //: MainTable.register(TalkingEditBirthdayCell.self, forCellReuseIdentifier: "TalkingEditBirthdayCell")
        MainTable.register(AtBirthdayCell.self, forCellReuseIdentifier: (String(noti_sizeAdValue.suffix(6)) + "gEdi" + String(showVideoText.suffix(6)) + "dayCell"))
        //: MainTable.register(TalkingEditSignCell.self, forCellReuseIdentifier: "TalkingEditSignCell")
        MainTable.register(AttentionSignCell.self, forCellReuseIdentifier: (String(dataIndexViewName) + String(k_arrayManagerPlayName)))
        //: MainTable.register(TalkingEditAboutMeCell.self, forCellReuseIdentifier: "TalkingEditAboutMeCell")
        MainTable.register(TurnEditMeCell.self, forCellReuseIdentifier: (String(user_topName) + String(appColorName.prefix(5))))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
            //: make.top.equalTo(self.view).offset(20)
            make.top.equalTo(self.view).offset(20)
        }
    }
}
