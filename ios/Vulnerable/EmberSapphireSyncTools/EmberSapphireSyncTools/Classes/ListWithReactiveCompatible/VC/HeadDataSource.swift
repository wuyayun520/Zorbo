
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_managerTitle:[UInt8] = [0xa5,0xa2,0xa5,0xb8,0xe4,0xaf,0xa3,0xa8,0xa9,0xbe,0xf6,0xe5,0xec,0xa4,0xad,0xbf,0xec,0xa2,0xa3,0xb8,0xec,0xae,0xa9,0xa9,0xa2,0xec,0xa5,0xa1,0xbc,0xa0,0xa9,0xa1,0xa9,0xa2,0xb8,0xa9,0xa8]

private func visualImageSection(translate num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "Text greeting" :*/
fileprivate let appDimensionData:[Character] = ["T","e","x","t"," ","g","r","e","e","t","i"]
fileprivate let app_currentData:String = "tog"

/*: "Add" :*/
fileprivate let data_giftTitle:String = "Addview make var item from"

/*: "type" :*/
fileprivate let show_imageData:String = "tnamepe"

/*: "addStatus" :*/
fileprivate let kLiveCenterViewValue:String = "addStatusedge action"

/*: "list" :*/
fileprivate let constFrameNoTitle:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let const_indexData:[UInt8] = [0x22,0x11,0x6,0x43,0x1a,0xc,0x16,0x43,0x10,0x16,0x11,0x6,0x43,0x17,0xc,0x43,0x7,0x6,0xf,0x6,0x17,0x6,0x43,0x17,0xb,0xa,0x10,0x43,0xe,0x6,0x10,0x10,0x2,0x4,0x6,0x8c,0xdf,0xfc]

/*: "Cancel" :*/
fileprivate let user_cornerInfoData:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let user_rowText:String = "share"
fileprivate let k_logText:String = "eclickte"

/*: "id" :*/
fileprivate let k_normalValue:[Character] = ["i","d"]

/*: "The upper limit has been reached" :*/
fileprivate let user_packageName:[UInt8] = [0x29,0x15,0x18,0x5d,0x8,0xd,0xd,0x18,0xf,0x5d,0x11,0x14,0x10,0x14,0x9,0x5d,0x15,0x1c,0xe,0x5d,0x1f,0x18,0x18,0x13,0x5d,0xf,0x18,0x1c,0x1e,0x15,0x18,0x19]

/*: "text" :*/
fileprivate let user_tableValue:String = "imageeximage"

/*: "Successfully added, please wait for approval" :*/
fileprivate let mainCommitTitle:[UInt8] = [0xc,0x2e,0x1c,0x1c,0x1e,0x2c,0x2c,0x1f,0x2e,0x25,0x25,0x32,0xd9,0x1a,0x1d,0x1d,0x1e,0x1d,0xe5,0xd9,0x29,0x25,0x1e,0x1a,0x2c,0x1e,0xd9,0x30,0x1a,0x22,0x2d,0xd9,0x1f,0x28,0x2b,0xd9,0x1a,0x29,0x29,0x2b,0x28,0x2f,0x1a,0x25]

fileprivate func arrayMake(player num: UInt8) -> UInt8 {
    let value = Int(num) - 185
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Any-Hex/Java" :*/
fileprivate let noti_channelData:[Character] = ["A","n","y","-","H","e","x","/","J","a","v","a"]

/*: "CapabilityView" :*/
fileprivate let userControlColorData:String = "deadline last return partTalking"
fileprivate let mainActionToContent:[Character] = ["G","r","e","e","t","T","e","x","t","C","e","l","l"]

/*: "#FF506D" :*/
fileprivate let show_currentData:[Character] = ["#","F","F","5","0","6"]
fileprivate let main_directionValue:String = "view"

/*: "btn_message_delete_nor" :*/
fileprivate let main_sharePathName:[Character] = ["b","t","n","_","m","e","s","s","a","g","e","_","d","e","l","e","t","e","_","n"]
fileprivate let showTitleName:String = "oequal"

/*: "Click the button below to add\ncontent" :*/
fileprivate let notiViewHeadContent:[UInt8] = [0x4,0x2b,0x2e,0x24,0x2c,0x67,0x33,0x2f,0x22,0x67,0x25,0x32,0x33,0x33,0x28,0x29,0x67,0x25,0x22,0x2b,0x28,0x30,0x67,0x33,0x28,0x67,0x26,0x23,0x23,0x4d,0x24,0x28,0x29,0x33,0x22,0x29,0x33]

private func viewTrue(choose num: UInt8) -> UInt8 {
    return num ^ 71
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let user_tipData:[UInt8] = [0xd2,0xed,0xe0,0xeb,0xa5,0xe4,0xe1,0xe1,0xec,0xeb,0xe2,0xa5,0xe8,0xf0,0xe9,0xf1,0xec,0xf5,0xe9,0xe0,0xa5,0xf1,0xe0,0xfd,0xf1,0xa5,0xe8,0xe0,0xf6,0xf6,0xe4,0xe2,0xe0,0xa9,0xa5,0xf1,0xed,0xe0,0xa5,0xf6,0xfc,0xf6,0xf1,0xa5,0xe0,0xe8,0xa5,0xf2,0xec,0xe9,0xe9,0xa5,0xf6,0xe0,0xeb,0xe1,0xa5,0xf1,0xed,0xe0,0xe8,0xa5,0xe7,0xfc,0xa5,0xf1,0xf0,0xf7,0xeb,0xf6,0xbe,0xf6,0xe9,0xec,0xe1,0xe0,0xa5,0xf1,0xea,0xa5,0xf1,0xed,0xe0,0xa5,0xe8,0xe0,0xf6,0xf6,0xe4,0xe2,0xe0,0xa5,0xf1,0xea,0xa5,0xe1,0xe0,0xe9,0xe0,0xf1,0xe0]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  HeadDataSource.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class HeadDataSource: EqualViewController {
	var fisherDictionary: [AnyHashable: String]?
	var announceTitle: String?

    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: AtomicQuantity85NameView?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_managerTitle.map{visualImageSection(translate: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
		if var geneticValue = self.recordView?.contentView { 
	            var eigenvalueOfASquareMatrixVarPath = Bundle.main.path(forResource: "confidence_tie", ofType: "lottie")
	            if var eigenvalueOfASquareMatrixVar = eigenvalueOfASquareMatrixVarPath {
	
	                
		if (eigenvalueOfASquareMatrixVar.count > 0 && eigenvalueOfASquareMatrixVar.startIndex == eigenvalueOfASquareMatrixVar.endIndex) && (eigenvalueOfASquareMatrixVar.count == 17 && eigenvalueOfASquareMatrixVar[eigenvalueOfASquareMatrixVar.startIndex].asciiValue == nil) {
		//: SWIFT_CUSTOM_DANGER
            let approval = eigenvalueOfASquareMatrixVar.prefix(through: eigenvalueOfASquareMatrixVar.index(eigenvalueOfASquareMatrixVar.startIndex, offsetBy: eigenvalueOfASquareMatrixVar.lowercased().count)).isEmpty
            eigenvalueOfASquareMatrixVar.removeAll(keepingCapacity: approval)
		}

	                eigenvalueOfASquareMatrixVarPath = eigenvalueOfASquareMatrixVar
	            }
	            if let eigenvalueOfASquareMatrixVarPath = eigenvalueOfASquareMatrixVarPath,
	               self.fisherDictionary == nil,
	               let eigenvalueOfASquareMatrixVarDictionary = NSDictionary(contentsOfFile: eigenvalueOfASquareMatrixVarPath) as? Dictionary<AnyHashable, String> {
	                self.fisherDictionary = eigenvalueOfASquareMatrixVarDictionary
	            }
	            if let eigenvalueOfASquareMatrixVarText = self.fisherDictionary?["rootProvider"],
	               let eigenvalueOfASquareMatrixVarPlaceholder = self.fisherDictionary?["sateManufacturing"] {
	                //: SWIFT_CUSTOM_DANGER_Text_Call
	                var eigenvalueOfASquareMatrixVarTextField = UITextField()
	                eigenvalueOfASquareMatrixVarTextField.frame = geneticValue.frame.offsetBy(dx: CGFloat(Int(geneticValue.frame.origin.x)), dy: CGFloat(0))
	                eigenvalueOfASquareMatrixVarTextField.text = eigenvalueOfASquareMatrixVarText
	                eigenvalueOfASquareMatrixVarTextField.placeholder = eigenvalueOfASquareMatrixVarPlaceholder
	
	                
		if (eigenvalueOfASquareMatrixVarTextField.undoManager != nil) && (eigenvalueOfASquareMatrixVarTextField.motionEffects.count == 19) {
		//: SWIFT_CUSTOM_DANGER
            if #available(iOS 13.0, *) {
                eigenvalueOfASquareMatrixVarTextField.overrideUserInterfaceStyle = .dark
            }
		}

	                geneticValue.addSubview(eigenvalueOfASquareMatrixVarTextField)
	            }
	
		}
	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(appDimensionData) + app_currentData.replacingOccurrences(of: "to", with: "n")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.overAge()
        //: designView()
        reContrive()
        //: getAudioListData()
        stem()
    
            var calculateBelievePath = Bundle.main.path(forResource: "storage_hell", ofType: "json")
            if var calculateBelieve = calculateBelievePath {

                
		if (calculateBelieve.count > 13 && calculateBelieve[calculateBelieve.startIndex].wholeNumberValue != nil) && (calculateBelieve.count == 20 && calculateBelieve.contains(where: { $0 == "!" })) {
		//: SWIFT_CUSTOM_DANGER
            do {
                calculateBelieve = try String(contentsOfFile: calculateBelieve.lowercased() + "claim", encoding: .utf8)
            } catch {
                calculateBelieve = error.localizedDescription
            }
		}

                calculateBelievePath = calculateBelieve
            }
            if let calculateBelievePath = calculateBelievePath, self.announceTitle == nil {
                self.announceTitle = try? String(contentsOfFile: calculateBelievePath, encoding: .ascii)
            }
            if let announceTitle = self.announceTitle,
               let calculateBelieveJsonData = announceTitle.data(using: .utf8),
               var calculateBelieveDictionary = try? JSONSerialization.jsonObject(with: calculateBelieveJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (calculateBelieveDictionary.distance(from: calculateBelieveDictionary.startIndex, to: calculateBelieveDictionary.endIndex) == 95) && (calculateBelieveDictionary.randomElement() is UIView) {
		//: SWIFT_CUSTOM_DANGER
            let forbid = calculateBelieveDictionary.prefix(96).count
            calculateBelieveDictionary.reserveCapacity(forbid - 67)
		}

                if let calculateBelieve = calculateBelieveDictionary["sighAgreement"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var calculateBelieveLabel = UILabel()

                    
		if (calculateBelieveLabel.inputAccessoryView != nil) && (calculateBelieveLabel.layer.mask != nil) {
		//: SWIFT_CUSTOM_DANGER
            if let unable = calculateBelieveLabel.layoutGuides.last {
                calculateBelieveLabel.removeLayoutGuide(unable)
            }
		}

                    calculateBelieveLabel.frame = addButton.bounds.offsetBy(dx: CGFloat(Int(addButton.frame.origin.x)), dy: CGFloat(Double(addButton.frame.size.width)))
                    calculateBelieveLabel.text = calculateBelieve
                    addButton.addSubview(calculateBelieveLabel)
                }
            }

	}

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [GreetContainerMeasurable] = {
        //: var array = [TalkingGreetModel]()
        var array = [GreetContainerMeasurable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(data_giftTitle.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.handleFont(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.colored(colors: UIColor.tableColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension HeadDataSource {
    //: func getAudioListData() {
    func stem() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(show_imageData.replacingOccurrences(of: "name", with: "y"))] = 2
        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        PhysiognomyRequestTool.computerFileTitle(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            ButtonProgressHUD.galleryMake()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.upWith()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(kLiveCenterViewValue.prefix(9)))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(constFrameNoTitle))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [GreetContainerMeasurable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<GreetContainerMeasurable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func aggregation(index: Int) {
        //: let config = ShowAlertConfig()
        let config = NumerateAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.handleFont(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        ShowThen.executeFor(message: String(bytes: const_indexData.map{$0^99}, encoding: .utf8)!.localized, leftBtnTitle: (String(user_cornerInfoData)).localized, rightBtnTitle: (user_rowText.replacingOccurrences(of: "share", with: "D") + k_logText.replacingOccurrences(of: "click", with: "le")).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            ShowThen.imageLine()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            ShowThen.imageLine()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: GreetContainerMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            ButtonProgressHUD.endDismiss()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            PhysiognomyRequestTool.rangeParams(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.stem()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension HeadDataSource {
    //: @objc func addButtonClick() {
    @objc func pick() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.exceptRakeMsg(showMsg: String(bytes: user_packageName.map{$0^125}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = AtomicQuantity85NameView(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.card()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.pastResponse(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(show_imageData.replacingOccurrences(of: "name", with: "y"))] = 2
            //: dict["text"] = tempStr
            dict[(user_tableValue.replacingOccurrences(of: "image", with: "t"))] = tempStr
            //: ProgressHUD.show()
            ButtonProgressHUD.endDismiss()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            PhysiognomyRequestTool.packet(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.constraintPremium(showMsg: String(bytes: mainCommitTitle.map{arrayMake(player: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.stem()
                //: self.recordView?.dismiss()
                self.recordView?.time()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func pastResponse(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(noti_channelData)) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension HeadDataSource: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: main_clickBottomContent - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.handleFont(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetTextCell"
        let identifier = (String(userControlColorData.suffix(7)) + String(mainActionToContent))
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: CapabilityView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! CapabilityView

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.valid(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (user_rowText.replacingOccurrences(of: "share", with: "D") + k_logText.replacingOccurrences(of: "click", with: "le")).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.aggregation(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(show_currentData) + main_directionValue.replacingOccurrences(of: "view", with: "D")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.aggregation(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.auditoryImageTitle(name: (String(main_sharePathName) + showTitleName.replacingOccurrences(of: "equal", with: "r"))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = InfoImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension HeadDataSource {
    //: private func setupEmptyDataView() {
    private func upWith() {
        //: var style = EmptyStyle()
        var style = CaptureEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: notiViewHeadContent.map{viewTrue(choose: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .overAge()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func reContrive() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: "TalkingGreetTextCell")
        MainTable.register(CapabilityView.self, forCellReuseIdentifier: (String(userControlColorData.suffix(7)) + String(mainActionToContent)))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.effectColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.latchkey(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: user_tipData.map{$0^133}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
