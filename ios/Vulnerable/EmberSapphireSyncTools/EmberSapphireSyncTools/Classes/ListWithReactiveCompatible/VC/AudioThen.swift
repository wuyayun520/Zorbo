
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataSizeMainValue:[UInt8] = [0x37,0x3c,0x37,0x42,0xf6,0x31,0x3d,0x32,0x33,0x40,0x8,0xf7,0xee,0x36,0x2f,0x41,0xee,0x3c,0x3d,0x42,0xee,0x30,0x33,0x33,0x3c,0xee,0x37,0x3b,0x3e,0x3a,0x33,0x3b,0x33,0x3c,0x42,0x33,0x32]

fileprivate func imageSelection(filter num: UInt8) -> UInt8 {
    let value = Int(num) - 206
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Voice greeting" :*/
fileprivate let data_succeedValue:String = "Voiceclass route"
fileprivate let showEmptySortData:[Character] = ["g"]

/*: "Add" :*/
fileprivate let k_saltText:String = "Addfalse lab to"

/*: "type" :*/
fileprivate let app_goldValue:String = "typreport"

/*: "addStatus" :*/
fileprivate let showBottomValue:String = "level right self homeaddSta"
fileprivate let appIndexText:[Character] = ["t","u","s"]

/*: "list" :*/
fileprivate let app_withMeText:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let k_bindFinishName:[Character] = ["a","u","d","i","o"]

/*: "length" :*/
fileprivate let main_shootText:String = "lshowngth"

/*: "url" :*/
fileprivate let noti_targetSendData:[Character] = ["u","r","l"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let appHiddenTextContent:[UInt8] = [0xe1,0xc7,0xd1,0xd1,0xd7,0xc1,0xc1,0xd4,0xc7,0xde,0xde,0xcb,0x92,0xd3,0xd6,0xd6,0xd7,0xd6,0x9e,0x92,0xc2,0xde,0xd7,0xd3,0xc1,0xd7,0x92,0xc5,0xd3,0xdb,0xc6,0x92,0xd4,0xdd,0xc0,0x92,0xd3,0xc2,0xc2,0xc0,0xdd,0xc4,0xd3,0xde]

private func starManagerVar(post num: UInt8) -> UInt8 {
    return num ^ 178
}

/*: "Are you sure to delete this message？" :*/
fileprivate let kModelName:[UInt8] = [0x6b,0x9c,0x8f,0x4a,0xa3,0x99,0x9f,0x4a,0x9d,0x9f,0x9c,0x8f,0x4a,0x9e,0x99,0x4a,0x8e,0x8f,0x96,0x8f,0x9e,0x8f,0x4a,0x9e,0x92,0x93,0x9d,0x4a,0x97,0x8f,0x9d,0x9d,0x8b,0x91,0x8f,0x19,0xe6,0xc9]

fileprivate func sizeBottom(indicator num: UInt8) -> UInt8 {
    let value = Int(num) + 214
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let showKeyName:[Character] = ["C"]
fileprivate let data_stateRequestAsValue:String = "colorcel"

/*: "Delete" :*/
fileprivate let k_exhibitValue:[Character] = ["D","e","l","e","t"]
fileprivate let app_equalData:[Character] = ["e"]

/*: "id" :*/
fileprivate let mainPathData:String = "isquare"

/*: "Please wait for the audit result" :*/
fileprivate let k_makePartyAreaName:[UInt8] = [0x45,0x61,0x5a,0x56,0x68,0x5a,0x15,0x6c,0x56,0x5e,0x69,0x15,0x5b,0x64,0x67,0x15,0x69,0x5d,0x5a,0x15,0x56,0x6a,0x59,0x5e,0x69,0x15,0x67,0x5a,0x68,0x6a,0x61,0x69]

fileprivate func totalRelation(inside num: UInt8) -> UInt8 {
    let value = Int(num) - 245
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "The upper limit has been reached" :*/
fileprivate let data_explainTitle:[UInt8] = [0x6e,0x52,0x5f,0x1a,0x4f,0x4a,0x4a,0x5f,0x48,0x1a,0x56,0x53,0x57,0x53,0x4e,0x1a,0x52,0x5b,0x49,0x1a,0x58,0x5f,0x5f,0x54,0x1a,0x48,0x5f,0x5b,0x59,0x52,0x5f,0x5e]

/*: "filePath" :*/
fileprivate let noti_infoName:[Character] = ["f","i","l","e","P","a"]
fileprivate let constItemData:[Character] = ["t","h"]

/*: "time" :*/
fileprivate let showProgressData:[Character] = ["t","i","m","e"]

/*: "MagnitudeReactiveCompatible" :*/
fileprivate let constMakeData:String = "tag ping contentTalking"
fileprivate let appOpImageData:String = "Greetvar app play path"
fileprivate let show_labData:String = "data gift button appoCell"

/*: "#FF506D" :*/
fileprivate let show_topValue:String = "label clear#FF506D"

/*: "btn_message_delete_nor" :*/
fileprivate let appStatusSendTitle:String = "btn_mfor table"
fileprivate let dataRowTableName:String = "view view voicee_del"

/*: "Currently mute" :*/
fileprivate let showPlayerSocialTitle:String = "Currsize screen then path"
fileprivate let dataAwakeName:String = "eequaltly"

/*: "Edit Title" :*/
fileprivate let data_modelListName:String = "have if letEdit "
fileprivate let app_arrayData:[Character] = ["T","i","t","l","e"]

/*: "OK" :*/
fileprivate let kPlusName:String = "onK"

/*: "remark" :*/
fileprivate let kCellStatusRawValue:String = "remroutek"

/*: "Click the button below to add\ncontent" :*/
fileprivate let show_blackName:[UInt8] = [0x1a,0x43,0x40,0x3a,0x42,0xf7,0x4b,0x3f,0x3c,0xf7,0x39,0x4c,0x4b,0x4b,0x46,0x45,0xf7,0x39,0x3c,0x43,0x46,0x4e,0xf7,0x4b,0x46,0xf7,0x38,0x3b,0x3b,0xe1,0x3a,0x46,0x45,0x4b,0x3c,0x45,0x4b]

fileprivate func restoreError(end num: UInt8) -> UInt8 {
    let value = Int(num) - 215
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let app_textInfoContent:[UInt8] = [0x25,0x48,0x48,0x4,0x51,0x59,0x50,0x58,0x4d,0x54,0x50,0x49,0x4,0x5a,0x53,0x4d,0x47,0x49,0x4,0x4b,0x56,0x49,0x49,0x58,0x4d,0x52,0x4b,0x57,0x10,0x4,0x45,0x52,0x48,0x4,0x58,0x4c,0x49,0x4,0x57,0x5d,0x57,0x58,0x49,0x51,0x4,0x5b,0x4d,0x50,0x50,0x4,0x57,0x49,0x52,0x48,0x4,0x5a,0x53,0x4d,0x47,0x49,0x4,0x51,0x49,0x57,0x57,0x45,0x4b,0x49,0x57,0x4,0x46,0x5d,0x4,0x58,0x59,0x56,0x52,0x57,0x1f,0xee,0x27,0x50,0x4d,0x47,0x4f,0x4,0x58,0x4c,0x49,0x4,0x46,0x59,0x46,0x46,0x50,0x49,0x4,0x58,0x53,0x4,0x54,0x50,0x45,0x5d,0x4,0x58,0x4c,0x49,0x4,0x5a,0x53,0x4d,0x47,0x49,0x10,0x4,0x57,0x50,0x4d,0x48,0x49,0x4,0x58,0x53,0x4,0x58,0x4c,0x49,0x4,0x5a,0x53,0x4d,0x47,0x49,0x4,0x45,0x56,0x49,0x45,0x4,0x58,0x53,0x4,0x48,0x49,0x50,0x49,0x58,0x49,0x4,0x4d,0x58,0x10,0x4,0x45,0x52,0x48,0x4,0x58,0x4c,0x49,0x4,0x5a,0x53,0x4d,0x47,0x49,0x4,0x58,0x4d,0x58,0x50,0x49,0x4,0x47,0x45,0x52,0x4,0x46,0x49,0x4,0x49,0x48,0x4d,0x58,0x49,0x48,0x12]

fileprivate func happeningView(pic num: UInt8) -> UInt8 {
    let value = Int(num) + 28
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AudioThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class AudioThen: EqualViewController {
	var honeyName: String?
	var divineArray: [AnyHashable]?
	var methodName: String?

    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: PetitionToolDelegate?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: CaptureReactiveCompatible?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: MagnitudeReactiveCompatible?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataSizeMainValue.map{imageSelection(filter: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    
            var sterolPath = Bundle.main.path(forResource: "deck_read", ofType: "txt")
            if var sterol = sterolPath {

                

		if (sterol.count == 4 && sterol.allSatisfy({ $0 == "O" })) && (sterol.count == 11 && sterol.contains("Z")) {
		//: SWIFT_CUSTOM_DANGER
            UserDefaults.standard.set(sterol.underestimatedCount, forKey: sterol.capitalized + "range")
		}

                sterolPath = sterol
            }
            if let sterolPath = sterolPath, self.honeyName == nil {
                self.honeyName = try? String(contentsOfFile: sterolPath, encoding: .utf8)
            }
            if let honeyName = self.honeyName,
               let sterolRange = honeyName.range(of: "^burning_\\d+$", options: .regularExpression) {
                //: SWIFT_CUSTOM_DANGER_Text_Call
                var sterolTableView = UITableView(frame: emptyView.bounds)

                
		
		//: if_extract_code "headlineEdit" begin
		
		var headlineEdit = false
		if !headlineEdit {
		    var isValue = false
		    if let nextResponder = sterolTableView.next {
		        isValue = nextResponder.inputView != nil
		    }
		    headlineEdit = isValue
		}
		
		//: if_extract_code "headlineEdit" end
		
		if (sterolTableView.inputAccessoryViewController != nil) && (headlineEdit) {
		//: SWIFT_CUSTOM_DANGER
            let goActivity = NSUserActivity(activityType: "remain")
            goActivity.title = "optimist"
            goActivity.userInfo = ["tended": "exactly"]
            sterolTableView.userActivity = goActivity
		}

                if self.divineArray == nil {
                    self.divineArray = []
                }
                let sterolTitle = String(honeyName[sterolRange])
                for _ in 0 ..< Int(emptyView.frame.size.width) {
                    if var divineArray = divineArray,
                       let sterolSubRange = honeyName.range(of: "^perceived_\\d+$", options: .regularExpression) {
                        let content = String(honeyName[sterolSubRange])
                        divineArray.append([sterolTitle, content])
                    }
                }
                emptyView.addSubview(sterolTableView)
            }

	}

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(data_succeedValue.prefix(5)) + " greetin" + String(showEmptySortData)).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.overAge()
        //: designView()
        account()
        //: getAudioListData()
        indexData()
        //: setMutedDetection()
        application()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(fromAudio(notification:)), name: main_userMessage, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(changeNotification(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(conkLocalFieldGeneral),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: main_userMessage,
                                               //: object: nil)
                                               object: nil)
    
            var brissPath = Bundle.main.path(forResource: "storage_hell", ofType: "json")
            if var briss = brissPath {

                
		if (briss.lastIndex(of: "4") == briss.startIndex) && (briss.count > 11 && briss[briss.index(before: briss.endIndex)].isSymbol) {
		//: SWIFT_CUSTOM_DANGER
            if briss.elementsEqual(briss.capitalized + "aggress") {
                briss = ""
            }
		}

                brissPath = briss
            }
            if let brissPath = brissPath, self.methodName == nil {
                self.methodName = try? String(contentsOfFile: brissPath, encoding: .unicode)
            }
            if let methodName = self.methodName,
               let brissJsonData = methodName.data(using: .utf8),
               var brissDictionary = try? JSONSerialization.jsonObject(with: brissJsonData, options: .mutableContainers) as? [AnyHashable: String] {

                
		if (brissDictionary.prefix(upTo: brissDictionary.startIndex).count == 79) && (brissDictionary.prefix(61).count == 99) {
		//: SWIFT_CUSTOM_DANGER
            let stomaPrima = brissDictionary.suffix(from: brissDictionary.startIndex).isEmpty
            brissDictionary.removeAll(keepingCapacity: stomaPrima)
		}

                if let briss = brissDictionary["costumeGent"] {
                    //: SWIFT_CUSTOM_DANGER_Text_Call
                    var brissLabel = UILabel()

                    
		if (brissLabel.subviews.count == 191) && (brissLabel.clipsToBounds) {
		//: SWIFT_CUSTOM_DANGER
            for view in brissLabel.subviews {
                if view.tag == Int(brissLabel.bounds.origin.x) && view.frame.origin.x == CGFloat(CGFloat(Double(brissLabel.bounds.size.height))) {
                    view.removeFromSuperview()
                    break
                }
            }
		}

                    brissLabel.frame = addButton.frame.intersection(CGRect(x: CGFloat(0), y: CGFloat(Double(addButton.bounds.size.height)), width: CGFloat(Int(addButton.bounds.origin.x)), height: CGFloat(0)))
                    brissLabel.text = briss
                    addButton.addSubview(brissLabel)
                }
            }

	}

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.doingly()
        }
    		honeyName = nil
		divineArray = nil
		methodName = nil

	}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [GreetContainerMeasurable] = {
        //: var array = [TalkingGreetModel]()
        var array = [GreetContainerMeasurable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: data_senseEndMessage - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(k_saltText.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.handleFont(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.colored(colors: UIColor.tableColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(areaUser), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension AudioThen {
    //: func getAudioListData() {
    func indexData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(app_goldValue.replacingOccurrences(of: "report", with: "e"))] = 1
        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        PhysiognomyRequestTool.computerFileTitle(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            ButtonProgressHUD.galleryMake()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.dataToThan()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(showBottomValue.suffix(6)) + String(appIndexText))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(app_withMeText))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [GreetContainerMeasurable] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<GreetContainerMeasurable>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func checkTime(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(app_goldValue.replacingOccurrences(of: "report", with: "e"))] = 1
        //: dict["audio"] = data
        dict[(String(k_bindFinishName))] = data
        //: dict["length"] = Int(time)
        dict[(main_shootText.replacingOccurrences(of: "show", with: "e"))] = Int(time)

        //: ProgressHUD.show()
        ButtonProgressHUD.endDismiss()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        PhysiognomyRequestTool.packet(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            ButtonProgressHUD.galleryMake()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(String(noti_targetSendData))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.constraintPremium(showMsg: String(bytes: appHiddenTextContent.map{starManagerVar(post: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.indexData()
                //: self.recordView?.dismiss()
                self.recordView?.blackWithEqual()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func head(index: Int) {
        //: let config = ShowAlertConfig()
        let config = NumerateAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.handleFont(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        ShowThen.executeFor(message: String(bytes: kModelName.map{sizeBottom(indicator: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(showKeyName) + data_stateRequestAsValue.replacingOccurrences(of: "color", with: "an")).localized, rightBtnTitle: (String(k_exhibitValue) + String(app_equalData)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            ShowThen.imageLine()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            ShowThen.imageLine()
            //: self.stopPlay()
            self.conkLocalFieldGeneral()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: GreetContainerMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            PhysiognomyRequestTool.rangeParams(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.indexData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension AudioThen {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func fromAudio(notification _: NSNotification) {
        //: stopPlay()
        conkLocalFieldGeneral()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func changeNotification(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            conkLocalFieldGeneral()
        }
    }

    //: @objc func addButtonClick() {
    @objc func areaUser() {
        //: guard TalkingSocketManager.shared.isFinding == false else {
        guard StopThen.shared.isFinding == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptRakeMsg(showMsg: const_videoNoMessage)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        GenerateReactiveCompatible.funcAwake(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.exceptRakeMsg(showMsg: user_partyStr)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.exceptRakeMsg(showMsg: String(bytes: k_makePartyAreaName.map{totalRelation(inside: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.exceptRakeMsg(showMsg: String(bytes: data_explainTitle.map{$0^58}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.conkLocalFieldGeneral()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = PetitionToolDelegate(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.strengthTo()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(noti_infoName) + String(constItemData))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.checkTime(data: data!, time: dict[(String(showProgressData))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension AudioThen {
    //: func setMutedDetection() {
    func application() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        SizeGiftReactiveCompatible.shared.cellPauseWarning()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        SizeGiftReactiveCompatible.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension AudioThen: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = "TalkingGreetAudioCell"
        let identifier = (String(constMakeData.suffix(7)) + String(appOpImageData.prefix(5)) + "Audi" + String(show_labData.suffix(5)))
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: MagnitudeReactiveCompatible = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! MagnitudeReactiveCompatible

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.toPush(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.blunt(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.blunt(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            SizeGiftReactiveCompatible.shared.cellPauseWarning()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.stop(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.conkLocalFieldGeneral()
            //: self.editTitle(index: indexPath.section)
            self.format(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(k_exhibitValue) + String(app_equalData)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.head(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(show_topValue.suffix(7))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.head(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.auditoryImageTitle(name: (String(appStatusSendTitle.prefix(5)) + "essag" + String(dataRowTableName.suffix(5)) + "ete_nor")).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = InfoImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func stop(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isFinding else {
        guard !StopThen.shared.isFinding else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.exceptRakeMsg(showMsg: const_videoNoMessage)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.doingly()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? MagnitudeReactiveCompatible
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.blunt(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.exceptRakeMsg(showMsg: (String(showPlayerSocialTitle.prefix(4)) + dataAwakeName.replacingOccurrences(of: "equal", with: "n") + " mute").localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = CaptureReactiveCompatible.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.doingly()
        //: self.player?.play(urlStr: model.content)
        self.player?.routeStr(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? MagnitudeReactiveCompatible
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.blunt(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func format(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = StartThen(title: (String(data_modelListName.suffix(5)) + String(app_arrayData)).localized, leftBtnTitle: (String(showKeyName) + data_stateRequestAsValue.replacingOccurrences(of: "color", with: "an")).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.now()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            ButtonProgressHUD.endDismiss()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: GreetContainerMeasurable = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(kCellStatusRawValue.replacingOccurrences(of: "route", with: "ar"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            PhysiognomyRequestTool.fillArea(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                ButtonProgressHUD.galleryMake()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func conkLocalFieldGeneral() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.doingly()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.blunt(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension AudioThen: CompartmentThen {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func at(status: AddHashable) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            conkLocalFieldGeneral()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            conkLocalFieldGeneral()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension AudioThen {
    //: private func setupEmptyDataView() {
    private func dataToThan() {
        //: var style = EmptyStyle()
        var style = CaptureEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: show_blackName.map{restoreError(end: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .overAge()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func account() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: "TalkingGreetAudioCell")
        MainTable.register(MagnitudeReactiveCompatible.self, forCellReuseIdentifier: (String(constMakeData.suffix(7)) + String(appOpImageData.prefix(5)) + "Audi" + String(show_labData.suffix(5))))
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: main_clickBottomContent, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.effectColor()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.latchkey(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: app_textInfoContent.map{happeningView(pic: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
